# CareMetric CareBase — Living Backlog

**Status:** Canonical forward backlog
**Last verified against main:** `a8c719b` (2026-08-14), reviewed on branch `claude/app-wide-code-review-7hrzrz`: second tranche of the same review pass, after the first went green in CI. **One client rule was looser than the server's, and one page graded a facility with other facilities' records.** `revoke_class_checkin_tokens` refuses a reason under ten characters; ClassDetail's Revoke button enabled at five, so a trainer revoking a leaked QR check-in code got an enabled button, a raw Postgres error, and every outstanding code still working -- on the one control whose own copy says a leaked code keeps working until you revoke it. The rule moved into `lib/classCheckinTokens.ts` pinned to the migration by a test that reads the SQL, the shape `completedClassCorrection.ts` already uses for the two correction RPCs in that same migration. Swept the whole surface for siblings: every other client-side length guard on a bounded RPC text argument is at or above what the server enforces, and no client status literal falls outside its column's CHECK set. **RegulatoryCrosswalk** derived `activeFacilityId` as `facilityId || facilities?.[0]?.id || ""`, and every list hook applies its facilityId filter only `if` truthy -- so the five sources that were not gated on `Boolean(activeFacilityId)` (resident compliance items, incidents, corrective actions, inspection items, violations) swept the whole organization on first paint while the three that were gated returned nothing. `buildRegulatoryCrosswalkRows` runs on whatever has arrived, so that paint rendered a full Chapter 2600/2800 crosswalk -- per-citation status, evidence counts, open gaps, due dates -- computed from other facilities' records under this facility's name. Two rendering gaps made the partial state read as finished: the summary tiles dash out while a source is in flight but the cards below them did not, and the governed-rule banner is driven by `rows.every(...)`, which is vacuously true on an empty page, so a loading crosswalk announced "Governed rule coverage: 0 of 0 / Every crosswalk obligation is backed by an approved, effective, checksum-pinned rule version". Both are gated now. The same ungated shape was in RegulatoryCopilot (its employee picker briefly offered other facilities' staff), Schedule.tsx (every schedule in the organization), and MedicationIntegration/FhirIntegration (every resident name and room number in the organization, fetched to build a lookup map and thrown away). `useListViolations`, `useListAllResidentComplianceItems` and `useListSchedules` gained the optional `options.enabled` parameter their siblings already carried. A sweep for the pattern -- a hook whose scoping filter is conditional, called with an id derived through an empty-string fallback and no `enabled` -- now comes back clean across every page and component. **Checked and found clean in this tranche**: there is no ESLint in this repository, so `useMemo`/`useCallback` dependency arrays are unchecked by anything -- a scan of every one of them against the component state they close over found no genuine omission (the only hits were identifiers appearing in comments, string literals and object keys). Standing gaps re-read and unchanged: SG-2 and SG-4 are product/database decisions this touches in no way. No register rows opened or retired -- review-pass fixes, not scheduled work. Previous stamp, `84dd9bc` (2026-08-14), reviewed on branch `claude/app-wide-code-review-7hrzrz`: full-app bug hunt across the SPA, the Supabase edge functions, the migrations and the voice gateway. Every existing gate was green first (typecheck, 1,722 tests, seventeen static checks), so the findings are all things no check was watching for. **Five defect classes, each one a rule the codebase already applies somewhere and had not finished applying.** *Paginated reads without a unique tie-break.* `.range()` is OFFSET/LIMIT, so when a page boundary falls inside a run of equal sort keys Postgres may resolve successive page requests differently -- a row lands on two pages and another on none. Nine sites already carried `.order("id")` for exactly this reason; fifteen did not, and the columns they sorted on are the ones most likely to repeat: a policy-attestation campaign stamps ONE `due_date` across every row it fans out, so that whole table is a single equality run; `training_class_attendees` had no ORDER BY at all under a sweep whose output is the attendee COUNT shown in list views; a renewal cycle gives a cohort one `due_date`; assigning a course to a facility writes one `assigned_at`. The full-sweep loops matter most -- they exist to stop PostgREST's max-rows cap from under-counting compliance data, and without a total order they reintroduce the under-count they were written to prevent. `process-organization-export-jobs` had the same defect in the sweep that enumerates an org's documents for its export archive, where an omitted row is missing from an export the manifest still calls complete. *Exports that never downloaded.* Fifteen surfaces each carried their own blob-URL-and-anchor download, drifted into three behaviours, two of which do not work: a detached anchor's `click()` is a silent no-op in Firefox, and `URL.revokeObjectURL` on the line after `.click()` can invalidate the URL before the browser reads it. Neither throws, so the success toast fires either way. Dashboard and InspectionReadiness already had the correct append-click-remove-defer sequence, each fixed on its own as it was reported -- which is why the fix is one `lib/browserDownload.ts` rather than a fourteenth copy. `downloadCsv` in `lib/csv.ts` also escaped every data cell and joined the header keys raw. *A dozen popups from one handler.* The compliance binder's CSV appendix called `window.open()` per file past an `await`; every browser blocks all but the first, and the toast claimed all eleven had opened. Each signed URL is fetched and saved instead, reporting what actually landed. *Three access records storing an attacker-chosen IP.* `_shared/clientIp.ts` exists because `cf-connecting-ip` is meaningful only behind a verified Cloudflare and the FIRST hop of `x-forwarded-for` is the half the caller writes; seven functions were moved onto it and three were not -- `attest-policy` (the ESIGN/UETA record of intent, which also honoured `x-real-ip`), and the two guest-download fingerprints. All three are `verify_jwt=false`, so the party supplying the header is the party being attributed. *A voice session that leaked its slot and could end the process.* `attachSession` takes the concurrency slot on its first line, then constructs a `VoiceSession`; a throw there held the user slot, the global slot and an open billing span forever, and the rejection had nowhere to go -- it is `void`-ed from a `handleUpgrade` callback that runs after `handleBrowserUpgrade`'s own promise has settled, so index.ts's `.catch` never covered it, and Node terminates on an unhandled rejection, taking every other live session with it. The phone transport already released its slot the same way; the new test fails on the old code with exactly that signature. **Checked and found clean**: RLS is enabled on all 432 public tables (the five that read as missing enable it through a `do $do$` loop) and all 855 SECURITY DEFINER functions pin `search_path`; `computeStatus`/`computeDueDate` still match `recalculate_compliance_core` arm for arm; `grade_quiz_attempt` scores against every question in the quiz, not only the answered ones; the whole `dateUtils` surface round-trips across both DST transitions and leap day under a fresh property suite; every search box escapes both the LIKE pattern and the PostgREST `or()` syntax; all eight bulk importers take the job lease before the first row; and no `useState`-from-prop dialog is missing its remount key. **Correction to an earlier note in this pass**: `pnpm --filter caremetric-carebase build` was reported green when it had in fact failed on absent `VITE_*` variables; it builds, and `check:startup`/`check:bundle` pass, once they are supplied. Standing gaps re-read and unchanged: SG-2 and SG-4 are product/database decisions this touches in no way. No register rows opened or retired -- review-pass fixes, not scheduled work. Previous stamp, `69bd9e2` (2026-08-14), reviewed on branch `claude/workflow-review-optimization-beqw0r`: review of every workflow, first the CI/Actions ones and then the app's user workflows. **Three gates were reporting instead of blocking.** `ci.yml`'s path filter read the PR diff with `mapfile -t FILES < <(git diff ...)`, and process substitution is covered by neither `set -e` nor `pipefail` -- so an *errored* diff was indistinguishable from an empty one, every gated job was skipped, and `ci-result` counts skipped as success: a git error merged a pull request with no validation at all (reproduced against the pre-fix script with an unreachable base sha). It now warns and runs the full matrix. `supabase db lint --level error` never failed CI, because `--level` selects what is printed and `--fail-on` selects what changes the exit code, defaulting to `none` -- G25.1 already recorded that this invocation "had been reporting" two real errors while only `check:database`, which passes `--fail-on error`, went red; the CI step now passes it too, so a pre-existing lint error on main will surface as a real failure. `dhs-source-freshness.yml` opened its "PA DHS sources stale" issue on `if: failure()`, so a checkout or setup-node blip filed a false compliance signal -- its `id: check` had been declared for this and never used; `deploy-migrations.yml` opened a production deploy-failure issue on `always()`, which is true even when the run itself was cancelled, so every concurrency-superseded deploy filed one. Both fixes were already written and reasoned about elsewhere in this repository and simply had not been applied here. **Dependabot was never watching `.github/actions/*`** -- for the github-actions ecosystem a bare `directory: /` covers `.github/workflows` only, which is why the composite action still pinned setup-node v4 and cache v4.2.3 while every workflow pin was current; a `directories` glob plus grouping now covers it in one PR a week instead of ten that each booted the 45-minute `database` job. Also: `.github/*` narrowed to `workflows`/`actions` so editing `dependabot.yml` no longer starts a Supabase stack; `filter: blob:none` on the four full-history checkouts that only walk trees (verified in a real partial clone that it stays non-shallow, so `planning-registers`' own shallow guard still lets tree mode run) with `secret-scan` deliberately excluded; the deploy gate's two passes over the same 50 runs collapsed to one (101 REST calls to 51). **On the app side, the survey readiness path named three "next" steps at once**: every step in `SurveyPrepChecklist` decided its own status independently, so with a binder on file and readiness still short, three of four rendered as "current" simultaneously, each with its own primary button, in a card whose entire purpose is to name the one next action -- replaced with the first-not-done-wins pipeline the onboarding checklist on EmployeeDetail already used. **And its Resume button could never be clicked**: Survey Day while *active* was reported as a completed step, and `JobChecklist` correctly renders no action on a completed step, so the live workspace showed "Survey Day is active / Resume" with nothing behind it. Both are now locked down by `SurveyPrepChecklist.render.test.tsx`, which asserts one current step across all 24 input combinations and fails 3 of 4 against the old component. `JobChecklist`'s `blocked` state also rendered a button and reused the `upcoming` icon, so an unactionable step was indistinguishable from an unreached one while being excluded from the progress denominator. `OfflineCourse` read `navigator.onLine` straight into JSX, which is not reactive -- on the one page built for losing connectivity, reconnecting left the sync button disabled and the video link hidden until an unrelated re-render (now subscribed to the online/offline events, as DocumentCareDialog and UnsyncedDraftsPanel already are). `AiCourseWizard` submitted plan name and course count in both generation modes, so switching back to a single course left a typed plan name invisible but live -- still satisfying the "enough to generate" gate, so Generate would fire on a form that looked empty. `Dashboard`'s `benchmark_viewed` telemetry reported `route: "/app/dashboard"`, a path no `<Route>` declares (the page is mounted at `/app`), making those rows unjoinable against the `route_viewed` rows emitted for the same surface. **Checked and found clean**: all 172 distinct frontend in-app link paths (including object-literal nav config) resolve against the 215 declared routes and no sidebar link lacks a route; the ALF-not-ALR terminology rule holds and is test-enforced; `TakeCourse`'s gating and furthest-visited logic and `stateFormWorkflow`'s step indices are sound. One suspected defect was not real -- back-navigation in the offline course looked like it regressed saved progress, but `queueOfflineProgress` already clamps monotonically. **Deliberately not changed**: the CI `database` job runs `supabase start` then `supabase db reset --no-seed`, applying all 581 migrations twice; every way to avoid it either changes local-dev behaviour or obscures the job, and no Supabase stack was available here to prove the alternative, so it is flagged rather than changed blind. Standing gaps re-read and unchanged: SG-2 and SG-4 are product/database decisions this touches in no way. No register rows opened or retired -- review-pass fixes, not scheduled work. Previous stamp, `ec5bf1e` (2026-08-14), reviewed on branch `claude/stripe-integration-billing-review-bq17cw`: follow-up to #469 (merged), covering two review findings that arrived as it landed. **The operations note contradicted the monitoring #469 added.** Both documents said a missing Stripe secret is invisible until a customer tries to pay; that is no longer true of `STRIPE_SECRET_KEY`, which #469 made surface hourly as a failed `billing_sync_not_configured` run and a stale watchdog alert. The claim is narrowed to the secret it still holds for -- `STRIPE_BILLING_WEBHOOK_SECRET`, where nothing schedules the endpoint, so there is no run to fail and the first symptom is real subscription traffic quietly failing to reconcile. **And G270 opened**: #469 keyed the watchdog fix on `execution_kind`, which is a label, and six critical definitions are labelled `sql_cron` while their cron commands are `net.http_post` calls to Edge Functions -- all six with zero runs of their own, so pg_cron is the only thing feeding their freshness. Not fixed here: re-pointing which definition owns each cron entry across certificates, binder exports, document analysis, data exports, retention and integration webhooks would start several care- and compliance-critical jobs paging at once, which deserves its own verification rather than riding along. **Review round (2026-08-14):** two ordering/accuracy defects fixed in the activation guidance. The checklist ran Checkout at step 6 and configured the webhook at step 7, so an operator following it in order would create a real Stripe subscription that never reconciles into `billing_subscriptions` -- the exact silent divergence this whole review is about, encoded in the checklist's own sequence. Endpoint configuration and its test event now come before any Checkout run. And step 5 listed `SUPABASE_URL`/`SUPABASE_ANON_KEY`/`SUPABASE_SERVICE_ROLE_KEY` as secrets to set; the platform populates those, so they were padding a list operators verify with `supabase secrets list` and making a populated-looking list mean nothing about the Stripe secrets. Step 5 is now the three an operator actually owns. Standing gaps re-read and unchanged: SG-2 and SG-4 are product/database decisions this touches in no way. A2 unchanged -- the two secrets, the Customer Portal configuration and the duplicate `caremetricai.com` webhook endpoint all remain operator actions. Previous stamp, `30b5ea1` (2026-08-14), reviewed on branch `claude/stripe-integration-billing-review-bq17cw`: review of the Stripe integration, billing and subscription, merged up to main. The code is sound -- 27 edge-function tests pass, the webhook is signature-verified, idempotent, out-of-order-safe and dead-letters poison events rather than retrying them for 72h, and the flat catalog agrees across `packages`/`package_billing_prices`, `marketingPricing.ts`, BILLING_MODEL.md and the live Stripe Prices. Verified directly against live account `acct_1SinLKB06O2UFlNz`: all four Prices active, livemode, `per_unit` with `tiers_mode` null (simple flat, not graduated) and `unit_amount` matching `base_amount_cents`; the webhook endpoint already exists, enabled, on the right URL, subscribed to every event the processor handles, pinned to api_version `2026-02-25.clover` -- which also confirms `STRIPE_API_VERSION` names a real Stripe version; zero customers and zero subscriptions, matching the database. **Two Edge Function secrets are absent and each alone blocks revenue**: without `STRIPE_SECRET_KEY` Checkout, Portal and the hourly sync all fail, and without `STRIPE_BILLING_WEBHOOK_SECRET` every event is rejected `400 invalid_signature` so nothing reconciles -- A2 carries the operator action. **Correction to this branch's earlier stamp**: it called `BILLING_RETURN_URL_ORIGINS` a third required secret. That was true when the branch was cut and is no longer true -- `4eb298c` added `resolvePhase2BillingReturnOrigins`, which falls back through `PUBLIC_APP_URL` and `SIGNUP_REDIRECT_ORIGINS` to a `https://cmcarebase.com` default, so an unset allowlist is not an empty one. It still deserves setting explicitly, because unset means the allowlist is whatever that chain resolves to and a deploy on any other origin is refused, but it is not a blocker. PHASE2_OPERATIONS.md now describes the fallback rather than contradicting it. **And nothing reported the outage** -- G269 opened and closed here: the critical job definition and the one holding the cron name were different rows, splitting exactly across the watchdog's two filters, and the watchdog credited pg_cron's exit status, which for a `net.http_post` entry reports delivery rather than work. G24 amended: its recursion fix was necessary but not sufficient, and this is why that row could not notice. `modular_product_entitlements` test 18 was fixed on main in parallel with this branch; main's version is kept. Standing gaps re-read and unchanged: SG-2 and SG-4 are product/database decisions this touches in no way. Previous stamp, `4eb298c` (2026-08-13), reviewed on branch `fix/review-guest-links-and-today-caps`: rollback_organization_signup now deletes leftover audit_logs (append-only trigger disabled for that delete) and auto-provisioned billing_accounts/settings/schedule policies so organizations DELETE is not blocked by audit_logs_organization_id_fkey; process_stripe_billing_event sets privileged_write so protect_subscription_fields does not silently revert the checkout package_id stamp. Previous stamp, `ccce162` (2026-08-13): rollback_organization_signup now deletes enterprise_access_grants (and backfill exceptions) before scope memberships — leftover org_admin profiles project grants via sync_profile_builtin_enterprise_access, and membership_id is ON DELETE RESTRICT, so the previous rollback died and left the orphan tenant. Entitlements pgTAP now expects the live Train/CareBase Stripe Price IDs from `33df47c` instead of requiring them still to be null. Previous stamp, `c05a633` (2026-08-13): restamp after merging main's live Stripe price map (`33df47c` / `20260813180000_map_live_flat_stripe_prices`) and renaming the signup-rollback migration to `20260813181000` so the versions no longer collide. Review follow-up: pagination `id` tie-breakers, prefer submitted certification attempts, identity-scoped offline latch. Previous stamp, `ee4c41b` (2026-08-13): Stripe self-serve signup/checkout pass. Failed `signup-organization` cleanup could not delete the new org (`enterprise_organization_memberships` ON DELETE RESTRICT), so a bounced invite left an orphan tenant and a retry minted another; rollback now also detaches leftover `profiles.organization_id` (NO ACTION) and clears scope memberships before the org delete, and the handler rolls the org back *before* deleting the invited auth user (scope memberships RESTRICT on `profile_id` used to make `deleteUser` fail first). `checkout.session.completed` only upserted the customer and never stamped `package_id` or a subscription stub, so a paid org could still look like an in-app trial (and get locked out when `trial_ends_at` lapsed) if `customer.subscription.*` was delayed; expanded `{customer,id}` / `{subscription,id}` objects are read, and a trial checkout (`no_payment_required`) keeps the account in trial. `BILLING_RETURN_URL_ORIGINS` was documented optional but an empty list rejected every Checkout/Portal return URL. `create-billing-session` parsed unbounded JSON and sent `customer_update.address=auto` without `billing_address_collection=required` (Stripe rejects that). Signup burned rate-limit quota before reading `signup_enabled`, lost the edge-function error body in the UI, accepted credentialed redirect URLs, and swallowed `updateUserById` app_metadata failures. Checkout now always collects a payment method; returning from Checkout refetches billing state for ~16s so the stub/package appear without a manual reload; trial-expiry notices deep-link to `/app/billing`. Standing gaps SG-2 and SG-4 unchanged. No register rows opened or retired — review-pass fixes, not scheduled work. Previous stamp, leftover defects after #458–#466: Guest/passport/move-in share links and the public-token history rewrite omitted BASE_PATH (404 outside the SPA). Today’s work-item and alert lists stopped at PostgREST’s 1000-row cap and could report a quiet board. Class-notice QR codes rejected a path-bearing origin and ignored PUBLIC_APP_URL / SIGNUP_REDIRECT_ORIGINS. Two overlapping offline first-downloads could mint two AES keys. Voice gateway: uncaught phone meter writes, hangup-during-claim on the browser upgrade, and a Postgres daily-budget insert that double-counted every phone call. `check-edge-function-auth` still certified a commented-out gate. Failed inspections rolled the schedule forward and resolved `inspection_due`. The SCORM bridge swallowed errors after the session was already marked completed, and a completing-commit retry died on the active gate. Two assessors could open two certification attempts. Resident/staffing list RPCs still defaulted to UTC `CURRENT_DATE`. Auditors could overwrite official incident PDFs. Public Stripe/Twilio/SendGrid (and post-auth FHIR/SCIM) webhooks buffered unbounded bodies. Invitation and learning-package status leaked across tenants; survey-packet guest download returned token-state reasons. Standing gaps SG-2 and SG-4 unchanged. No register rows opened or retired — review-pass fixes, not scheduled work. Previous stamp, `7d0d2aa` (2026-08-10), branch `claude/railway-build-range-error-s60qd8`: the Railway build failed in its test step on `ReferenceError: window is not defined` in `deploymentRecovery.test.ts`, with no product change behind it — the deploy-blocking half of "Railway deployed rebuilds whose tests never ran" (closed below) working exactly as intended. Railway's builder sets `NODE_ENV=production`; Vite derives `import.meta.env.PROD` from `process.env.NODE_ENV` alone (`mode` stays "test") and Vitest only *defaults* NODE_ENV, so the suite ran with PROD true, `reportClientError`'s production gate stopped short-circuiting for the first time, and `buildClientErrorReport` dereferenced `window` in Vitest's node environment — green on every developer machine, red only where it blocks the deploy. The missing global is the defect, not the test: reporting is called from `ErrorBoundary` and `QueryError`, so a throw there replaces a recoverable render failure with a blank page, and the same line already fails in a real browser on any `http://` origin, where `crypto.randomUUID` is unavailable because it is secure-context only. `clientErrorReporting.ts` now reads `location`/`navigator`/`document`/`crypto` through an optional view of `globalThis`, with fallbacks the `report-client-error` function accepts (root route — it 400s anything not starting with "/" — `"unknown"` visibility, and a v4-shaped correlation id that still passes its strict UUID check), and wraps its synchronous half in try/catch so "reporting must never create another application failure" covers more than a rejected fetch. Separately, `vite.config.ts` pins `NODE_ENV=test` when Vitest resolves it, so a test run means the same thing on Railway as locally instead of silently switching on production-gated branches; verified the reporting fix stands on its own by re-running the full suite under `NODE_ENV=production` with that pin disabled (161 files / 1638 tests pass). Standing gaps re-read and unchanged: SG-2 and SG-4 are product/database decisions this touches in no way. No register rows opened or retired — a build-integrity fix, not scheduled work. Previous stamp, `937f334` (2026-08-10), branch `claude/app-processes-review-wbedp1`: sixth full-app review pass — 54 adversarially-verified defects fixed (course-progress wipe + SCORM bridge/idempotency, import pipeline vocabulary/idempotency/grants, dry-run auto-apply, binder/PDF local dates + sandbox exclusion, stale query invalidations, dead routes and admin-mount bounces, ResetPassword recovery gating). Standing gap inherited from #462: its stamp named pre-squash `77a0c5cb`, which the squash merge could not preserve, so this check has been red on main since. This stamp names this branch's review head (bumped once for the trigger-function revoke, which pgTAP's anon-surface ratchet caught in CI); merge this PR with a merge commit to keep it a main ancestor, or restamp in a BACKLOG-only follow-up after a squash. Second tranche re-verified: guard scripts now parse mid-line DATE columns and real SQL comments, inspection schedules advance again, PoC snapshots scoped to violation readers, incident windows and digest links are facility-local, the seeded catalog's remaining customer-facing "ALR" strings read "ALF", and offline course progress only marks sent percents as synced. Review follow-up: per-row import receipts go through record_data_import_row_receipt (no job-wide recount or event per row), the binder's CSV escaping matches the canonical quote-only-when-needed helper, both room appliers reconcile surplus beds on a lowered bed_count (blocking on occupied/reserved/held) and land is_active through the definer RPCs (the table's UPDATE grant stays revoked), offline conflict retries advance the receipt sequence, receipt sequences are scoped per (device, assignment) with stored-outcome replay, and the copilot deadline-window flip was reverted -- verification showed due_at is stamped as end-of-day markers, so gt/lte was already right. No register rows opened or retired — review-pass fixes, not scheduled work.

**Owner:** the owner-operator (single person, platform admin)

**How to update:** edit this file in the same change set that ships or retires work, and
bump the stamp above. This is enforced, not requested — `pnpm run check:planning-registers`
fails when application source, a migration, or an edge function moves without this file
moving with it. Do not create a parallel planning register; the same check rejects new
root-level review markdown.

---

## Why this file is enforced

It was created as "the single living backlog" and was wrong two commits later. PR #355
shipped SCORM, POC-lifecycle, and import-worker code against rows this file still listed
as `open`, and the stamp above still pointed at the pre-#355 commit. Nothing failed,
so nobody noticed.

That is the same failure mode as the standing gaps below: this repository enforces
about twenty machine-checkable invariants rigorously and enforced its planning documents
not at all, so the planning documents are where the drift went. The fix was to make the
register checkable rather than to ask people to be more careful. See
`scripts/check-planning-registers.mjs`.

---

## Operating reality: one person

This product is owned and run by one person, who holds the platform-admin (super admin)
identity. That is not a staffing gap to be worked around; it is the permanent constraint
this backlog is planned against, and it changes three things:

- **`ops_only` does not mean "someone else".** Those rows are not delegated. They are the
  same person, wearing a different hat, on a different day. An owner column reading
  "Eng/ops", "Product", "QA", or "Legal" is a *kind of work*, never a handoff.
- **Nothing runs in parallel.** Two tracks "in parallel" means one of them is not moving.
  The sequence below is single-threaded on purpose.
- **Separation-of-duties controls cannot do their job.** Several gates in this codebase
  require two distinct identities (most sharply `approve_regulatory_rule_version`, which
  refuses when `authored_by = auth.uid()`). One person can always satisfy those
  mechanically with a second account — and satisfying them that way delivers none of the
  independent review they exist to provide. Where that matters, the row says so rather
  than pretending the gate was met.

---

## Register map

One canonical list. Everything else is labelled in its own first lines, and the label is
checked.

| Document | Role |
| --- | --- |
| **BACKLOG.md** (this file) | **Canonical.** Open work, ordered by launch readiness |
| [IMPLEMENTATION_PLAN.md](IMPLEMENTATION_PLAN.md) | Reference — long-horizon five-phase program |
| [RESIDENT_360_PROGRAM_PLAN.md](RESIDENT_360_PROGRAM_PLAN.md) | Reference — Resident 360 program design |
| [SURVEY_DAY_MODE_SPEC.md](SURVEY_DAY_MODE_SPEC.md) | Reference — Survey Day mode spec |
| [PA_DHS_ANNUAL_TRAINING_MATRIX.md](PA_DHS_ANNUAL_TRAINING_MATRIX.md) | Reference — PA DHS requirement matrix |
| [docs/design/POC_LIFECYCLE.md](docs/design/POC_LIFECYCLE.md) | Reference — Plan-of-Correction design |
| [docs/design/SCORM_PRODUCTION_HARDENING.md](docs/design/SCORM_PRODUCTION_HARDENING.md) | Reference — SCORM production PR plan |
| [docs/ops/TIER_A_PILOT_OPS_CHECKLIST.md](docs/ops/TIER_A_PILOT_OPS_CHECKLIST.md) | Reference — ops-only Tier A rows |
| [docs/ops/SG2_DECISION.md](docs/ops/SG2_DECISION.md) | Reference — SG-2 counsel-cleared option 2 record; activation follow-up remains (2026-08-02) |
| ROADMAP.md, WORKFLOW_UX_REVIEW_2026-07-31.md, docs/audits/\*, CAREBASE_25_\*, EFFICIENCY_REVIEW.md, END_USER_REVIEW.md, ENHANCEMENT_REPORT.md, PLATFORM_ENHANCEMENTS.md, root `PennTrain_*` | **Superseded.** Dated evidence only — do not plan from them |

Six of those superseded documents still read as live registers before this pass, and two
claimed authority in their own opening paragraph. If any of them contradicts this file or
code on `main`, **trust code and this file.**

---

## Standing gaps

Accepted, unfixed problems that survive review cycles *because nothing fails while they
are open*. Each row has a review date. When it passes, `check:planning-registers` goes red
until someone closes the row, moves it to "Explicitly not now", or deliberately re-dates it
— which puts a person on record for the decision. Letting it sit quietly is the option the
check removes.

| ID | Gap | Why it survives | Gate to close | Owner | Review by |
| --- | --- | --- | --- | --- | --- |
| SG-4 | **`public.record_resident_service_task` is superseded, uncalled from this repository, still granted to `authenticated`, and cannot be closed out without changing behaviour for callers this repository cannot enumerate.** **This row now carries the whole finding.** It was opened on `main` via #452 while G15.13 — the row in the unreachable-capability table that started the question — was still in review, so the same RPC had two rows reaching the same conclusion through different reasoning. They are folded here, because two rows that disagree about *why* are worse than one row that is right, and this one is the one with an owner and a review date. `useRecordResidentServiceTask` (`useResidentServiceTasks.ts`) kept the old name but reaches `record_service_task_response`; the only in-repo references left are the pgTAP suite and generated types, so it reads as dead code. It is not: `20260713160000` grants execute to `authenticated` and nothing has revoked it, so it is a live PostgREST surface. All three ways to close it out change behaviour. **Drop it** or **revoke execute** — the caller's request starts failing (42883 / 42501). Both *used* to also remove the last caller of `app_private.evaluate_service_task_exception` and retire the `service_task_alerts` queue with it; `20260805040000` gave `record_service_task_response` that same call, so that consequence is gone and the drop is a smaller change than this row described. It is still breaking for the caller, which is what keeps the row open. **Delegate to the successor via a shim** — the option that looks safe, and the one this row first refused for a reason that was wrong. **Retracted, and stated here rather than quietly dropped: the claim that `completed_by_other` has no equivalent response in the successor, so a shim would record "somebody else did this" as an ordinary completion and lose the fact.** The translation exists in this repository, is tested, and runs on every successor call already: `lib/serviceDeliveryContract.ts`'s `completionResponseForServiceOutcome` maps `completed` / `completed_late` / `completed_by_other` onto `completed_as_planned`, and `useResidentServiceTasks` preserves the distinction in `exception_details` as `legacy_status` plus a `completed_by_other` boolean. Its own doc comment says it exists to accept both vocabularies at the shared boundary. A SQL shim would do the same and has a proven mapping to copy. One caveat before anyone leans on that preservation: `legacy_status` is written by `useResidentServiceTasks` and read back by nothing — no surface in this repository consumes it — so the fact survives in the stored row and nowhere else, available to whoever goes looking for it and to nobody who does not. **What actually blocks the shim is narrower, and is not about fidelity:** `record_service_task_response` refuses any response the requirement's `acceptable_completion_responses` does not list (22023), and a shim that delegates inherits that gate, so calls the predecessor accepts today would start failing on a rule their caller has never seen. That is inherent to delegating — the only way around it is not to delegate. Two further divergences are real but are **cost rather than blocker**, because a careful shim could re-implement both, and they are the reason a careless one is worse than leaving this alone. One of the two has since been closed: the successor **used to never evaluate exception thresholds**, so alerts stopped for the caller that used to get them — `20260805040000` wired `app_private.evaluate_service_task_exception` into `record_service_task_response`, so a shim no longer costs the caller its alerts. What remains is that the successor **enforces neither** the legacy's ≥3-char note on a non-completed outcome **nor** `requires_two_staff`, so calls that used to be refused start succeeding — the worse direction for a compliance record. Recorded on the objects themselves in `20260805000000_the_legacy_service_task_command_is_not_dead_code.sql`, whose fidelity argument `20260805030000_completed_by_other_is_not_lost_in_translation.sql` corrects in place on the function comment — a note in a planning file does not reach the person running "drop the unused RPCs", and neither does a retraction that only lands in the planning file. Pinned behaviourally in `support_plan_service_tasks.test.sql`: still granted; legacy records `completed_by_other` as its own status; successor raises 22023 on it. The first three still pass — `20260805030000`'s correction was to the reasoning drawn from them, not to the facts. The fourth, that the successor's `not_completed` produces no `service_task_alert`, is the one assertion that stopped being true: `20260805040000` inverted it rather than deleting it, because the fact it pinned is exactly what that migration changes. | Leaving it untouched is the only outcome that changes nothing for an unenumerable caller, and nothing fails while both commands exist — they just write the same table under different rules. The cost is real and is the reason this is a gap and not a decision: two write paths, one of which no in-repo surface exercises, so its validation and its alerting drift out of test coverage by ordinary use. The alerting half of that drift **was** live in-product and is now fixed: since the UI moved to the successor, `service_task_alerts` had no producer any in-repo surface reached while `useListServiceTaskAlerts` still read that queue — closed by `20260805040000`. What remains is the validation half, exercised by no in-repo surface. | Either (a) evidence about callers — PostgREST logs or `pg_stat_statements` over a real window showing nobody outside this repository invokes it, which turns the drop into a fact rather than a guess; or (b) a decision that the plan's `acceptable_completion_responses` gate may apply to legacy callers too, which is what makes a delegating shim honest rather than silently tightening — a product decision about existing requirement rows, not a code change — the alerting path no longer needs deciding, since `20260805040000` gave the successor the same threshold evaluation. **Do not close this row by writing the shim while (b) is undecided.** The reason is no longer that a shim cannot express `completed_by_other` — it can, and the mapping is already written — but that a shim inherits a rule its callers have never been subject to, and the failure mode is a call that used to succeed now failing. | **You** (product/eng) | 2026-10-01 |
| SG-2 | Counsel cleared option 2 and `20260802010000_pa_regulatory_rule_pack_templates.sql` seeded `pa.pch.2600.65.personnel` and `pa.alf.2800.65.personnel`, but no active PA governed version exists yet. Until one of those drafts completes install → review → activate, the copilot remains a drafting aid for Pennsylvania. **2026-08-04: the seeded template content itself (not just the option-2 approach) is now attorney-approved, confirmed via product owner (no separate written record) -- a distinct, narrower confirmation than the option-2 approach clearance `docs/ops/SG2_DECISION.md` already recorded.** **2026-08-04, separately: activation no longer requires a live-shadow period** (`20260804020000_activation_no_longer_waits_on_a_shadow_period.sql`) -- see this file's own stamp above for the full account. | The templates now exist, are installable, and their content is legally cleared, so the product can look "done" before any PA governed version is actually active -- legal clearance is not activation evidence. | Install one PA draft, complete independent review and fixture verification, and activate a PA governed version with evidence. | **You** (product/ops -- legal coordination is now discharged on both the approach and the content) | 2026-09-01 |

---

Closed this pass: **SG-3, the video-watch-gate Storage gap.** `20260802030000_remove_pilot_program.sql`
swept `learning.video_watch_gate` to globally enabled along with the other 3 former pilot flags,
which hard-locked New Employee Orientation's 3 HeyGen video blocks (`20260724040747`) everywhere
except the production project -- the only place their real video files exist. Per the product
owner's explicit choice among the 3 options this row laid out:
`scripts/seed-course-video-placeholders.mjs` uploads a tiny, clearly-labeled placeholder MP4 to
the 3 block paths via the Storage REST API, fixing the missing asset without touching the gate's
logic at all. Refuses to
run (exit 1, before any network call) unless `SUPABASE_URL` is a loopback address, with no
override. Wired into CI's `database` job and into `local-supabase-stack.sh` for local dev;
`changes` job's `DB_PATTERNS` extended so the script itself is path-filter-covered. **Verified,
not just implemented:** PR #447's own CI run confirms the new "Seed course-video placeholders"
step completed successfully, and the Playwright e2e suite that runs immediately after it in the
same job also passed -- so the fix is live and nothing downstream regressed.

Closed this pass: **F1 split the three pages over 40KB.** `CourseDetail.tsx`, `ResidentFinancialOperations.tsx`,
and `ResidentAssessmentFormEditor.tsx` decomposed into `course-detail/`, `resident-financial-operations/`,
and `resident-assessment-form-tabs/` (the last mirrors the existing `resident-tabs/` convention). Pure
structural refactor, zero intended behavior change — verified per-file (typecheck immediately after each
split, not just at the end) plus a DOM-id byte-diff against the pre-split files to catch attribute drift
a normal diff wouldn't surface. No e2e coverage existed for any of the three pages to regression-check
against (confirmed by route grep); unit suite (1136 tests) and production builds are the verification signal.

Closed this pass: **D4 column-mapping UI for non-canonical CSVs.** All 8 `bulk-import-*` edge
functions parse CSV by header name, not position, so a client-side relabel before the existing
D3 dry-run/apply pipeline was sufficient — no edge function, RPC, or ledger change. New
`importColumnMapping.ts` (exact/alias/fuzzy-suggested mapping, never auto-applied without user
review) and `ImportColumnMapping.tsx` UI in `DataImportCenter.tsx`; a canonical exact-header
upload still flows through unchanged. 27 new tests.

Closed this pass: **E3 fire drill DHS monthly tracker PDF.** The 9-field form and per-drill print
view already existed; added the missing rollup — `generate-fire-drill-tracker-pdf` edge function
(facility + month, all 9 DHS fields, mirrors `generate-incident-report-pdf`'s auth/RLS pattern) and
a "Download Monthly Fire Drill Tracker" action on `InspectionItems.tsx`. New private
`fire-drill-tracker-exports` storage bucket, write via service-role only, read scoped by org +
facility assignment. Verified against the local Supabase stack: 2782/2782 pgTAP including
`tenant_isolation_invariants`; `db lint`/`db advisors` clean. No new route.

Closed this pass: **F3 root README replaced with product + agent runbook.** Root `README.md`
was a marketing-site-redesign handoff doc; that content moved to
`docs/marketing/MARKETING_SITE_REDESIGN_HANDOFF.md` (with provenance note) and root `README.md`
now orients a reader on the actual product, monorepo layout, and points to `AGENTS.md` for
setup/commands and `BACKLOG.md` for current work, without duplicating either.

Closed this pass: **E2 med-admin "who can pass meds today" board joined onto Schedule.**
`useMedAdminAuthorization` extracts the cert (MED-INIT/MED-RENEW) + practicum + diabetes-education
join `MedAdminRoster.tsx` already computed into a shared hook; `ScheduleDetail.tsx` now shows a
per-employee authorization badge (only for staff flagged `administers_medications`) and a gap
banner for any scheduled `(date, shift)` with med-admin staff present but none currently
authorized. `MedAdminRoster.tsx` itself now consumes the shared hook instead of a second copy of
the join. 23 new unit tests.

Closed this pass: **SG-1 pilot-cohort notification gate removed; SG-2 liability gate cleared
by counsel; PA personnel templates seeded.** SG-1 was closed by deleting the pilot program
outright rather than by enrolling a pilot org: `20260802030000_remove_pilot_program.sql`
set `notifications.expanded_delivery_types` and `notifications.critical_multichannel` (plus
`screening.on_hire_exclusion` and `learning.video_watch_gate`) to `global`, non-expiring, for
every organization — no console, no manual enrollment, no separate gate. Review caught a
second, independent gate behind it: `record_organization_signup` never created an
`organization_settings` row, and that table defaults both notification switches to `false`,
so a real signup still received nothing even with the flags global.
`20260802040000_signup_creates_organization_settings.sql` closes that too, so a real signup
now gets email/SMS delivery the same way a demo org always did. Deleting the Pilot Cohort
Console also removed the only in-app UI for `set_release_flag`/`set_feature_kill_switch`;
`/admin/release-flags` (`ReleaseFlags.tsx`) replaces it with a minimal, non-pilot surface —
flags and kill switches only, no cohort enrollment.
Option 2 is now counsel-cleared in [docs/ops/SG2_DECISION.md](docs/ops/SG2_DECISION.md),
and `20260802010000_pa_regulatory_rule_pack_templates.sql` seeded
`pa.pch.2600.65.personnel` plus `pa.alf.2800.65.personnel`. SG-2 remains open as an
activation gap until a PA governed version is actually active.

Closed this pass: **PA rule pack install UI wired.** Enterprise Foundation's regulatory
expansion panel now offers install buttons for both seeded PA templates
(`pa.pch.2600.65.personnel`, `pa.alf.2800.65.personnel`) alongside the Ohio mechanism
demo, and the Regulatory Copilot empty-state / alert copy directs operators to install
and activate a PA pack instead of describing PA coverage as permanently out of scope.

Closed this pass: **Railway deployed rebuilds whose tests never ran.** `railway.json`
built with `typecheck && build && check-bundle-budget` and no test step, on its own
push-triggered pipeline — so a red suite still shipped. The repo had already solved this
exact class of problem for migrations (`deploy-migrations.yml` waits on a validated CI
SHA; see `MIGRATION_DEPLOYMENT_AUDIT.md`) and never applied it to the application deploy.
The build command now runs the unit suite and the startup check.

Closed this pass: **Marketing suite documentation terminology + Landing design fidelity (#377).**
Public marketing surfaces (Landing.tsx, Features, FAQ) now exclusively use “documentation” /
“documentation rooms” / “Survey documentation” (never “evidence”). Landing restored to
CareBase Landing v2 design fidelity (Prove the work., Education spend, Guest documentation
portals) with regression test; pricing remains single-source from marketingPricing.ts;
self-serve CTAs preserved. Internal product routes unchanged.

Closed this pass: **D3 durable import apply RPCs — all 8 domains complete (#413).** Dedicated
SECURITY DEFINER RPCs (`import_apply_training_record`, `import_apply_employee_credential`,
`import_apply_room_with_beds`, `import_apply_incident`) granted only to service_role mirror
the interactive RPC business rules but replace `auth.uid()` with NULL (system-applied import,
no human reviewer). `PENDING_DURABLE_DOMAINS` is empty; the worker dispatches all 8 domains.
No table-level INSERT/UPDATE grants widened on incidents or employee_credentials;
interactive RPCs for authenticated callers unchanged.

Closed this pass: **B1 accept-time bridge bundling.** `accept-learning-package` edge function
injects `carebase/learning-runtime-bridge.js` into the package zip at accept time, re-hashes,
and updates `content_sha256`. Client routes through the edge function so injection cannot be
skipped. Org-scoped storage policies on the `learning-packages` bucket ship in
`20260731230000_residual_product_gaps_wave2.sql`. Residual market confidence is B3 (real
vendor packages); production apply of migrations remains A1 ops.

Closed this pass: **C5 first-class citation_ref on survey evidence packets.**
`survey_evidence_packet_items.citation_ref` is a first-class column; add/list/assemble RPCs prefer it for
regulation order (label parse remains the fallback); Survey Day packet UI accepts an explicit citation
and shows a badge from the stored field.

Closed this pass: **E1 Home IA density.** Primary navigation and page copy now separate the three
roles cleanly: **Today** = action / due work, **Compliance scorecard** = health / trends, and
**Inspection Readiness / Survey Day** = survey prep / live entrance conference.

Closed this pass: **B4 SCORM/xAPI completion → training record / hour bucket (trigger-based).**
AFTER INSERT on learning_runtime_commits calls internal bridge_learning_runtime_completion
when first completed commit arrives (quiz blocks still gate; `pa_today()`; privileged_write;
upsert employee_training_records + recalculate_compliance_core). Leaves commit_learning_runtime_state
untouched so existing pgTAP remains green.

Closed this pass: **D5 sample CSVs reachable from Import Center.** `ImportSampleDownloads` is
now rendered on `DataImportCenter` (after the domain-templates card). Sample employee /
training-record / credential CSVs under `public/import-samples/` are downloadable for
dry-run practice. Column order matches `importTemplate()`.

---

## Snapshot (what is true on main today)

### Shipped and credible (do not re-litigate)

- Multi-tenant CareBase SPA + Supabase (RLS, Auth, Storage, Edge Functions, pg_cron)
- Flat billing model (Train / CareBase); Stripe qty=1 intent
- Release flags / kill switches (global rollout; no pilot-cohort gate)
- Learning package runtime bridge (opaque iframe, nonce, `event.source`, commit sequencing)
  with unit, integration, and Chromium e2e proof
- Accept-time bridge bundling into package zips (B1) + org-scoped `learning-packages` storage policies
- Multi-domain Data Import Center: all 8 domains active
- Durable import worker: all 8 domains apply from ledger under service-role (direct table or import_apply_* SECURITY DEFINER RPCs)
- Survey evidence packet zip + guest download path
- Credential OCR structured extraction path
- Violations → corrective actions → retraining assignment → POC PDF → status ladder,
  now with immutable POC versions and auto-created corrective-action work items (#355)
- Clinical/EHR hybrid (native chart + FHIR ingest; lightweight employee caregiver charting
  surface at `/me/residents`, offline-tolerant), opt-in — `docs/HIPAA_CLINICAL_DATA.md`
- Dense ops surface: Survey Day, Work Queue, Training Matrix, Today, binder, evidence
  room, lifecycle cases, invitations
- Marketing public suite: documentation terminology lock + Landing design fidelity (#377)
- SCORM/xAPI runtime completion bridges to assignment + training records / hour buckets (B4)
- SG-2 counsel-cleared install path exists for PA personnel templates; activation still pending — [SG2_DECISION.md](docs/ops/SG2_DECISION.md)
- Survey evidence packet first-class `citation_ref` + regulation-ordered list/manifest (C5)
- Offline documentation for one already-queued floor task when a device goes offline, Tier 1 (E5) — encrypted IndexedDB draft store, sync with block-and-flag conflict handling, proactive wipe on identity change; broader offline scope remains open

### Still open (highest risk first)

1. Stripe Prices mapped and internal checkout smoke
2. SCORM real vendor packages (B3); B1/B4/B5 shipped, adapter injection wired
3. Wave 3/4 verticals: policy campaigns beyond E4's knowledge-check slice (declarative targeting, scheduling, reminders); offline drafts beyond E5 Tier 1's single floor-queue-task scope
4. `clinical_data_consent` on disclosure paths — **Closed (counsel cleared 2026-08).** Charting stays ungated. Outbound disclosure now requires `granted`: FHIR write-back enqueue + drain, organization export of clinical/FHIR resident-keyed rows, and designated-person portal document share/download (plus prep-instruction redaction). Shared helpers in `20260806090000_clinical_disclosure_requires_consent.sql`; managers set posture via `set_resident_clinical_data_consent` on the clinical chart.
5. Both offline draft safety gates (`assertServiceDraftAllowed`, `assertObservationDraftAllowed`) validate every field except `createdAt` and `syncState` — an unparseable `createdAt` makes a draft immune to both purge clocks and to the overdue warning, and an unrecognized `syncState` makes it invisible to the panel as well. The write paths always set both correctly, so this only bites as the tamper/corruption gate the modules claim to be. Symmetric across both lanes; fix them together or they drift. **Closed.** `offlineDraftFieldGuards.ts` holds the rule and each lane passes its own state set -- the service lane knows `conflict`/`stale`, the observation lane deliberately does not, and a test pins that disagreement so a later tidy-up cannot quietly unify them. Both state lists are typed `Record<Union, true>` rather than written as arrays, so adding a state and forgetting the list fails at compile time instead of the gate refusing a legitimate draft at runtime on a device. Worth recording why it was not merely cosmetic: the `NaN` from an unparseable `createdAt` makes every clock comparison false, so the draft is never overdue, never expired and never purged, while an unrecognized `syncState` keeps it out of both panel lists -- together, care documentation held on a device permanently and invisibly.
6. **A mid-session identity change wipes the offline draft store but not the react-query cache.** `auth.tsx` handles an admin changing someone's role, organization, or facility mid-shift as a first-class case — that effect calls `wipeOfflineServiceDrafts()` — but it never calls `queryClient.clear()`, which only `signOut` and `SIGNED_IN` do. So any cached query whose key does not itself carry the identity keeps serving the previous context's rows until its own `staleTime` lapses. Surfaced by Copilot against the two caregiver hooks and fixed there by putting `(profileId, organizationId, role)` in the key, which is the convention several org-scoped hooks already follow — but the two hooks were the symptom, not the cause. Worst case is a signed storage URL, which is bearer-authorized for its full TTL and keeps resolving no matter what RLS would now say. The general fix is one `queryClient.clear()` in that effect; it was left out of PR #436 because it changes cache behaviour for every query in the app and wants its own change. **Closed.** `auth.tsx`'s identity-change effect now calls `queryClient.clear()` when the signed-in identity actually changes under a surviving session, which was the one transition in that file without a clear -- SIGNED_IN, a definitively-missing profile, a deactivated profile, the AuthProfileError sign-out and `useSignOut` all already had one. The gate is a separate predicate (`sessionIdentity.ts`) rather than the offline-store one sitting next to it, and that distinction is the whole trap: `shouldWipeOfflineServiceDraftData` returns true for any non-employee role by design, so reusing it would clear every manager's entire cache on every identity evaluation. Deactivation is deliberately not part of the new predicate -- its own effect already signs out and clears, and testing activity here would re-fire on every later evaluation of a still-inactive identity rather than once on the transition. What no client-side fix reaches: a signed storage URL already handed to the browser stays bearer-authorized for its full TTL, so short TTLs remain the real mitigation for that half. **Correction (Codex, PR #443): calling this closed was wrong.** The row itself names "role, organization, or facility", and the fix covers the first two. A facility transfer changes the authoritative scope on `employees.facility_id` while profile id, organization id and role all stay put, so `signedInIdentityChanged` returns false and the clear never runs -- leaving the old facility's resident rosters, photos and charts served from cache until their own staleTimes lapse. It is not a one-line predicate change: `AuthUser` carries no facility at all (id, name, email, role, organizationId, isActive), and facility lives on `employees`, not `profiles` -- so the auth profile query has to fetch it before any predicate can see it, and the effect's dependency list has to include it before the effect would even re-run. That is a change to core auth data-fetching and wants its own diff rather than being appended to a five-part PR. **Facility now covered too.** `AuthUser` gains `facilityId` from its own query rather than a PostgREST embed on the profile select -- an embed fails the WHOLE request, which would put every sign-in behind the `employees` RLS policy resolving for every role; separate, it degrades to "facility unknown" instead of "cannot sign in". The comparison is three-valued and that is the load-bearing part: `undefined` (unresolved) appears twice in every ordinary session -- once before the facility query settles and once immediately after each `queryClient.clear()` wipes it -- so an unresolved value on either side must read as not-a-change or the app would clear on every sign-in and then clear again on its own refetch, never converging. `null` is a resolved value, not an absent one: someone with no `employees` row genuinely has no facility, and gaining one is a real scope change. `facilityId` is in the effect's dependency list as well as the predicate, or a transfer would never re-run the effect that asks. **Hardened further (PR #449):** the clear was never the whole client-side story. `useResidentPhotoUrls` (`useResidentPhotos.ts`) already keys its signed-URL query on `(profileId, organizationId, role)`, not just the resident, which closes a narrower race the clear alone does not: a sign already in flight at the moment identity changes can resolve *after* `queryClient.clear()` runs, writing into a key the new identity still reads. Two sibling hooks signing the same resident photos -- `useResidentPhotoUrl` in `useResidentAdministrativeMaster.ts` and the same-named local hook in `ResidentCareHeader.tsx` -- were keyed on the document id alone and missed that. Both now match. The irreducible half is unchanged: a signed URL already handed to the browser stays bearer-authorized for its own TTL no matter what any of this does, so short TTLs remain the only mitigation for that part, as recorded above.
7. **Offline sync still has no persistent trigger.** Raised by Codex on PR #436 as two halves; only the first is still open. (a) `UnsyncedDraftsPanel` now syncs on mount as well as on the `online` event, which covers a caregiver walking back to Floor or the roster after signal returns — but the panel is still mounted only on those two routes, so nobody watches connectivity while the caregiver sits on `/me/residents/:id`, which is exactly where a reading is taken. The offline toast's promise that it "syncs once you're back online" is therefore true only after a navigation. The fix is a headless syncer in the employee shell rather than a component on two pages. (b) *Closed* — `ChangeOfConditionQueue` now honours `?report=1&resident=<id>` by opening the creation dialog with that resident preselected, and consumes the flag so closing or reloading does not reopen it. **Sharpened by Codex on PR #440:** the `online` event is not merely a *late* signal for this, it is frequently no signal at all. The failures the offline fallback exists for -- a LAN link with no route out, a bad DNS resolver, a captive portal, Supabase itself down -- all leave `navigator.onLine` reading `true` throughout both the failure *and* the recovery, so the transition never fires and the mount-sync only helps if the user happens to navigate. Tier 3 mounting the panel on `ChangeOfConditionDetail` narrows the gap (that page is now a third place a mount-sync can fire) but does not close it: an aide who stays on one event through the outage and its recovery still sees no retry, and the toast promising it will sync once back online is unbacked for exactly the case that produced the draft. The headless syncer is the fix, and it should poll reachability rather than trust the browser flag. **Closed.** `OfflineSyncManager` is mounted once in `MainLayout`, so the loop runs for the whole signed-in session rather than on the three pages that happen to carry the panel -- none of which is `/me/residents/:id`, where a reading is actually taken. It retries on a backoff (30s/60s/2m/5m) while a backlog exists and stops when it drains, keeps the `online` listener as the earliest signal when it does fire, and uses no reachability probe: the sync itself is the honest test of whether the server is reachable, and a network-level failure is already a keep-and-retry rather than a loss. The critical-reading warning moved to the shell with it, which was required rather than tidying -- a background sync that can now run on any page could otherwise chart a critical vital sign and display nothing. The two-lane run (sequential, stop-on-wipe) is shared by the panel's manual button and the loop via `useOfflineSyncRunner`, serialised by a module-level latch: two components cannot be ordered by a ref, and overlapping runs would reintroduce from the outside exactly the device-re-registration race the lane ordering closes from the inside.
8. **There are now two offline receipt ledgers, and `20260803090000` argued explicitly for one.** That migration widened `offline_service_draft_receipts` with a `draft_kind` discriminator rather than copying it, on the grounds that `unique (device_id, idempotency_key)` "has to hold across BOTH kinds" and that two tables mean "two places to look when auditing what a device sent." `20260803110000` then added `offline_observation_draft_receipts` as a separate table anyway. The two are otherwise identical in posture — same `modules.carebase` gating, same `profile_id = auth.uid()` select policy, same `offline_device_registrations` parent, same append-only + TRUNCATE guards — so none of the usual reasons to split apply. What does not resolve cleanly is the name: the deployed table is `offline_service_draft_receipts`, and a third kind would put clinical, PHI-linked rows behind a service-lane name. The uniqueness half of the concern is inert today (both lanes mint keys with `crypto.randomUUID()`); the audit half is real. Three ways out — leave it split, widen the service ledger and accept the name, or rename it lane-neutral (`offline_draft_receipts`) and widen. **Decided 2026-08-03: rename lane-neutral, then widen.** One caveat found while scoping it, which makes it larger than it looks: plpgsql resolves table names at runtime, so renaming the relation breaks `sync_offline_service_task_draft` and `sync_offline_unscheduled_service_draft` on their next call, and both deployed functions have to be reproduced with the identifier substituted (~260 lines of another lane's shipped code) in the same migration that does the rename. Indexes, the select policy, both triggers and the guard function repoint by OID via `alter ... rename`, but `product_module_resources.resource_name` and `audit_entity_manifest.table_name` key on the name and must be rewritten or the table silently drops out of entitlement enforcement and the audit coverage report. Worth doing as its own change rather than inside a caregiver-charting PR. **Update (E5 Tier 3, `20260803130000`):** the next lane arrived and went onto the service ledger as a third `draft_kind`, `change_observation`. That is not a re-decision of this question, and it is worth recording why it could not have gone the other way: `offline_observation_draft_receipts` constrains `outcome` to `applied`/`duplicate`/`rejected`/`wipe_required` and argues in its own header that `conflict` and `stale` are values its flow can never produce. A change-of-condition monitoring observation *can* go stale -- the event closes while the device is offline -- so filing it there would have meant widening that table's vocabulary to re-admit exactly the two values it excluded on purpose. The state is therefore three kinds on the service ledger plus one separate observation ledger, and the naming half of the concern is sharper than before: `change_observation` rows point at a `resident_change_events` row, which is clinical-adjacent, behind a service-lane name. Tier 3 keeps the observation *text* off the receipt entirely, so no clinical narrative lands there -- but the linkage does. The third option still resolves it. **Half closed (`20260803140000`): the name is fixed, the split is not.** `offline_service_draft_receipts` is now `offline_draft_receipts`, so the three kinds it already holds -- including the clinical-adjacent `change_observation` -- no longer sit behind a service-lane name. Done as `alter table ... rename` rather than a copy: the rows are append-only evidence of what devices sent, and recreating them elsewhere would rewrite that evidence's identity for no reason. The two things a rename does not carry were the whole risk and are both asserted in-migration: `product_module_resources.resource_name` and `audit_entity_manifest.table_name` key on the name, so leaving them would have dropped the table out of `modules.carebase` enforcement and the audit coverage report *without erroring*; and the three sync RPCs embed the name in their bodies, so every offline sync would have started raising "relation does not exist" on deploy (recreated from their live definitions, every other line asserted byte-identical). The append-only guard was renamed with it, on the same principle `20260802060000` used when it refused to reuse `prevent_phase4_evidence_mutation` -- a guard whose message misdescribes what failed is a trap for whoever reads the error log. **Still open: absorbing `offline_observation_draft_receipts`.** That is a data migration between two append-only PHI tables plus a fourth sync function to retarget -- a different kind of change from a rename, deserving its own reviewable diff. It is now a plain widening rather than a widening plus a rename, which is why this half went first. **Closed (`20260803150000`).** The fourth kind now lives on the one ledger: `offline_draft_receipts` gained `observation_type` and `observation_id`, both CHECKs widened to admit `clinical_observation`, the rows moved, and `sync_offline_clinical_observation_draft` was recreated from its live definition pointing at the survivor. The uniqueness promise the observation table's own header called "the only thing preventing a reconnect from double-charting a vital sign" now holds across every key a device can mint, rather than within one lane -- it was true before only because both lanes happen to use `crypto.randomUUID()`, which is luck, not a constraint. **One thing was genuinely given up and is recorded rather than glossed:** the absorbed table constrained `outcome` to four values and argued that `conflict` and `stale` are states its flow can never reach. That was correct and useful, and the shared ledger permits all six, so the column no longer says it. Re-encoding it as a per-kind CHECK arm would restate a rule the sync function already guarantees by never writing those values; the shape CHECK still pins what each kind must *carry*, which is the half that catches a malformed row. The row move is an `insert ... select` with an explicit count assertion and a pre-flight cross-lane key collision check, because these rows are append-only evidence and, unlike the rename, this cannot be undone by renaming back -- both guards were mutation-tested (a deliberately lossy copy raises; a planted duplicate key raises before the insert rather than surfacing as a bare 23505). The two registries that key on the table NAME came out with delete-count assertions for the same reason the rename asserted its updates: a leftover row would keep describing a dropped table to entitlement enforcement and to `get_audit_coverage`, and neither would error. The absorbed lane also had no pgTAP coverage at all, which is how a second ledger went unnoticed in the first place; it now has twelve assertions in the same file as the other three tiers -- including a cross-kind replay proving a key spent by a change observation is spent for vitals too, and that the replay charts nothing.
9. **The dependency gate no longer fails a branch for the world's schedule.** *(Closed — recorded because it changed a security gate's semantics.)* `check-dependencies.mjs` audits the LIVE npm advisory database, so a newly-published advisory turned every open branch red the day it landed, main included, with no commit behind it — three times in one week (GHSA-mh99-v99m-4gvg, then GHSA-rgw5-rvv9-x895, then GHSA-7p8r-x3mc-p8w7), each time blocking branches that had not touched a dependency. The failure mode that matters is not the delay, it is that people learn to read a red gate as noise. Neither obvious fix is acceptable: stop auditing live, or pin a stale snapshot — both trade away what the gate is for. Instead the gate now answers a question it could not before: did THIS change introduce the vulnerable package? `--base <ref>` runs the same audit twice, for HEAD's dependency set and the base ref's, and an advisory firing against both is pre-existing — reported loudly, not failed. Comparison is by advisory id across two registry audits rather than by matching `vulnerable_versions` locally, because reimplementing semver range logic inside a security gate is where a subtle bug would be invisible. **Fails closed:** no `--base`, or an unreadable one, degrades to the original strict behaviour with a printed reason; pushes to main pass no base and are always strict. The pre-existing half lands in `dependency-advisories.yml`, a daily strict audit of main that opens an issue — so a vulnerability main ships still gets surfaced without waiting for someone to push. `--self-test` covers the classifier, including that a null base fails everything.

---

## Ticket register

Status values: `open` · `in_progress` · `blocked` · `done` · `ops_only`
Size: `S` days · `M` 1–2 weeks · `L` multi-week

### Tier A — Launch / revenue locks (do first)

Ops-only rows are tracked in [docs/ops/TIER_A_PILOT_OPS_CHECKLIST.md](docs/ops/TIER_A_PILOT_OPS_CHECKLIST.md).

| ID | Ticket | Size | Status | Notes |
| --- | --- | --- | --- | --- |
| A1 | Deploy residual migrations + edge functions; verify migration stamp | S | ops_only | Code on main; production apply is ops — includes `20260801220000_durable_import_apply_rpcs.sql` from #413 |
| A2 | Map flat Stripe Prices; internal checkout smoke with qty=1 | S | ops_only | See BILLING_MODEL.md launch checklist. **Mapping half done** (`20260813180000`): Train and CareBase month/year now carry livemode `price_...` IDs on `acct_1SinLKB06O2UFlNz`, and the catalog agrees with `marketingPricing.ts` at flat $239/$2,390 and $499/$4,990. **The smoke test cannot run yet, and the blocker is now identified rather than assumed**: two Edge Function secrets are absent from the project — `STRIPE_SECRET_KEY` and `STRIPE_BILLING_WEBHOOK_SECRET`. Each fails differently and both are required: Checkout and Portal answer `503 billing_not_configured`; the webhook rejects every event `400 invalid_signature`, so nothing reconciles (`app_private.stripe_billing_events` has zero rows and no Stripe event has ever been processed); (`BILLING_RETURN_URL_ORIGINS` was listed here as a third blocker; `4eb298c` added a fallback through `PUBLIC_APP_URL`/`SIGNUP_REDIRECT_ORIGINS` to a `https://cmcarebase.com` default, so it is no longer one -- still worth setting explicitly, since unset means the allowlist is whatever that chain resolves to and a deploy on another origin is refused `400 invalid_return_url`). Set all three, then run the smoke test. Until the webhook is verified working, the `existing_subscription_requires_portal` guard reads local subscription state Stripe can never populate, so a customer who completes Checkout and returns can start a second subscription. **2026-08-14, verified against live Stripe `acct_1SinLKB06O2UFlNz` ("CareMetric AI"):** all four Prices are correct and checkout-ready -- active, livemode, `billing_scheme: per_unit` with `tiers_mode: null` (simple flat, which is what the ops note in BILLING_MODEL.md requires, not graduated), `usage_type: licensed`, USD, and `unit_amount` 23900 / 239000 / 49900 / 499000 exactly matching `package_billing_prices`. **The webhook endpoint also already exists and is correct** -- `we_1U48PvB06O2UFlNzGy00BF7I` -> `https://xsqobvvreaovwibxwyvv.supabase.co/functions/v1/stripe-billing-webhook`, status enabled, subscribed to every event the processor handles, and pinned to api_version `2026-02-25.clover`, which confirms `STRIPE_API_VERSION` names a real Stripe version. Only its signing secret needs copying into `STRIPE_BILLING_WEBHOOK_SECRET`. Zero customers and zero subscriptions on the account, matching the database exactly -- a clean slate, no reconciliation backlog and no double-billing exposure from existing state. **Two genuine gaps remain beyond the secrets.** (1) *The Customer Portal has never been configured*: `/v1/billing_portal/configurations` is empty, and `create-billing-session` creates portal sessions with only `customer` and `return_url`, relying on the account default -- so "Manage billing" will fail even once the secrets are set. This is checklist step 4 in BILLING_MODEL.md and it is not done. (2) *A second live webhook endpoint consumes the same billing events*: `we_1SjmmSB06O2UFlNz5zvbhuq6` -> `https://caremetricai.com/api/stripeWebhook` (api_version `2025-12-15.clover`) is enabled and subscribed to `checkout.session.completed`, `customer.subscription.*` and the invoice payment events. Dead or live, both are problems -- failing deliveries that Stripe eventually disables, or a second system acting on CareBase's subscription events -- so it needs reviewing before launch. A third endpoint, `stripe-connect-webhook`, points at the unrelated bolt-native-database project and carries Connect events only, so it does not overlap. Separately, **no sales tax is collected**: Stripe Tax is `status: pending` (missing head_office), the Prices are `tax_behavior: exclusive`, and Checkout never sets `automatic_tax` -- a deliberate business/compliance decision to make, not a defect |
| A5 | BAAs / HIPAA-eligible tiers confirmed for the live customer path | S | ops_only | Legal/attorney-approved 2026-08-04, confirmed via product owner (no separate written record) -- the clinical-path BAA/HIPAA-eligible-tier language itself is cleared. Remains `ops_only` until BAA execution with the actual vendor(s) and tier confirmation in the live billing config are done outside the repo; legal sign-off on the language is not that execution. |

### Tier B — SCORM production hardening

Full plan: [docs/design/SCORM_PRODUCTION_HARDENING.md](docs/design/SCORM_PRODUCTION_HARDENING.md)

| ID | Ticket | Size | Status | Notes |
| --- | --- | --- | --- | --- |
| B1 | Bundle `learning-runtime-bridge.js` into accepted package zip at accept time | M | done | `accept-learning-package` edge injects bridge via fflate, re-hashes, updates `content_sha256`; client cannot skip. Storage bucket + org-scoped policies in `20260731230000`. Residual market confidence is B3; production verify is A1. |
| B2 | Handshake timeout + learner-visible recovery in `StandardsRuntimePlayer` | S | done | #355. 12s watchdog, `idle→waiting→connected/timed_out/error`, learner-visible recovery |
| B3 | One Storyline + one Captivate golden fixture package in repo | M | in_progress | Contract fixtures (`storyline-shaped` / `captivate-shaped`) prove the API. Real vendor exports are owner-gated — see fixtures README owner drop-path. |
| B4 | Bridge SCORM complete → training record / hour bucket | M | done | Trigger on first completed learning_runtime_commits row → bridge_learning_runtime_completion; assignment + training_records + hour buckets when courses.training_type_id set; quiz blocks still gate; pa_today(); internal-only |
| B5 | Trainer package quarantine UX (reject reason + re-upload) | S | done | `QuarantinePackageDialog` imported into `GovernedLearning.tsx`; replaces `window.prompt`. Trainer can now reach quarantine from the Standards tab |

### Tier C — Plan of Correction depth

Full design: [docs/design/POC_LIFECYCLE.md](docs/design/POC_LIFECYCLE.md)

| ID | Ticket | Size | Status | Notes |
| --- | --- | --- | --- | --- |
| C1 | Immutable POC versions on submit (append-only history) | M | done | #355. `plan_of_correction_versions` + `submit_plan_of_correction` + `list_plan_of_correction_versions` |
| C2 | Effectiveness gate before `verified` | M | done | `20260801120000_poc_verify_requires_closed_actions.sql` added the missing half: verify now also counts corrective actions not in (`completed`, `cancelled`) and refuses while any remain, including actions reopened after `corrected` |
| C3 | Auto work_items from open corrective actions | S | done | #355. `submit_plan_of_correction` inserts deduplicated `violation_corrective_action` work items on the PA facility day |
| C4 | POC due-date escalation into manager digest / SMS | S | done | **Digest half** (`20260802080000`): the manager weekly digest counts plans of correction due within 7 days or already overdue, on violations not yet `corrected`/`verified`, facility-scoped and cut on the PA day. **SMS half** (`20260803000000`): daily `escalate-plans-of-correction` sweep, registered in the job control plane as critical. Two types — `plan_of_correction_due_soon` warns three days out through the preferred channel; `plan_of_correction_overdue` joins the critical set, which is the email+SMS path. So SMS only follows a warning that already went out, and only for a missed deadline. `poc_due_soon_notified_at`/`poc_overdue_notified_at` on `dhs_violations` give the daily job a memory (warn once; re-escalate weekly while outstanding), and a BEFORE UPDATE trigger clears both when `poc_due_date` moves so an extended deadline is warned about again. Templates carry no violation free text |
| C5 | Entrance-conference ordered packet by reg number | M | done | First-class `citation_ref` on `survey_evidence_packet_items` + `p_citation_ref` on `add_survey_evidence_packet_item`; list/assemble order prefer `citation_ref` then label parse; Survey Day UI citation input + badge; pgTAP covers structured citation preference. Label-parse fallback remains for older rows. |

### Tier D — Delivery & imports

| ID | Ticket | Size | Status | Notes |
| --- | --- | --- | --- | --- |
| D1 | Monday manager digest email | S | done | The digest **was already built and scheduled** — `queue_manager_weekly_digests()` on `manager-weekly-digest` (Mon 12:00 UTC), with `manager_digest_snapshots`, a rendered page, and registered email/push templates. The row's previous note ("the digest itself is still not built") was wrong. What SG-1's closure exposed was a *duplicate*: `send-monday-digest` fired the same minute to the same audience, so every manager got two. `20260802080000` retires that schedule and folds its resident-compliance counts (the one thing it reported and the survivor didn't) into the manager digest |
| D2 | Turn on due/overdue/approval notifications for all organizations | S | done | This *was* SG-1 — closed by `20260802030000_remove_pilot_program.sql`, which set the release flags to `global` |
| D3 | Durable import worker (apply from ledger, resume after browser close) | M | done | All 8 domains durable under service-role: `employees`, `residents`, `resident_contacts`, `assessments` via direct table; `rooms`, `credentials`, `training_records`, `incidents` via dedicated `import_apply_*` SECURITY DEFINER RPCs granted only to service_role (#413). No table-level INSERT/UPDATE grants widened on restricted tables. |
| D4 | Column mapping UI for non-canonical CSVs | M | done | Client-side relabel; `importColumnMapping.ts` + `ImportColumnMapping.tsx` |
| D5 | Sample realistic PA facility CSVs in Help / Import Center | S | done | Sample employee / training-record / credential CSVs under `public/import-samples/` with `importSamples.ts` registry and `ImportSampleDownloads` component. Column order matches `importTemplate()`. Component is now rendered on `DataImportCenter` (after domain-templates card) so samples are reachable from the UI. |

### Tier E — Daily operations wedges

| ID | Ticket | Size | Status | Notes |
| --- | --- | --- | --- | --- |
| E1 | Home IA: Today = action, scorecard = health, Command Center = survey | S | done | Sidebar + primary surface titles/subtitles now read as Today = action, Compliance scorecard = health/trends, Inspection Readiness / Survey Day = survey prep / live entrance conference. |
| E2 | Med-admin "who can pass meds today" board on Schedule | M | done | `useMedAdminAuthorization` shared hook; per-employee badge + no-one-authorized gap banner on `ScheduleDetail.tsx` |
| E3 | Fire drill DHS 9-field form + monthly tracker PDF | M | done | `generate-fire-drill-tracker-pdf` + `InspectionItems.tsx` action; #5 PCH / #3 ALF citation |
| E4 | Policy campaign center (version pin, targets, knowledge check) | L | done | MedTrainer deal-breaker. **Two of the three parts already shipped in `20260705151703_policy_attestation_core` and were reachable from `PolicyDocumentDetail.tsx`** — version pin (`policy_attestation_campaigns.policy_document_version_id` is NOT NULL, published versions frozen by `lock_published_policy_version`, every attestation stores the signed `content_hash`) and targets (campaigns fan out to explicitly picked employees at assign time, the training-plan pattern the core migration chose deliberately). **Knowledge check was the genuine gap and is now done**: `policy_campaign_questions` + `policy_knowledge_check_attempts`, server-side grading via `submit_policy_knowledge_check`, questions read through `get_policy_knowledge_check` (answer key absent from its signature, not merely filtered), `attest-policy` refuses to record an attestation for a campaign with questions until a passing attempt exists. **Reminders were listed as open and were not** — `send_policy_attestation_reminders()` shipped in `20260705152437` and has been scheduled daily since `20260711162509`. It had no test coverage, and `20260803010000` fixes three targeting defects it had accumulated: it stamped `reminder_sent_at` on attestations it had not notified (the UPDATE's predicate was broader than the INSERT's), it reminded terminated employees indefinitely, and it created notification rows for deactivated profiles. **Declarative targeting done** (`20260803050000`, `20260803060000`): a campaign can now be a rule rather than a point-in-time pick. `targeting_mode` plus the predicates `compliance_profile_mapping_rules` already matches employees on -- facility, facility type, worker type, job-title ILIKE -- deliberately reusing that vocabulary rather than inventing a second one. `materialize_policy_campaign_targets` enrols every active matching employee not already on the campaign (idempotent via `policy_attestations_campaign_employee_uk`), the `materialize-policy-campaign-targets` daily job keeps membership true as the roster moves, and the creation RPC sets the target and enrols the initial roster in one transaction so an author does not see an empty campaign until the next sweep. A `CHECK` forbids a declarative campaign with no predicates -- "no predicates" reads downstream as "no constraint on any dimension", which would enrol the whole organization. Enrolment is never revoked: an attestation on file is evidence, and leaving the rule is not the same decision as deleting it. **Recurrence done** (`20260803070000`, `20260803080000`), closing the row: `recurrence_months` on the series parent, `spawn-policy-campaign-cycles` opening each cycle 30 days before it is due, pinned to the document's *current* published version and carrying the targeting rule and knowledge-check questions forward as its own copies. Each cycle is a new campaign rather than a reset, because an attestation is evidence -- clearing last year's signatures to start this year would destroy the record the feature exists to produce. A `CHECK` stops a spawned cycle from recurring itself, which would double the series every period |
| E6 | A promoted staff member cannot sign a policy they are assigned | S | done | **Found and closed 2026-08-03 while fixing the E4 reminder sweep.** `/app/my-attestations` (`App.tsx`, `ROSTERED_STAFF_ROLES` = org_admin/facility_manager/trainer) renders the same `MyAttestations` page for rostered non-employee roles — the page already resolved off `useGetEmployeeByProfileId(user.id)` with no role assumption, so it is correct there unchanged and shows "no employee record" for a manager who has none. `20260803020000` makes both attestation notifications resolve their link from the recipient's own profile role instead of hard-coding `/me/attestations`. Deliberately **not** in `Sidebar.tsx`: most managers hold no `employees` row and a permanent nav item would be a reliably empty page for them; the route is registered in `APP_PAGES` so the manifest governs it and search finds it, and the people who need it arrive from their notification. The original defect: a profile keeps its `employees` link when its role changes — `admin_update_profile` severs `employees.profile_id` only when the *organization* changes (`e.organization_id is distinct from v_row.organization_id`), not the role — and `apply_scim_change` links an IdP-mapped role to an employee row with no role constraint. So the ordinary "the aide is now the shift supervisor" promotion leaves a `facility_manager` (or `trainer`) holding an employee record. Assign that person a policy attestation and they **cannot satisfy it through any UI**: the only self-service surface is `/me/attestations`, which `ProtectedRoute` restricts to `allowedRoles={["employee"]}` and silently redirects them to `/app`. The attestation stays `pending` forever, the campaign reports them outstanding forever, and `send_policy_attestation_reminders` re-notifies them every three days — `20260803010000` makes that *more* visible, since their employee row is active and their profile is active. `/me/*` stays employee-only by design — the fix adds the `/app`-side surface rather than admitting other roles into the employee portal, so `EMPLOYEE_ONLY` in `appDomains.ts`, `helpCopilot.ts` and `navigationPreferences.ts` are untouched |
| E5 | Offline service documentation drafts (IndexedDB) + conflict rules | L | done | Tier 1 (MVP) done: a direct-care employee can document one already-queued floor task while offline and have it sync with block-and-flag conflict handling — new `offlineServiceDraftSafety.ts` (closed-interface draft shape, runtime value validation), `offlineServiceDraftCache.ts` (own encrypted IndexedDB store, separate from `offlineLearning.ts`), `useOfflineServiceDrafts.ts`, `UnsyncedDraftsPanel.tsx` on `Floor.tsx`; server side `sync_offline_service_task_draft` classifies applied/duplicate/conflict/stale/rejected/wipe_required against `record_service_task_response`, `offline_service_draft_receipts` append-only. Proactive wipe wired in `auth.tsx` on sign-out and identity change. Deliberately does not snapshot the full resident record or other residents' data. **Tier 2 server side done** (`20260803090000`): unscheduled services -- the care nobody else knows happened -- now have an offline path. Tier 1 could only document an already-queued task, so an aide in a back hallway could record a scheduled reposition but not the unplanned one they did five minutes earlier. `sync_offline_unscheduled_service_draft` mirrors Tier 1's semantics exactly (device ownership as a hard failure, idempotency replay returning the *original* outcome, clock-plausibility judgment) and **delegates authorization to `record_unscheduled_service`** rather than restating its caller-scope rule. `offline_service_draft_receipts` gained a `draft_kind` discriminator rather than being copied, so `unique (device_id, idempotency_key)` keeps holding across both kinds -- two tables would have meant two independent uniqueness domains and a quietly weaker promise. **Tier 2 complete.** `OfflineFloorDraft` is now a discriminated union, the IndexedDB store carries both kinds, `syncDraft` routes to the right RPC, and `useSaveOfflineUnscheduledDraft` creates one — and `UnscheduledServiceDialog` on `Floor.tsx` falls back to the offline draft path — proactively when `navigator.onLine` is false, and in its catch when the call fails at the network level, which is the case `navigator.onLine` misses (LAN link, no route to Supabase). A genuine rejection still surfaces rather than disappearing into a silent draft. The migration hazard was real and is covered: `taskId` is bound into the envelopes **AES-GCM AAD**, not merely stored, so records already on devices are sealed against a scope built from it. `scopeId` is written for both kinds and equals `taskId` for a service draft, keeping new scope strings byte-identical, and readers fall back to `taskId` when `scopeId` is absent. A test removes that fallback and fails, and only that test. The store-level constraints this had to satisfy, for the record: `OfflineServiceDraft` becomes a discriminated union (its `taskId` is required today and an unscheduled draft has none); `StoredDraftRecord` needs `taskId` optional plus a `kind` discriminator, and **reads must treat a missing `kind` as `service_task`** because records already sitting on aides' devices predate it; **No IndexedDB version bump was required** — `DRAFT_STORE` is created with `keyPath: "draftId"` and no secondary index, so `taskId` is a plain payload column and making it optional changes no index. **Tier 3 done, closing the row** (`20260803100000`): a change-of-condition event carries a monitoring cadence ("check every two hours for 24 hours"), and walking it is what produces the evidence the resident was actually watched -- in resident rooms and back hallways, where the wifi is worst. `sync_offline_change_observation_draft` delegates to `add_change_event_monitoring` exactly as Tier 2 delegates to `record_unscheduled_service`, and `offline_service_draft_receipts` gained a third `draft_kind` rather than a third table, so `unique (device_id, idempotency_key)` still holds across every kind a device can generate -- now asserted behaviourally, by replaying a Tier 2 key through the Tier 3 RPC and getting Tier 2's outcome back. Two rules are specific to this tier. **A closed event is `stale`, not `rejected`**: `add_change_event_monitoring` raises the same `22023` for a closed event and for unusable text, and the offline path re-reads the event status to tell them apart, because an observation that can no longer be filed is real and needs a supervisor, not a retry. **An implausible device clock is clamped to `now()`, not nulled** as in Tiers 1-2, because `resident_change_monitoring_entries.observed_at` is NOT NULL -- nulling it would fail the insert and cost the aide a real bedside observation over a clock problem they cannot see; the raw client value stays on the receipt so a bad clock is still visible in the ledger. **The observation text is deliberately never written to the receipt**, which is append-only: a `rejected` or `stale` observation never enters the resident's record at all, and copying its clinical text into a table that can never be corrected or removed would create a permanent second copy of exactly what the record deliberately does not have. `UnsyncedDraftsPanel` is now mounted on `ChangeOfConditionDetail` as well as `Floor` -- it is a device-level outbox, and a draft whose only surface an aide never opens would sit invisible until the purge ceiling silently deleted it. `create_resident_change_event` is **deliberately excluded**: it opens a regulatory workflow (compliance item, incident, follow-ups, reassessment) and has no idempotency rule, so two offline aides finding the same resident on the floor would produce two events, two compliance items and two incidents for one fall -- worse than the aide getting a plain "you are offline" and telling a supervisor in person, which is what identifying a change of condition requires anyway. Also aligned Tier 2's revoked-device check with Tiers 1 and 3: it tested only `revoked_at`, which no reachable state distinguishes today, but a boundary a later change can defeat by moving one column and not another is held together by coincidence. |

### Tier F — Engineering hygiene

| ID | Ticket | Size | Status | Notes |
| --- | --- | --- | --- | --- |
| F1 | Split pages >40 KB before feature work (`CourseDetail`, `ResidentFinancialOperations`, `ResidentAssessmentFormEditor`) | M | done | Decomposed into `course-detail/`, `resident-financial-operations/`, `resident-assessment-form-tabs/` |
| F2 | Finish route-manifest ownership of sidebar/search/modules | M | done | All three lists now governed by `routeRegistrationIssues`: `APP_COMMAND_ACTIONS` (command palette), `productModules.ts`'s `CORE/TRAIN/WORKFORCE/COMPLIANCE/BILLING_PATHS`, and `Sidebar.tsx`'s own `getNavSections` (read as text, like `App.tsx`). Caught a live bug in the process — `/app/survey-day` was in three sidebar sections with a registered route but no `APP_PAGES` entry, so `canViewPath` silently dropped it from the nav for every role |
| F3 | Replace root README marketing handoff with product + agent runbook | S | done | Old content relocated to `docs/marketing/MARKETING_SITE_REDESIGN_HANDOFF.md` |
| F4 | Banner stale root reviews as historical | S | done | 29 documents bannered; `check:planning-registers` keeps them that way |
| F5 | Reduce total JavaScript shipped to the browser | M | done | **Resolved by measuring rather than optimizing — the premise was wrong.** This row was opened on "4.2 MiB of JavaScript pushed to a tablet on facility wifi". That number is the sum of **every** chunk, including ~150 lazy route chunks a given user never fetches, so it never described anyone's download. What a first load actually costs: `index.html` preloads exactly five chunks — `index` 533, `supabase` 207, `radix` 133, `query` 35, `router` 12 KiB = **922 KiB raw**, and `server/index.mjs` negotiates `br` over `gzip` against the `.br` siblings `server/precompress.mjs` emits, so **220 KiB brotli actually crosses the wire**. For an application of this size that is unremarkable, and the largest single piece (`index`, 127 KiB brotli) is the app shell rather than anything liftable. No optimization is warranted. The aggregate budget stays useful as what it was always documented to be — a regression tripwire that catches a route module leaking into the shell — not a user-facing weight. Re-open only if the *entry path* grows, which is the thing worth watching |
| F6 | Confirm two pending regulatory grace periods; close the citation-library CI-coverage follow-up | S | done | 2600.141 (PCH medical evaluation) and 2800.225 (ALR annual reassessment) annual-cycle grace periods confirmed at 15 days via PA DHS's own Regulatory Compliance Guides (`20260804000000`); `paRegulatoryCitations.ts`'s two `pending_confirmation` entries are now `verified`. `check-dhs-sources.mjs` extended to live-check the citation library's `pacodeandbulletin.gov` links plus its own staleness date, closing that file's own FOLLOW-UP comment. **2600.141's confirmed figure is not yet applied to `grace_period_days`** -- see F9 -- so this row closes the research/citation-library scope only, not the operational schedule for PCH medical evaluations |
| F7 | De-duplicate hand-synced TOTP derivation in e2e tests | S | done | `resident-lifecycle.spec.ts` and `role-routing.spec.ts` each derived TOTP codes independently ("kept in sync by hand"); extracted to `e2e/helpers/totp.ts` matching the existing `e2e/helpers/` convention. Pure extraction, confirmed byte-identical output |
| F8 | Notify platform admins when a demo request arrives | S | done | `request-demo` previously only logged a comment that admins triage the table by hand. `notify_platform_admins_of_demo_request` (`20260804110000`) fans out an in-app `public.notifications` row (existing bell, no new external integration) to every active platform_admin; called best-effort, after the `demo_requests` insert already succeeded, so a notification bug can never fail a real submission. `notification_type` CHECK widened additively (`demo_request_received`); `organization_id` made nullable for this org-less recipient case. **Post-review fix (2026-08-04, Codex):** body originally put uncapped `name`/`organization` before the contact email, which a long value could push past `Header.tsx`'s `line-clamp-2` with no expand/detail-page affordance (`link` is `null`) to recover it -- reordered email-first with defensive length caps |
| F9 | Split `medical_evaluation` into separate initial/annual item_types so 2600.141's confirmed grace can be applied | M | done | **One row was carrying two regulatory cycles, and the two facility types had resolved that impossible choice in opposite directions.** `medical_evaluation` covered both the evaluation due at admission and the annual one, with a single `grace_period_days`: `instantiate_resident_compliance_items()` copied it onto the initial item and `complete_resident_compliance_item()` copied it forward onto every successor. **ALF carried 15 on both tracks** — this register's own note on those rows calls it a *15-day annual grace*, and it was being applied to initial evaluations too, so an ALF resident whose initial evaluation was ten days past due read as covered. That is the permissive error and the dangerous one. PCH carried 0 deliberately, which `20260804000000` explains at length: it confirmed the annual figure and refused to apply it because of this exact sharing, leaving annual evaluations flagged overdue 15 days early. The strict error — an operational cost, not a compliance risk. `20260804170000` performs the split the assessment items already have: `medical_evaluation` keeps its meaning as the initial evaluation at grace 0 for both facility types, and `annual_medical_evaluation` is new and carries the 15 days both RCGs confirm. **No regulation was re-researched** — both figures were already in the table with their sources; they finally have rows that can hold them. **The test caught a design error in the first cut.** Instantiation creates one item per active rule pack at admission, so the annual row was being created at admission *and* again at completion. That also exposed why the annual cannot be admission-anchored at all: 2600.141(b)(1) is an anniversary of the most recent evaluation, so a resident admitted on the 1st and evaluated on the 25th would owe the next one 24 days early, every year. New `instantiate_at_admission` column — explicit, rather than overloading `is_active` or `offset_basis` — and the completion path builds the successor from the annual row. Existing items are split by position: a resident's earliest evaluation is the initial one, later ones are the renewals the old code produced by copying the type forward. **This changes live status for ALF residents whose initial evaluation is 1–15 days past due — they move from compliant to expired, which is what the regulation says they were the whole time.** 21 pgTAP assertions, including the pair the shared row made impossible: an ALF initial ten days past due is `expired`, an ALF annual ten days past due is not, and sixteen days is (so the grace is 15, not unbounded) |
| F10 | Reconcile `paRegulatoryCitations.ts`'s informal `verified` status with `dhs_citation_topics.verification_status`'s governed one | S | done | **The two systems disagreed in the direction that flatters the product: the informal one said yes to everything.** All seven entries in `paRegulatoryCitations.ts` carried `verification.status: "verified"`. The governed column, which only `record_citation_verification()` can set and which demands a named platform admin, a date and a source URL, says `approximate` or `unverified` for those same sections — and still does, because that function has never been invoked for any citation. Of the two options this row offered, only one was mine to take: running the RPC is a real human attestation and fabricating it would be exactly the laundering the governed column exists to prevent. So the library stops claiming a status it does not own. `verification: { status, note }` becomes `provenance: { note }` — the note was always the valuable half, recording which reviewed artifact each wording came from, and it survives intact. `citationDisplayLabel` now takes the governed status and **defaults to "not verified" when given none**, which is the truthful default and currently describes every entry; `verified` deliberately adds no badge, so its absence everywhere else is visible rather than ambient. `AssessmentReviewDialog` and `AssessmentsTab` read `dhs_citation_topics` for it. A test now asserts no entry has a `verification` property at all, so the parallel status cannot quietly return. **Still open, and not something code can close:** no citation is governed-verified. `InspectionReadiness` was already telling the truth about that; now the assessment surfaces do too |

### Tier G — Shipped schema nothing calls

Found by sweeping every `public` RPC granted to `authenticated` against the client and edge-function
source. This is the shape the Appointments and hospital-return gaps both had: schema, RPCs, RLS and
grants all present and correct, and no caller anywhere, so the capability reads as delivered in the
plan and is unreachable in the product. The sweep is `scripts/`-free by design (it is ten lines of
grep) but the finding is not: **79 of 556 granted RPCs have no caller**, most of them legitimately
(RLS helpers like `current_role`, clock helpers like `pa_now`, functions called from other function
bodies). The rows below are the ones verified to be genuine user-facing dead ends.

| ID | Ticket | Size | Status | Notes |
| --- | --- | --- | --- | --- |
| G1 | Hospital return has no write path | M | done | `complete_hospital_return` had no caller. `start_hospital_transfer` **is** wired, so an episode could reach `status='out'` and stop there permanently — and `ResidentHospitalSection` said "the resident is currently out, reconciliation starts when they return" with no way to record a return. Everything Phase 5b delivered hung off that call: the reconciliation checklist, the gated closure, the seeded `hospital_return_review`, the follow-up work item, and the `hospital_return_reconciliation` card. All unreachable. `RecordHospitalReturnDialog` closes it; verified end-to-end through PostgREST as an authenticated `org_admin` (episode → `returned`, work item `hospital_return`/open/24h, review seeded `draft`, timeline row) |
| G2 | Citation verification has no write path | M | done | Phase 10b shipped `record_citation_verification`, `record_citation_superseded` and `get_citation_governance_status`, platform-admin only, and **nothing calls any of them**. `citationGovernance.ts` is wired to *display* the qualifier, so the readiness table correctly shows citations as approximate/unverified — but there is no surface through which anyone can ever record a verification. The plan's own position is that seeding needs a compliance SME with the regulation in front of them; this row is the narrower point that even an SME has no way in. `CitationGovernanceSection` (lazy, 9.1 KiB, mounted on `/admin/regulatory-updates`) is the way in: status counts, a stale-verification warning, the full topic table with each row's live qualifier, and dialogs over both write RPCs. `verificationFormIssues` states the server's rules up front rather than letting the operator find out by submitting — the server still enforces them, and both refusals were exercised. Verified end-to-end over PostgREST as a platform admin: a sourceless verification is refused ("Verification requires the source the citation was read from"), a real one moves the row to `verified` with the verifier and date stamped, the governance summary went `{unverified:16, approximate:6}` -> `{verified:1, unverified:16, approximate:5}`, and a supersession with no successor is refused. **No content was seeded** — that still needs a compliance SME with the regulation open, which is the plan's position and unchanged |
| G3 | Admissions pipeline stage has no advance path | S | done | `advance_admission_pipeline_stage` has no caller, so the Phase 9a funnel is read-only through the UI. **Checked, and it was worse than the row guessed.** `update_admission_prospect` writes `stage` (the decision lifecycle); `pipeline_stage` (the fourteen-stage funnel) is written by `advance_admission_pipeline_stage` and by nothing else, so after `20260726170000`'s one-time backfill every new prospect sat at its `new_inquiry` default permanently — the funnel, its conversion rates, its referral-source ROI and its weighted pipeline value were all computed over a column that never moved. **What hid it:** the review dialog's one stage control was *labelled* "Pipeline stage" and bound to `stage`, so it looked like the funnel had a write path. That label now says "Decision stage", and a real funnel control sits beside it, saved by its own RPC and showing the stage's meaning plus a note when the move is backwards. Backwards is permitted deliberately — `admissionPipeline.ts` says a funnel that refuses to record a regression gets worked around in a spreadsheet. Verified over PostgREST as an `org_admin`: `new_inquiry` -> `tour_scheduled` accepted, an unknown stage refused, a backwards move to `contact_attempted` accepted |
| G4 | Audit the other verified-dead RPCs | S | done | **Audited; each one checked rather than assumed, which changed the answer three times.** Results split into G5–G8 below. Two turned out not to be dead ends at all: `get_resident_administrative_packet` is a superseded read (the record reads the table directly and writes through `save_resident_administrative_master`), and `grant_duty_eligibility_override` is a *second* override path alongside the wired `create_schedule_eligibility_override` — a duplicate-concept question, not a gap, and the same shape as the two-ledger and digest/sweep divergences this repo has closed before |
| G5 | Medication resident mapping had no write path | M | done | `map_medication_resident` is the only writer of `medication_resident_mappings` and had no caller. An `unmatched_resident` exception — the eMAR sending administration data for somebody CareBase cannot identify — could be acknowledged, assigned an owner, given an SLA work item and dismissed, but **never actually fixed**, because fixing it means creating the mapping. Everything downstream joins through that row, so the administration evidence for an unmapped resident never reaches their chart or timeline. The exception dialog now offers "Map to resident" for exactly that exception type; the RPC resolves the exception itself, so the two cannot disagree. Verified over PostgREST as an `org_admin`: mapping row created, exception auto-resolved with "Resident mapping completed" |
| G6 | A completed training class cannot be corrected | S | done | **Confirmed by the database itself:** seeding a completed class's attendance directly raises *"completed class attendance is immutable; use the audited correction RPC"* — the RPC that had no caller. `complete_training_class` was wired; `correct_completed_training_class` and `correct_completed_class_attendee` were not, by anything, so the one legitimate way to fix a wrong attendance record did not exist in the product. `CompletedClassCorrectionCard` (lazy, org_admin/platform_admin only, matching `assert_completed_class_corrector`) is that surface. It offers only the four fields the server accepts, because hours and dates are what each attendee's training record was computed from. `completedClassCorrection.ts` pins the 10-character reason minimum and the field allow-list to the migration SQL by test, so the form and the server cannot drift. Verified over PostgREST as an `org_admin`: short reason refused, `scheduled_hours` refused, a real correction applied, and correcting attendance absent→attended **created the training record**, which is what compliance is computed from |
| G7 | A shift swap request cannot be withdrawn | S | done | `request_shift_swap` and `decide_shift_swap` were both wired; `cancel_shift_swap_request` was not, so a requester could raise a swap but not withdraw one they no longer wanted — it sat in the manager's queue until decided, and the only way out was asking a manager to deny something nobody was asking for any more. A pending-swaps card on `MySchedule` with a Withdraw action, deliberately self-service rather than a second manager control: the RPC scopes itself to `e.profile_id = auth.uid()`, so only the requester can use it, and `shift_swap_requests_select` already let an employee read their own rows so no new policy was needed. Verified over PostgREST: a short reason refused, the withdrawal applied with its reason stored, and withdrawing twice refused (*"Only pending swaps can be canceled"*) |
| G8 | Certification attempts had no entry point at all | L | done | **The row was wrong about the shape, and checking is what found it.** It read as "approval has no surface". The truth: `certification_attempts` and `certification_attempt_items` have full structure and **nothing anywhere creates a row in either** — no RPC, no edge function, no trigger. So `approve_certification_attempt`, which is rigorous (assessor qualified *at observation time*, separation of duties, checklist published and effective then, every required item evidenced and signed, signed decision plus evidence checksum), approved rows that could not exist, and `in_progress` / `submitted` were unreachable states. Building only the approval screen would have produced a screen over a permanently-empty table, so the scope grew deliberately: `20260804120000` adds `start_certification_attempt`, `record_certification_attempt_item` and `submit_certification_attempt` — the observation path — and `CertificationAttemptSection` (lazy, on the employee record's Credentials tab, org-scoped because `assert_phase3_admin` admits org_admin and `qualifications.manage` holders, not only platform admins). `app_private.certification_attempt_blockers` checks the three preconditions knowable *before* an observation, so an assessor is refused before spending an hour at a bedside; it is deliberately a **subset**, not a second gate — approve re-checks everything and stays authoritative. 18 pgTAP assertions drive the whole path as an authenticated `org_admin`, ending on the two that matter: the approval function runs for the first time, and the employee holds the `active` qualification it exists to grant |
| G9 | Survey packet guest access was a one-way door | M | done | **Found by measuring what the first sweep asserted.** That pass said 79 uncalled RPCs were "most legitimately" so and verified ten by hand; re-running it against *all* SQL — other function bodies, triggers, policies, cron — turned the assertion into a count: of 69 with no client or edge caller, **49 are genuinely live server-side, 12 are exercised only by pgTAP, and 8 are reachable by nothing at all.** The sharpest of the 8: `issue_survey_packet_guest_grant` is wired and `revoke_survey_packet_guest_grant` is not, and **no surface listed existing grants**. So a survey evidence packet — compliance evidence assembled for an external surveyor — could be shared with a guest, and afterwards nobody could see who held access or take it back. The same product already does this correctly for move-in guests, where issue *and* revoke are both wired. `GuestGrantList` now shows every grant with its state, expiry and download count, keeping revoked and expired ones visible because who *used to* have access is the auditor's question, and offers Revoke with a required reason. One real correctness detail found while verifying: the RPC returns `false` rather than raising when the grant is missing, so the hook treats a non-`true` result as a failure — resolving successfully there would tell somebody access was withdrawn when nothing was. Verified over PostgREST as an `org_admin`: short reason refused, missing grant returns false, real revocation stamps `revoked_at`/`revoked_by`/`revocation_reason` |
| G10 | The rest of the measured sweep | L | done | **Nineteen findings, and the measurement was right about most of them and wrong about one in a way worth writing down.** Eight fixes, each verified by driving the RPC over PostgREST as a real authenticated user rather than by reading the SQL. **`bulk_update_alert_status`:** `useBulkUpdateAlerts` was updating the `alerts` table directly, so bulk resolution skipped the `alerts_bulk_status_updated` audit log, never stamped `resolved_at` (making a human-resolved alert indistinguishable from a nightly-sweep one) and bypassed per-alert facility authorization; it now calls the RPC and surfaces per-row `failed`/`unauthorized` outcomes instead of reporting success for rows the server refused. **`revoke_class_checkin_tokens`:** generate was wired and rotating every 30s while revoke had no caller — rotation is not revocation, so a code that left the room kept working until it rotated; `QrCheckinCard` now revokes with a required reason. **Governed content — the largest, and it needed a migration:** `GovernedLearning` dispatched only `review_governed_content_revision`, against a revision ID typed into a text box, so it reviewed revisions nothing could create and an approved one could never be published — 3 of 4 lifecycle steps unreachable. Wiring the three RPCs was not enough: `governed_content_assets`, the row every revision hangs off, has `grant select` and nothing else — no INSERT grant, no RLS write policy, no RPC — so in production the table is empty for every tenant with no way to add a row, and `create_governed_content_revision` would have raised 'Governed asset not found' for every course. `20260804130000` adds `register_governed_content_asset` (idempotent on the existing unique key, because bringing a course under governance is a statement about the course rather than an event), and `GovernedContentRevisionsPanel` walks author → submit → review → publish with separation of duties stated up front rather than surfaced as a 42501. Snapshots are built from the course version's own blocks and sorted, so re-snapshotting an unchanged version hashes identically and a reviewer can tell nothing moved. 15 pgTAP assertions drive registration and the whole lifecycle as two authenticated users. **`record_resident_dme_event`:** the register card promised it 'preserves assignment history and repair/inspection documentation' and nothing could write a history row after registration; worse, the 'DME inspections due' metric counts in-use items with no `inspected` row inside their frequency window, so it could only ever rise — every item with an inspection frequency was permanently overdue from the day it was registered. Proven on a live stack: the count went 1 → 0 after recording an inspection through the new register. **`end_enterprise_role_grant`:** the console could issue an effective-dated grant and never close one, a one-way door on standing access; `StandingGrantsCard` lists open grants with their age and ends them with a required reason (verified: blank reason 22023, end-before-start 22007, valid call closes the grant and appends the reason to its permanent record). **`create_hris_import_run` + `upsert_enterprise_role_template`:** Validate and Apply were both wired to a run ID typed by hand, and nothing minted one, so neither was reachable in practice; the grant console likewise asked for a role template as a raw UUID and a tenant could never define one beyond the six seeded built-ins. Both now have real pickers and a create path (verified: same request ID returns the same run, unknown permission and empty permission set both refused). **`get_notification_delivery_health` / `preview_notification_template`:** the console could retry one failed delivery but could not see whether the queue was moving at all — a stuck worker looked like a quiet day — and could only preview the draft in the editor, never the saved version that is actually sending. **The correction:** this row previously guessed `record_service_exception_follow_up` 'may be a superseded duplicate' because the documentation path persists follow-up answers. Checking it: `record_service_task_response` does set `supervisor_notified`, but that is a self-report by the person documenting, and **only** the dormant function creates the `service-exception:<id>` work item — a tracked row with a due date someone must close. So the gap was real but narrower than 'no route to a supervisor', and the button is deliberately offered even when the box was ticked. **Ruled out rather than built:** `set_package_entitlement` is not a dead end — an `ingest_legacy_package_contract` trigger already syncs `packages.features` into `package_entitlements`, which the resolver reads, so the live path works and the RPC only adds effective-dating and a contract reference that no requirement asks for yet. **Recorded, not built, for want of a host surface:** `plan_audit_archive`, `simulate_workflow_automation_rule`, `set_facility_clinical_enabled`, `evaluate_learning_path` (driven by the runtime, not an admin), `create_deduplicated_work_item`; and `unassign_organization_release_cohort`, whose assign counterpart is itself unwired. The sweep now reports **4 reachable by nothing and 6 pgTAP-only, down from 8 and 12** |
| G11 | The sweep reaches zero, and running it found a bug the sweep could not see | L | done | **The measurement was wrong in one direction and the running was right in another.** First the correction: the sweep read `create or replace` and `grant execute` out of migration text and never looked for `drop function`, so it reported `unassign_organization_release_cohort` as a finding six days after `20260802030000_remove_pilot_program.sql` deliberately removed it. Checking the live database rather than the files settled it in one query. The script now honours drops, and 4 of its previous "findings" were functions that do not exist. **Six real ones fixed.** `plan_audit_archive` was the only writer of `audit_archive_batches`, so the deliberate "freeze this range, hash it, record whether a legal hold covers it" step had no beginning; the Data Lifecycle panel now previews the manifest with the same function the plan stores, and the planned batch came back with the identical row count and digest. `simulate_workflow_automation_rule` had no caller while `run_workflow_automation_now` did — so the only way to learn what a rule would do was to let it do it, creating real work items and notifying real people; Value Center now dry-runs, and the simulation is most useful on a draft, which "Run now" refuses. `set_facility_clinical_enabled` is the only writer of a column that gates native clinical charting, so it could only ever hold its default `true` and a facility switched off by a direct write could never be switched back on. `set_package_entitlement` was **not** the dead end this register guessed either — a trigger already syncs `packages.features` into the table the resolver reads — but it is the only way to express a term with dates and a contract reference, and the probe shows exactly that: the immediate `legacy_backfill` row closed at the term boundary and a `sales_contract` term took over from 1 September. `create_deduplicated_work_item` was the sharpest: **the work queue could only ever fill itself**, every item arriving from a detected condition, with no create path in the client at all — and it is the only creator that goes through a template, which is what makes closure evidence enforceable. **And one that needed a migration, because there was nothing to call it on.** `evaluate_learning_path` is careful work — pinned definitions, prerequisites, score thresholds selecting a remedial branch, an explainable transition event per step — and **nothing anywhere wrote `learning_path_definitions`, `learning_path_versions` or `learning_path_assignments`**. No RPC, no trigger, no seed. `20260804140000` supplies authoring, publication and assignment, refusing the three shapes that make a path unwalkable (a prerequisite naming a step that is not in the path does not error at evaluation — it locks that step forever with an explanation that cannot say why). 22 pgTAP assertions walk the whole thing. **The bug reading could not have found.** Driving the new adaptive surface, the stale-state-version case never returned — not a 4xx, not a 5xx, a hung request and several concurrently active backends from one client call. Isolated with a throwaway function that does nothing but raise: `55000` answered instantly, `22023` answered instantly, **`40001` never returned**. PostgREST treats SQLSTATE 40001 as the transient serialization failure it is named for and retries; these are *deterministic* precondition checks that will never pass on retry. Four functions did this, and **two are already wired to live UI** — so today a manager deciding a shift swap somebody else changed underneath them gets a request that never comes back instead of "shift assignments changed after swap request". `20260804150000` redeclares all four with `55000`, the blocks copied programmatically rather than retyped so the only difference is five characters. **And one asymmetry the removal left behind.** That same pilot-program removal kept `assign_organization_release_cohort` and dropped its counterpart, while stating "the general release-flag / cohort / kill-switch mechanism itself is untouched". It was touched: an organization could be put into a cohort and nothing supported could take it out. `20260804160000` restores the original unassign verbatim — completing the removal's own stated intent rather than re-adding the console it deliberately deleted. Verified: assign creates the membership, unassign removes it. **The sweep now reports 0 and 0** — every function granted to `authenticated` has a client caller, an edge caller, or a live server-side reference |
| G12 | The gate that reported zero was counting a comment as a caller | M | done | **The zero this branch reported was not true, and the thing making it look true was the gate added to guarantee it.** A review flagged that `check-dormant-rpcs.mjs` collected every `drop function` across the whole scan and subtracted at the end, so a function dropped once and re-created later stayed excluded -- `unassign_organization_release_cohort` is dropped by `20260802030000` and restored by `20260804160000`, and the end-of-scan subtraction removed it from the check even though it is granted and live. That was real. Fixing it exposed a worse one the review had not seen: with the drop handled, the gate still passed, because `isCallSite` stripped declarative statements but not **comments**. The line excusing the function was the migration's own header explaining why it had been removed. Naming a function in prose -- which is what a good comment does -- marked it reachable. Both are fixed: drops apply per migration in filename order, and comments are stripped from the whole file (block comments before the line split, since a line-at-a-time reader cannot see it is inside one) before any line is tested. Self-test grew from 9 cases to 14, covering cross-migration drop-then-restore, a drop never undone, and prose naming a function. **The sweep now reports six, not zero.** One restored by this branch, five pre-existing on `main` and never caught because the hand-run sweep shared the same blind spot. Each is verified against the live catalog with a word-boundary check -- `LIKE '%name%'` first reported two callers for `get_resident_administrative_packet` that turned out to be its own renamed predecessors -- and each is allowlisted with a specific reason and an open row below. An entry in that allowlist is a debt, not an absolution |
| G12.1 | `unassign_organization_release_cohort` has no caller | S | done | **Wired, together with its counterpart.** The statement-level gate fix showed `assign_organization_release_cohort` is equally uncalled, so this was never a half-wired mechanism -- it was an entirely unreachable one, with the tables intact. `ReleaseCohortMembershipCard` lives on the release-flags page beside the flags and kill switches, which is what `20260802030000` said it was keeping. Deliberately not a re-creation of the pilot console it deleted: no pilot programme, no cohort authoring, just membership of cohorts that already exist. Previously: Restored by `20260804160000` to close the one-way door `20260802030000` left: assign was kept, unassign dropped, so an organization could be put into a release cohort and nothing supported could take it out. The function is the supported exit; the console that called it was deleted on purpose and is deliberately not re-added here. Needs a surface |
| G12.2 | `admin_emergency_update_course_block` has no caller | S | done | **Wired.** `EmergencyBlockCorrection`, offered per block on a published (locked) version and only to a platform admin -- the two conditions the server already enforced. The lock stays the rule; this is the recorded exception, with the reason and the before/after in `audit_logs`. Previously: Pre-existing on `main`. An emergency edit path for a published course block, with its own audit trail, that no admin screen offers |
| G12.3 | `get_resident_administrative_packet` has no caller | S | done | **Wired.** `AdministrativePacketCard` on the resident Documents tab. Shown as which of the nine sections are populated rather than as a rendered file, because every section already has a screen that owns it -- what nothing answered was "is anything missing before I hand this over", and an empty section is the finding a file review is looking for. Downloads the packet as it came back. Previously: Pre-existing on `main`. The current wrapper in a chain of renamed predecessors (`_base`, `_before_dietary`, `_before_calendar`); every rename carried the grant forward and nothing reads the packet it assembles |
| G12.4 | `grant_duty_eligibility_override` has no caller | S | done | **Wired.** `DutyEligibilityCard` on the employee record. This also gave `src/lib/dutyEligibility.ts` its first consumer: that module had modelled `overrideId` and an `overridable` flag per reason from the start and was imported by nothing, so the override is offered only where the library says it is a sensible answer -- "this person has no account" is not something to wave through, and the button does not appear. Previously: Pre-existing on `main`. Duty eligibility can be evaluated and refused, but the documented override that lets a supervisor accept the risk has no way in |
| G12.5 | `is_employee_access_active` has no caller | S | done | **Wired, and it corrected a badge that was lying.** The employee header read `profile_id != null` and announced "Portal access active", which is also true of a suspended employee and of a deactivated account; `is_employee_access_active` weighs employment status, the linked account and any suspension window together. `employee_access_suspensions` had no surface anywhere in the product. The `p_at` argument is why this is a function rather than a column -- once a suspension is lifted, nothing else can answer "did they have access the day of the incident", so `EmployeeAccessCard` asks it directly. Previously: Pre-existing on `main`. Reads as a helper predicate, but no other function body, policy, trigger or view references it either -- dead rather than internal |
| G12.6 | `revoke_offline_service_device` has no caller | S | done | **Wired.** `OfflineServiceDeviceCard` in Floor mode. Revoking is refused while unsynced drafts remain, and that refusal is the point rather than a nicety: the wipe is local and permanent, those drafts are care documentation that never reached the server, and nothing on the server can restore them. Previously: Pre-existing on `main`. Offline service devices can be registered but not revoked: the same one-way-door shape closed for survey-packet guest grants in G9 |
| G13 | The Codex review: twelve findings, twelve real | L | done | **Three P1s broke features this branch itself added.** Starting a certification attempt failed outright: the hook sent `p_observed_at: null` where the parameter has `default now()`, and a PostgreSQL default applies only when the argument is *omitted* -- an explicit null passes null into a NOT NULL column, so every start raised 23502. Proven against live PG rather than argued, and a sweep of all 278 functions with defaulted parameters confirmed it was the only instance (the other 471 defaulted parameters are `NULL::type`, where passing null changes nothing). A hospital return with new orders could never be closed -- the dialog writes only `pending_review`, the reconciliation refuses to close without `acknowledged`, and nothing wrote it; `20260804190000` adds `acknowledge_hospital_return_new_order` plus ack_at/_by/_note, so the record can say who accepted the orders and what changed. Annual medical evaluations were invisible to two edge functions: `generate-state-form-prefill` had no annual template key, so the State Forms Center answered "prefill only available" about a form sitting directly above it, and compliance-copilot still filtered a single item type, hiding every expired annual re-evaluation from the intent that exists to find them. **The nine P2s:** the funnel dropdown offered `admitted`, which the RPC refuses by design and `SETTABLE_STAGES` exists to exclude; Evaluate was offered on completed learning-path assignments, where an empty outcome would bump `state_version` and rewrite steps under a `completed` row; a closed appointment outcome with new orders still opened a follow-up nothing could close; a DME transfer parked the item in a `transferred` status, dropping it off the inspection schedule while it was still in service with a new resident; a completed-class correction invalidated a query key no hook uses; the archive Plan button was live while the row count was still loading, which is exactly how the empty batch it guards against gets made; the governed revision panel validated current course rows rather than the frozen snapshot the reviewer approves, wrong in both directions; and a checklist item recorded `not_applicable` -- which the record path accepts without evidence or signature -- could never be submitted or approved, because both completeness gates ignored `result`. That last one is covered by a new pgTAP suite that **fails against the pre-fix gate** ("These checklist items are not complete: insulin_draw") and passes against the fixed one. **The earlier review, for contrast:** two comments claiming Supabase throws plain objects so toasts would be blank. It does not here -- postgrest-js 2.110.1 declares `PostgrestError extends Error` -- but checking it found a narrower real path, since an RPC is a POST and POST is not retryable in postgrest-js, so a fetch failure rethrows the raw rejected value and bypasses the hook's `new Error(...)` wrap entirely. 8 non-defensive handlers, 6 of them pre-existing, now route through a shared `errorText` |
| G14 | The official form is cached, so CI stops asserting that pa.gov is up | M | done | **A test named for a PDF was really testing a government website.** `fetchDhsTemplate` did one unconditional `fetch` of `www.pa.gov` with a 15s abort, no cache and no retry, on the critical path of three edge functions -- so filling a reportable-incident form depended on DHS answering at that instant, and the E2E assertion "a reportable incident produces the official state-form PDF" failed twice on a slow pa.gov, with Playwright's single retry hitting the same wall. A suite that reports a product failure when a third party is slow is reporting the wrong thing. Now a read-through cache in a private `regulatory-templates` bucket, keyed by a hash of the source URL: DHS versions these documents in the URL itself (`...Effective-October-1-2016.pdf`), so a new form is a new key and a cached object never goes stale for the URL that produced it -- and there is deliberately no TTL, because a TTL would reintroduce the dependency on a schedule nobody chose. A miss retries three times with backoff and stores what it got; a form that genuinely cannot be downloaded still fails, because silently substituting another document for a regulated form would be worse. 8 Deno tests pin it, including that a cache hit does not touch the network at all, and the E2E `beforeAll` seeds the cache so the test measures what it is named after. **One thing this turned up:** `tenant_isolation_invariants` asserts every storage bucket is named by at least one object policy, and the new bucket needs none -- the edge functions use the service role. **Corrected after the fact:** the first attempt satisfied that invariant by granting platform admins a read nothing needed -- a capability invented to quiet a check. The invariant was corrected instead: every bucket is either policied or declared service-role-only in an explicit list with its reason, plus a second assertion that fails a stale declaration. Verified by breaking both |
| G15 | The gate read SQL a line at a time, and that hid twenty more | L | done | **Third correction to this script, and the largest.** A grant naming several functions puts one per line: only the first line carries `grant execute`, so every function after it read as a bare call and was excused from the check entirely. SQL is a statement language and this was reading it as lines. The check now drops whole grant/revoke/comment/drop/alter statements -- which can never contain a call -- while keeping `create function` BODIES, where most real calls live, and dropping only the signature so a function is not read as calling itself. Self-tests grew 14 -> 18, covering a multi-function grant, a multi-function revoke, a call inside a body surviving the header strip, and a function not counting as its own caller. **Twenty more dormant functions, every one verified against the live catalog rather than against this script's own parsing** -- word-boundary search over `pg_proc` bodies, RLS policies, views and triggers, plus client and edge source. The count has now been wrong three times, each time because the script read SQL badly, and each fix raised it: no `drop function` handling, then comments counted as callers, then this. "Zero" was never true. Twenty-one is the number. They are allowlisted with specific reasons and the rows below rather than wired here, because twenty capabilities is its own body of work and burying it in a 124-file pull request would hide it |
| G15.1 | `assign_organization_release_cohort` has no caller | S | done | **Wired with G12.1.** Its dormancy is the clearest example of what the line-by-line gate was hiding: it kept its grant through the pilot-console removal, lost its caller, and sat on a continuation line of a multi-function grant where nothing could see it. Previously: G12.1's counterpart, and dormant for the same reason: the pilot cohort console that called both was deleted on purpose by 20260802030000. Neither side has a caller; the mechanism is intact and unreachable. |
| G15.2 | `revoke_integration_api_credential` has no caller | S | done | **Wired.** `IntegrationRegisterCard` on the enterprise control plane, nested inside the provisioning card so both read the same organization field: issue above, take back below. Previously: An issued integration API credential cannot be revoked from anywhere. The same one-way door closed for survey-packet guest grants in G9, on a credential that authenticates machine access. |
| G15.3 | `rotate_integration_api_credential` has no caller | S | done | **Wired.** Rotation returns a plaintext key the server never stores again, so it is shown once and copied to the clipboard. Rotating needs no written reason -- rotating on a schedule is good practice and should not require an excuse -- while revoking does, because revoking answers an incident. Previously: Nor rotated, so a credential suspected of leaking can only be left in place. |
| G15.4 | `rotate_integration_webhook_secret` has no caller | S | done | **Wired.** Same one-shot secret handling, with the warning that the consumer needs the new secret before the old one stops being accepted or deliveries start failing signature checks. Previously: The signing secret a webhook consumer verifies against cannot be rotated. |
| G15.5 | `deactivate_integration_webhook_endpoint` has no caller | S | done | **Wired.** An endpoint can be switched off with a reason, and the register shows consecutive failures so a dead destination is visible before somebody goes looking. Previously: A webhook endpoint can be created but never switched off, so deliveries continue to a URL nobody controls any more. |
| G15.6 | `rotate_scim_connection_credential` has no caller | S | done | **Wired.** `ScimRegistryCard` beside the create card. The registry shows a `credential_hint` rather than the credential, so the rotation panel says at the moment it matters that the secret is readable once. Previously: The SCIM provisioning credential cannot be rotated, which is the standing remediation when a directory integration is compromised. |
| G15.7 | `get_scim_connection_registry` has no caller | S | done | **Wired.** Nothing could list a SCIM connection, which is why rotation had nowhere to be offered from. Previously: Takes no arguments and returns the SCIM connection registry. Nothing lists SCIM connections at all, so the rotation above has no surface to be offered from either. |
| G15.8 | `link_sso_identity_subject` has no caller | S | done | **Wired.** For an identity the provider sends that matches no account -- usually because somebody's subject identifier changed -- which without this means the person cannot sign in and nothing in the product can attach them. Previously: Binds an SSO provider subject to a profile with a stated link method. Without it an SSO identity that fails to auto-match cannot be attached by an administrator. |
| G15.9 | `grant_identity_break_glass` has no caller | S | done | **Wired.** `BreakGlassCard` on Security Governance, above Sensitive Activity because break-glass is what produces the activity below it. Requires a ticket reference and a reason, and caps the grant at 24 hours -- longer than that is not an emergency, it is a role. Previously: Emergency elevated access with a requester, a ticket reference, a reason and a mandatory expiry -- carefully built, and impossible to invoke. |
| G15.10 | `revoke_identity_break_glass` has no caller | S | done | **Wired, and this is the half that mattered.** A grant that cannot be revoked early runs to its expiry whatever the investigation finds. Previously: And impossible to end early, which is the half that matters most. |
| G15.11 | `record_training_attendance` has no caller | S | done | **Wired.** `SessionRosterCard` on the class page. The signature is a hash of a name the attendee types, following the convention `signatureDigest` already set for certification attempts: derived from something a person entered rather than a random token, with identity coming from the session the server already checked. Previously: Records attendance for a class registration with check-in/out times and evidence. Distinct from the class-attendance path already wired in G6; this one is the registration-level record. |
| G15.12 | `approve_training_session_completion` has no caller | S | done | **Wired together with G15.11, because the server ties them together:** approval refuses unless every registration marked `attended` carries signed evidence, and recording that evidence is the only thing that produces it. Wiring either alone would have shipped a button that always failed. The card also disables approval outside `scheduled`/`in_progress`, which the server refuses outright. Previously: Approves a training session's completion with a reason. Nothing offers the approval, so the state it grants cannot be reached. |
| G15.13 | `record_resident_service_task` is a superseded duplicate write path — tracked as SG-4 | S | done | **Folded into SG-4, which is now the only live row for this RPC.** Checked four times from this table, and the fourth check retracted the third: the reason recorded here for not making this a delegating shim — that `completed_by_other` has no equivalent response in the successor, so a shim would lose the fact — was false. `lib/serviceDeliveryContract.ts`'s `completionResponseForServiceOutcome` maps it onto `completed_as_planned` and `useResidentServiceTasks` preserves it in `exception_details` as `legacy_status` plus a boolean, so the mapping a shim needs already exists and is tested. What survives is narrower — the `acceptable_completion_responses` gate a delegating shim would inherit, plus the alerting and validation divergences — and all of it is a behavioural change for callers outside this repository, which is what makes it a standing gap with an owner and a review date rather than a permanently `open` row in a sweep table. `main` opened SG-4 for the same RPC via #452 while this row was in review, reaching the same conclusion — do not write the shim — through the premise this row retracted; SG-4 now carries both, with the premise corrected there and on the function comment (`20260805030000`). Nothing about the RPC changed: still granted, still superseded, still not to be dropped. Previously: a second full implementation writing the same rows on the older status/note/supervisor_notified model, so wiring it would have been actively wrong. |
| G15.14 | `retract_clinical_progress_note` has no caller | S | done | **Wired.** Sign and amend were both already on the note row; this third one was not, so a note charted against the wrong resident -- the mistake it exists for -- simply stood. It is not a delete: the server writes the current body into `clinical_progress_note_versions` and sets the note to `entered_in_error`, which is how a clinical record is corrected without losing what it used to say. The dialog says that, and says to amend instead when the point is to change what a note says rather than to withdraw it. Previously: A clinical progress note cannot be retracted. A note entered on the wrong resident stands, because retraction is the audited path and it has no caller. |
| G15.15 | `save_report_schedule` is a superseded shim, not a gap | S | done | **Checked before building, and the finding was real but my diagnosis was wrong.** I recorded this as "a report can be built and run but never scheduled". Scheduling works: the Value Center calls `save_report_schedule_configuration`, which `20260717021844` introduced as a strict superset -- delivery hour and minute, day of week, day of month, and a schedule id so an existing schedule can be edited -- and the same migration re-declared this older signature as a wrapper delegating with defaults of 07:00, Monday, the 1st. So it is dormant by design, and wiring it would have been the wrong answer: the richer function is the one a person should reach. Kept rather than dropped, because it is granted to `authenticated` and an external integration could still call the older signature. The allowlist reason is corrected to say this |
| G15.16 | `set_hris_import_row_decision` has no caller | S | done | **Wired, and it was the missing middle of a flow this branch had already half-fixed.** The Validate card's own description says validation "surfaces duplicate candidates for a human decision" -- and there was nowhere to make one, so a run could be validated and then applied with nothing decided in between, and `apply_hris_import_batch` had nothing to apply. `HrisRowDecisions` sits literally between the two commands. Only rows the server will accept a decision for get a control: it refuses anything whose `validation_status` is not `valid`, and refuses a `link` whose employee is not among that row's own candidates, so the link option is disabled when no candidates were found and the picker offers only those candidates. Offering an action that can only fail is the mistake the funnel dropdown made in G13. Previously: Accepts or rejects one row of an HRIS import against an employee. The import runs are creatable (G10) but individual row decisions cannot be recorded. |
| G15.17 | `get_regulatory_rule_snapshot` has no caller | S | done | **Wired.** "What did this rule say on the day this happened" is the question every retrospective compliance argument turns on, and no column can answer it once a rule is superseded. A date with no version in effect is shown as an answer rather than an error. Previously: Returns a rule as it stood on a date. Nothing asks, so 'what did the rule say when this happened' has no answer in the product. |
| G15.18 | `record_regulatory_shadow_run` is a machine entry point | S | done | **Not a gap.** It takes a rule version, a baseline version, a cohort window, an evaluated count and an engine version -- output from a rule engine running a candidate alongside the current one, not anything a person types. The differences it produces ARE now actionable (G15.19). What is missing is the engine that calls it, which does not exist in this repository; giving it a form would be inventing a user for it. Previously: Records a shadow evaluation of a candidate rule version against a cohort -- the mechanism for testing a regulation change before it binds. |
| G15.19 | `reconcile_regulatory_shadow_difference` has no caller | S | done | **Wired.** The resolution vocabulary is read out of the CHECK constraint rather than invented -- a first guess was three plausible words the server rejects. It is `expected_change` / `baseline_defect` / `candidate_defect`, with a rationale of at least ten characters and a 64-hex evidence checksum, both also constraints. Previously: Resolves one difference a shadow run found, with a rationale and an evidence checksum. No shadow run can be recorded, so no difference can exist to reconcile. |
| G15.20 | `record_regulatory_fixture_result` is a machine entry point | S | done | **Not a gap.** One golden-fixture result for a given engine version with a request id: conformance-harness output. Same as G15.18 -- the missing piece is the harness, not a button. Previously: Records a regulatory engine fixture result for a given engine version. The conformance harness has no entry point. |
| G16 | The dormant-RPC gate cannot see an unreachable caller | L | done | **A new class, and the branch that added the dormant-RPC gate proved it by introducing one.** That gate asks whether a database function has a caller. Satisfying it turns out not to mean the capability is reachable: a hook that calls the RPC, that no component renders, is a capability with no way in -- and the gate passes, because the RPC does have a caller. That caller is simply unreachable itself. `useAddAppointmentPreparationItem` was written on this branch, its screen was not, and the gate reported zero; it now has an inline form on the appointments tab. `scripts/check-unrendered-hooks.mjs` closes the class: an exported `use*` hook must be referenced by a `.tsx`, directly or through the *body* of another hook that is itself reachable, computed as a closure because a chain of hooks is only as reachable as the component at the end of it. **Thirty-five hooks had no renderer.** Fifteen were not debt and are deleted -- three unadopted helpers with no caller anywhere (`useIsMobile`, `useServerListState`, `useBulkAction`), nine superseded by a rendered sibling doing the same job better, and three that were never unreachable at all, where the gate itself was reading badly: one declared beside the component using it, and two vouched for by nothing but a comment that had outlived the call it described. Self-tests cover both, since blanking comments and string literals is what makes body-scoped reachability trustworthy. **Thirty-three hooks had no renderer, and all thirty-three are resolved: the allowlist is empty.** Sixteen were not debt and are deleted -- three unadopted helpers with no caller anywhere, and thirteen whose job a rendered sibling already did or whose stated purpose was served another way. Seventeen were real gaps, every one predating this branch, and every one now has a surface. Four of those started life recorded here as gaps and turned out not to be, once the successor was actually read (G16.1, G16.5, G16.18, G16.19) -- the same lesson as G15.13 and G15.15, learned four more times. The gate found its own fourth reading bug by being used (G16.23), which in turn found `queue_clinical_observation_writeback` (G16.24), a whole FHIR write-back pipeline whose queue nothing could fill |
| G16.1 | `useIssueCertificate` is a superseded compatibility endpoint | S | done | **Not a gap, and the migration that superseded it says so outright.** `20260711154819_atomic_course_completion_certificates.sql` moved issuance inside `complete_course_assignment()` because the two-call browser flow left a valid completion with no certificate whenever a request failed between the calls, and records that `issue_certificate` "remains as an idempotent compatibility endpoint". Wiring it would mint a certificate with no completion behind it -- the exact invariant that migration exists to hold. The hook is deleted; the function stays granted for callers outside this repository and is recorded in the dormant-RPC allowlist. **Deleting the hook is what exposed G16.23**, because it left the function with no caller except a string |
| G16.2 | `useCancelTimeOffRequest` has no renderer | S | done | **Wired.** On My Schedule's own time-off card, beside the swap withdrawal G7 added -- both RPCs are scoped to the requester (`employees.profile_id = auth.uid()`), refuse anything not `pending`, and demand a reason of at least five characters, so the control appears only on a pending request and enforces the same minimum. Previously: A submitted time-off request could be approved or denied by a supervisor, but the person who filed it could not withdraw it |
| G16.3 | `useAcknowledgeSupportPlan` has no renderer | S | done | **Wired.** "I have read this plan" on the active version, in the support-plan section of the resident record. Deliberately not behind `canManage`: the server accepts an acknowledgement from anyone whose scope includes the plan, because the record it produces is *this caregiver has read the plan they are working to*, and a manager-only control would capture the wrong people. The server's `on conflict do nothing` treats a double-tap as a double-tap, so once recorded the button becomes "You have read this version" rather than firing again. Previously: the acknowledgement that someone has actually seen the plan could not be recorded, which is the part an inspector asks for |
| G16.4 | `useSupportPlanAcknowledgments` has no renderer | S | done | **Wired with G16.3, because either half alone is worth nothing.** The expanded active version now lists who has read it and when, with any note. Acknowledgements are keyed by plan rather than by resident, so a new active version leaves everyone unacknowledged again -- which is the point of keeping them, and is visible here rather than implied. Previously: nothing wrote an acknowledgement and nothing displayed one, so the table was unreachable in both directions |
| G16.5 | `useLogResidentChangeOfCondition` is a superseded predecessor | S | done | **Not a gap, and my own row said otherwise until I read the successor.** I recorded this as "nothing can put a resident into the change-of-condition queue". Logging a change works: `LogChangeOfConditionDialog` calls `create_resident_change_event` from ResidentDetail and the State Forms Center, and that function writes the identical `significant_change_reassessment` item -- same due date, same warning and grace days -- plus the citation topic, the change event itself, its follow-up and its history, and optionally an incident. Wiring the thin one would have been actively wrong: a reassessment item with no change-of-condition record behind it, invisible in ChangeOfConditionQueue, which reads the events. The hook is deleted; the function stays, because `convert_shift_report_entry` calls it server-side to turn a triaged handoff entry into the same item. Third time checking before building changed the answer (G15.13, G15.15) |
| G16.6 | `usePinSurveyDayBinder` has no renderer | S | done | **Wired, and reading the trigger changed what it is for.** Pinning is normally automatic: activation pins the latest completed single-facility binder, and `autopin_survey_day_binder` re-pins any binder that completes during an active session. That last part is exactly why a manual pin matters -- a regeneration started by anyone silently replaces the binder a surveyor is being walked through, and there was no way back to the earlier one. The control offers only what the function will accept: succeeded exports whose `facility_ids` is exactly this facility, minus the one already pinned |
| G16.7 | `useRegisterForTrainingSession` has no renderer | S | done | **Wired, and this is the hook that named the class.** G15.11/G15.12 put signed attendance and completion approval onto a roster that nothing could populate. Registration now sits at the top of the same card, picking from the active employees the attendee dialog already offers, minus anyone already registered. Two details come from the function rather than from guessing: capacity counts only `registered` and `attended`, so a withdrawal or a waitlist entry does not hold a seat and the seats-taken figure is counted the same way; and over capacity the server silently waitlists rather than refusing, so the toast reports the receipt it returns -- "Waitlisted at #3" -- instead of claiming a registration. The control is disabled outside `scheduled`/`in_progress`, which the function refuses outright |
| G16.8 | `useSetIncidentQapiConsideration` has no renderer | S | done | **Wired, and this is the most legible form the class takes.** The close-loop checklist has always listed "QAPI consideration" as a stage that blocks closure, and `stageAction` returned nothing for it -- the checklist named the step, marked it outstanding, and offered no button, so an incident that needed the decision could never leave the stage. The dialog uses the server's own vocabulary: `linked` demands a project in this incident's facility, so the picker is scoped to that facility and the confirm stays disabled until one is chosen; `not_indicated` takes a note the server appends to the investigation findings, because a decision that quality improvement is not indicated is worth nothing without the reasoning |
| G16.9 | `useComplaintTrends` has no renderer | S | done | **Wired.** The Complaints tiles measure the present -- how many are open now, how many are high risk now. What they cannot answer is whether the same complaint keeps coming back, which is what `get_complaint_trends` counts: totals, a category breakdown, and both escalation paths (ombudsman referral, linked incident) over a window. It takes one facility and refuses anything outside scope, so the card appears with a specific facility selected rather than offering a control that would always fail on "All facilities" |
| G16.10 | `useResidentServiceUtilization` has no renderer | S | done | **Wired onto the support-plan tab, which is the point of it.** The plan says what care should be; `get_resident_service_utilization` says what it actually was -- unscheduled services, documented assistance and exceptions over 30, 60 or 90 days. A resident repeatedly receiving a service the plan does not list, or repeatedly refusing one it does, is the evidence the plan needs revising, and a plan that no longer matches the care given is what a survey finds. Putting the two on the same tab is the whole value |
| G16.11 | `useFeatureReleaseActive` has no renderer | S | done | **Wired on the release-flags page itself, deliberately not as a gate on some screen.** Cohort membership became administrable in G12.1/G15.1 and nothing read the result, so being in a cohort changed nothing anyone could see. Using it to gate an arbitrary feature would have invented a product decision nobody asked for; what the page was actually missing is confirmation that a change took effect. Each flag row now shows what the whole rollout/cohort/kill-switch evaluation returns for the person looking at it, so flipping a kill switch and watching the evaluated answer turn Off is the difference between having set a flag and having killed a feature. The read fails closed |
| G16.12 | `useUpdateViolation` has no renderer | S | done | **Wired as a partial gap, which is what reading the page first showed.** `status` is deliberately *not* editable here: it moves only through `PocLifecycleActions`' SECURITY DEFINER RPCs, which version the plan of correction and record who verified it, so a raw table update would step around all of that. What genuinely had no path is the citation as recorded -- reference, topic, severity, POC due date, surveyor, description -- so "Correct details" edits exactly those and says why the status is absent. Previously: a violation could be created, listed, opened and have a POC generated from it, and never corrected, so a typo in a citation stood |
| G16.13 | `useUpdateSchedule` has no renderer | S | done | **Wired, draft-only.** Title and period are editable while the schedule is a draft and not after: the period bounds the assignment grid, so moving it under a published schedule would silently orphan shifts employees have already been shown. The form refuses a period that ends before it starts, which the grid's own date enumeration would otherwise turn into an empty schedule |
| G16.14 | `useDeleteSchedule` has no renderer | S | done | **Wired with G16.13**, same draft-only rule and a confirmation that counts what goes with it -- `shift_assignments.schedule_id` is `on delete cascade`, so the dialog names the number of assignments being removed rather than implying the schedule is empty. Previously: a schedule created by mistake could not be removed |
| G16.15 | `useUpdateCourseVersion` has no renderer | S | done | **Wired, drafts only.** A published version is the record of what people were actually taught; editing it would rewrite history rather than correct a draft, so the control appears beside Publish and disappears with it. Previously: a version could be created, cloned, published and unpublished and its own title and description could not be corrected, so fixing a typo meant cloning the version. `useCompetencies.ts` names this hook in a comment as the pattern to follow, which is exactly how it looked reachable until the gate stopped counting comments as callers |
| G16.16 | `useUpdateEmployeeSchedulePreference` has no renderer | S | done | **Wired.** A typical-pattern row can now be amended in place instead of deleted and rebuilt. This one matters more than it looks: schedule auto-fill reads these patterns, so a stale one is not untidy, it quietly fills the wrong cells |
| G16.17 | `useListAdmissionActivities` has no renderer | S | done | **Wired directly under the form that writes them.** Every call, tour and follow-up was recorded by a rendered control and displayed nowhere, so the trail an admissions conversation depends on -- what was already tried, and how it went -- existed only in the database |
| G16.18 | `useListAllIncidentNotifications` is a dead whole-table read | S | done | **Not a gap; deleted.** It existed for deep-link resolution, and deep links carry the parent id directly -- `/app/incidents/<incident_id>` -- so there was never anything to resolve. An unbounded, unfiltered read of every visible notification row is also not something to leave lying around for someone to adopt |
| G16.19 | `useListAllIncidentNotificationsDetailed` is a dead whole-table read | S | done | **Not a gap; deleted.** Its own comment said Reports.tsx's incident-notification register used it. That register is produced entirely server-side by `generate_paged_compliance_report`, and Reports.tsx does not mention notifications at all. The comment described an intention, and the gate believed it until it stopped counting comments as callers |
| G16.20 | `useListAllInspectionEvents` is a dead whole-table read | S | done | **Not a gap; deleted.** Same deep-link resolution as G16.18, gone for the same reason. `useListViolationsBySourceInspectionEvents` is the join that is actually rendered, from the violation side |
| G16.21 | `useListCampaignQuestions` has no renderer | S | done | **Wired into the campaign's own expanded panel.** A campaign is created atomically with its questions by `create_policy_campaign_with_questions`, and after that nothing could show an administrator what it asks -- a campaign already in flight was a black box to the person who owns it. This is the authoring view, so it shows the answer key, behind the same policy-administrator boundary the questions were authored behind. The employee-side read (`usePolicyKnowledgeCheck`, deliberately without the key) was rendered all along |
| G16.23 | The dormant-RPC gate counted a quoted string as a caller | M | done | **Fourth time this script has read SQL wrongly, and this one was found by using it.** Deleting `useIssueCertificate` (G16.1) should have made `issue_certificate` dormant; the gate still passed. Its only apparent call site anywhere was its own error message -- `raise exception 'certificates are not directly writable by clients; use issue_certificate() / service role'` -- and the check counted that string as a call. `blankSqlStrings` now blanks single-quoted literals before the scan, preserving length so offsets hold and treating `''` as an escaped quote rather than a terminator; dollar-quoted bodies are untouched, because that is where real calls live. Then the fix bit immediately in the other direction: the comment explaining *why* the hook had been deleted named the RPC, and the client scan counted prose as a caller too, so `blankTsNonCode` blanks TypeScript comments -- comments only, deliberately not string literals, because in TypeScript every real call goes through `supabase.rpc("name")` and the name IS the literal. Getting that backwards would report the entire schema as dormant. Self-tests 18 -> 25, covering both directions in both languages |
| G16.24 | `queue_clinical_observation_writeback` has no caller | S | done | **Found by G16.23's fix, and it is the substantial one.** The `fhir-writeback` edge function drains a queue that nothing could fill: an observation could be charted and never sent to the resident's EHR, so the entire write-back pipeline had no entry point. It looked reached only because the edge function names it in a comment. Now "Send to EHR" on each observation in the clinical chart. The server refuses a retracted observation, and refuses unless the resident has an active mapping to a write-back-enabled FHIR source -- that refusal names which of the two is missing, which is better than anything guessable client-side, so it is surfaced verbatim rather than hidden behind a pre-emptive disable |
| G16.25 | Hook reachability stopped one layer short | M | done | **The gate had the same blind spot it was built to close, one layer out, and a 209-line panel proved it.** `ReadinessForecastPanel` renders a 30/60/90-day workforce forecast and is the only caller of `get_workforce_readiness_forecast` and `route_workforce_readiness_remediation` -- and nothing imported the panel. Both gates passed: the RPCs had hooks, the hooks had a `.tsx`, and the capability was three layers deep in nothing. "Referenced by some `.tsx`" was never the right rule; "referenced from a module the entry point can reach" is. Reachability is now computed from the real import graph, seeded at `main.tsx` and following static imports and `lazy(() => import(...))` alike, with specifiers read from the RAW source -- an import path IS a string literal, so the string-blanking that fixed G16.23 would have erased every edge, while comments are still stripped so a commented-out import cannot keep a dead file alive. A module nothing consumes is now reported in its own right, and deliberately seeded differently: modules count test files and the entry alike, because a pure helper exercised only by its own unit test is tested rather than dead (`lib/bulkActions.ts` is exactly that), while hooks are seeded at the entry alone, because a component only a test imports still has no way in for a user. Merging the two seeds would have reopened the hole. Self-tests 14 -> 20. The forecast panel now sits above the workforce tab's current numbers, where blockers that have not happened yet belong |
| G16.26 | The hook gate could not see the `const` declaration form | S | done | **Found by auditing the other gates for the failure Codex found in two of them, rather than waiting for a third review to find it.** `HOOK_DECLARATION` matched only `export function useX`. Twenty hooks are declared `export const useX = rpcMutation(...)` -- the whole of `useDietaryOperations` and `useResidentFinancialOperations` -- and none was ever examined. The check then reported the total it had read as though it were the total that exists: "908 exported hooks ... 0 awaiting a surface" was true of the 908 it could see and silent about the 20 it could not. **Every one of the twenty is reachable, so this closes no defect** -- worth stating plainly rather than implying the sweep found something, because the useful fact is that the blind spot was real and the space behind it was empty. The count is now 928 and means what it says. A `const` hook needs a different body boundary: its first `{` is usually a parameter's type literal, so brace-matching from there captures the annotation and stops, and a call in the real body would not propagate reachability. The other gates were audited for the same shape and have none -- `check-dormant-rpcs` and `check-rpc-call-signatures` match on word boundaries and `.rpc(`, which are receiver- and form-agnostic, and the route gate reads quoted literals whatever wraps them, so the SQL `concat('/app/residents/', ...)` form was already covered. Self-tests: 20 -> 27 |
| G17 | A link the server hands a user has to go somewhere | M | done | **The same class as G16, but the caller is the database.** The database and the edge functions build in-app links as string literals -- notification `link` columns, system-job `operator_route` values, and `search_workspace`, which branches on role to send a platform admin to `/admin/<kind>/<id>` instead of `/app/<kind>/<id>`. Nothing verified those paths exist, and nothing could have: the link is assembled in SQL and consumed as an opaque string, and `GlobalSearch` navigates `item.route` verbatim -- no `safePathForRole`, no fallback -- so there was nothing to soften a bad one. Three were broken. `check-server-route-links.mjs` compares every in-app path literal in a migration or edge function against the routes App.tsx declares, treating `:param` as a wildcard and a trailing `/` as the concatenation prefix it actually is. Two parser corrections were needed before the findings were trustworthy: a query string is a deep-link filter and not part of the route (seven notification links carry one), and edge-function TESTS hold fixture URLs that are deliberately not real routes. Self-tests: 13 |
| G17.1 | `search_workspace` sent platform admins to two routes that were never built | S | done | **Wired.** The RPC emits eleven `/admin/...` prefixes for `platform_admin`; nine had a route and `/admin/complaints` and `/admin/violations` did not, so searching a complaint number or a citation reference produced a result that landed on Not Found. The fix is the two missing route declarations, not a redirect: both `/app` equivalents are gated to `org_admin`/`facility_manager`/`auditor` and `platform_admin` is in neither set, so pointing them there would bounce. They reuse `ComplaintDetail` and `ViolationDetail` under `PLATFORM_ADMIN`, which is exactly what the nine working siblings already do, and they are registered in `APP_PAGES` so `routeRegistration.test.ts` holds them to the same parity as every other route |
| G17.2 | The integration webhook dispatch job's Open button went nowhere | S | done | **Fixed by migration.** System Jobs renders `operator_route` as an Open button, and this job's was `/admin/integrations`, which has never been a declared route -- so an operator investigating a stalled webhook dispatch landed on Not Found. It now points at `/admin/enterprise`, where the integration register, credential rotation and webhook endpoint controls actually live (G15.2-G15.5). The original literal stays in the 2026-07-11 migration because history is append-only, so it is recorded in the allowlist naming the migration that supersedes it -- which is what that file is for |
| G17.3 | The route check exempted exactly the links that interpolate | S | done | `serverLinkLiterals` skipped any literal containing `${...}`, on the reasoning that "the prefix is what matters". The effect was that an edge function building a link with a template literal -- the ordinary way a TypeScript function builds one -- was exempt from the check whose entire subject is server-emitted links, while the SQL concatenation form was checked. **The check was reading the case that had already been fixed and skipping the case that had not.** An interpolated value fills one path segment, which is exactly what a `:param` route accepts, so it now substitutes a stand-in segment and checks the result. That surfaced `compliance-copilot` emitting `/app/inspection-items/${row.id}` at two call sites against a route that does not exist; the rows come from `inspection_items` and `/app/inspections/:id` already renders `InspectionItemDetail`, so the link was wrong rather than the routes. One finding, no false positives across 660 server files. Self-tests: 16 |
| G18 | 131 RPC call sites had their type checking cast away | M | done | **Nothing was broken, nothing was protecting it, and the first reason recorded here for leaving it alone was wrong.** `rpc("name" as never, { ... } as never)` appears at 131 distinct call sites, and every one of those names is present in the generated `database.types.ts` -- the casts are not load-bearing, they are left over from code written before the types were regenerated. Inside `as never` a misspelt function or parameter is invisible to typecheck and to every unit test, and surfaces as a PostgREST 404 the first time a real user presses the button. **Removing them wholesale is still the wrong move, but not for the reason first given.** That reason was "155 of the 171 surfaced errors are an explicit `null` against a parameter whose default is also null, so they are semantically identical to omitting" -- generalised from a single sample. Measured properly, the 190 explicit-null arguments split **116 against `default null`** and **74 against parameters with no default at all**, and zero against a non-null default. The 74 break that argument: passing null to a required parameter is not "identical to omitting", since omitting it would be an error -- and they cannot be fixed by editing the call, because **the Supabase type generator does not model parameter nullability**. `create_complaint` declares `p_complainant_name text`, legally nullable (an anonymous complaint has no name), and the generator types it `p_complainant_name: string`. So removing the casts leaves 74 *correct* calls that cannot typecheck at all. That is the real reason to keep them, and it is a limitation of the generator rather than a cost-benefit judgement. `check-rpc-call-signatures.mjs` takes the value without the churn: it reads the migrations for what each function declares and holds every call site to it, cast or not -- the function exists in `public` after every create/drop in filename order; every `p_*` argument is one it declares; no argument is an explicit null where the default is not null; and no argument is an explicit `undefined` where the parameter has no default. Clean on all four across 809 functions and 804 source files, each proved non-vacuous by injecting a fault. Self-tests: 21 |
| G18.1 | The non-null-default rule is the P1 from this branch's own review, made permanent | S | done | PostgreSQL applies a default only when the argument is OMITTED; passing `null` passes null. `start_certification_attempt(p_observed_at timestamptz default now())` was called with an explicit null against a NOT NULL column, so starting a certification attempt failed outright -- a P1 in review, fixed earlier on this branch. The sweep confirms it was the only instance across the 143 functions that have a non-null-default parameter, and the rule now holds that line. Explicit null against `default null` is deliberately **not** flagged: it is identical to omitting the argument, and flagging it is what turned the wholesale cast removal into 155 pointless edits |
| G18.2 | The fourth rule: an explicit `undefined` is an omission, not a value | S | done | supabase-js drops undefined keys from the request body, and PostgREST resolves an RPC by the set of keys it receives -- so an explicit `undefined` against a parameter with no default means the function cannot be resolved at all (PGRST202), not that it runs with a null. Zero instances today across the 570 functions that have a required parameter; the rule holds that line. **What it deliberately does not claim to catch:** a value merely *typed* as possibly-undefined -- `payload.first_name` off a `Partial<>` -- reads as an ordinary expression to a text scanner. Removing the casts surfaces thirteen of those through typecheck, and every one checked is guarded (`enabled:` on the query, or callers that always pass the field), so they are latent rather than live. Catching them needs type information this check does not have, and the gate's comment says so rather than implying coverage it lacks |
| G19 | Sixteen cache invalidations refreshed nothing | M | done | **A mutation succeeds, the toast says it worked, and the list the user is looking at keeps showing the old value.** TanStack Query matches an invalidation by comparing key ELEMENTS, so `["training_class"]` does not match `["training_classes", id]` and `["daily-operations"]` does not match `["daily-operations-command-center", id]` -- string prefixes are not key prefixes. Nothing catches this: it is not a type error, the key is a perfectly good string, and a test that mocks the client sees a call that happened. Seventeen were dead -- sixteen found by the first version of this check, and a seventeenth it could not see, recorded in G19.2. **Six were one keystroke from correct and are now fixed** -- `revoke_class_checkin_tokens` (its ONLY invalidation, so revoking a check-in code left the class page showing it as live), `submit_credential_renewal` -> `employee_credentials`, `determine_incident_reportability` -> `incident_notifications`, and three `daily-operations` -> `daily-operations-command-center`. **Eleven named a concept nothing queries** and are deleted; each sits beside invalidations that do cover the surface, which is why nobody noticed. `check-query-invalidations.mjs` holds the line. Two parser corrections were needed first, and both mattered: key constants must resolve MODULE-FIRST, because several hook modules each declare their own `const QUERY_KEY` with a different root and a flat map silently resolves one module's constant to another's key; and query roots must be collected from sources with invalidations stripped, so an invalidation cannot vouch for itself. Self-tests: 9 |
| G19.2 | The seventeenth was in the check's own header comment, and the check could not see it | S | done | `check-query-invalidations.mjs` opened by listing four bugs "real when this was written". Three were fixed. The fourth -- offline unscheduled-service sync invalidating `["daily-operations"]` -- was still in the tree, and the gate reported a clean pass over it every run, because `INVALIDATE_PATTERN` only matched `queryKey: [` and this key was held in a variable: a per-kind record of key arrays walked in a loop, so no root ever appeared at a `queryKey:` site to be read. **A check that only reads the shape it was written against reports the absence of that shape as the absence of the bug.** `keyRoots` now returns how many keys it could not follow, and a file with an unfollowable invalidation has its array-of-arrays key tables read as the invalidation list -- which is what such a table is for. Caught the surviving bug on the first run, no allowlist entries, no false positives across 265 query roots. Self-tests: 16 |
| G19.3 | Five more invalidations that named nothing, or nothing enough | M | done | Not wrong keys -- missing ones, which the gate cannot see by construction: it asks whether an invalidation matches a query, never whether a mutation invalidated enough. `approve_training_session_completion` inserts a `training_class_attendees` row per attended registration and refreshed neither the roster nor the counts, though every other attendee mutation refreshes both and it writes more rows than any of them. Certification attempts left `["duty-eligibility"]` stale directly beneath a comment reading "a passed attempt grants an employee_qualifications row, which is what duty eligibility reads" -- the comment describes the line that was never written. Citation governance refreshed `["dhs-citation-topics"]` while Violations, ViolationDetail, InspectionReadiness and the assessment surfaces read `["dhs_citation_topics"]`: two hooks over one table under two roots, so verifying a citation updated the governance admin page and left every screen a surveyor looks at showing the old reference. `validate_hris_import_run` left `["hris-import-rows"]` stale, hiding the merge decisions it had just produced from the list rendered directly below its own button. Issuing a survey-packet guest grant did not refresh the list of who holds access, though revoking one did |
| G20 | Break-glass could not be granted at all, because the form was both parties | S | done | **The one path that has to work at 3am failed on its own precondition.** `grant_identity_break_glass` raises 'break-glass requests require a separate approver' when `p_requested_by = auth.uid()`; `BreakGlassCard` filled the requester in with the signed-in admin, so every grant submitted through the UI was rejected. The card was written on this branch specifically because emergency access could not be granted -- and shipped still unable to grant it. **Nothing on the branch could have caught this**: the hook takes `requestedBy` as its own argument and passes it through correctly, the call typechecks, the RPC exists and is granted, and `check-rpc-call-signatures` confirms every parameter is declared. The defect is entirely in which value the card chose, which is a semantic constraint stated in the function body and nowhere in its signature. Requester is now its own field, validated client-side so the refusal arrives while it can still be corrected rather than as a failed submission |
| G20.1 | A card that reported "active" for people with no account | S | done | `EmployeeAccessCard` mounted for every employee and called `is_employee_access_active`, whose `(e.profile_id is null or p.is_active)` clause returns true for an employee with no linked profile. That clause is right for what the function weighs -- a missing profile row is not a suspension -- and wrong as an answer to "can this person sign in", which is what the card's own description promises. The page header two hundred lines above already said "No portal access" for exactly these employees, so the page contradicted itself down the same column. Fixed in the card rather than the function: `is_employee_access_active` is shared, and changing what it returns to suit one screen's phrasing is a behaviour change to everything that reads it |
| G20.2 | A minimum the server did not agree with, and a correction that could not reach the content | S | done | `ReleaseCohortMembershipCard` enforced one `MIN_REASON = 5` across both its actions, but only unassign has a length check and it requires 8 -- so a 5-to-7 character removal reason passed the form and was refused by the server. The two constants are now named for the two rules, with the asymmetry recorded, because it is the SQL that is inconsistent and the client cannot fix that by averaging. Separately, `EmergencyBlockCorrection` could edit only the title, while `admin_emergency_update_course_block` accepts `p_body`, `p_video_url` and `p_document_id`: the wrong dose in the component's own doc comment lives in `body.content`, a field the form could not reach, so the surface could not perform the example it was written for. The body is merged rather than replaced -- `heygen` job state sits beside `content` in the same jsonb and a bare `{ content }` would drop it |
| G20.3 | The third instance of the break-glass class, and the comment that hid it | S | done | **Swept every site where an RPC compares a parameter to `auth.uid()` -- there are three, in two functions.** `grant_duty_eligibility_override` raises 'An override cannot be granted to yourself' when `auth.uid() = p_profile_id`, and `DutyEligibilityCard`'s `canOverride` checked the role half of that constraint and not the self half, so an org admin opening their own employee record was offered a form that could only fail. Much milder than G20 -- one record rather than every submission -- and recorded because of *why* it survived: the prop's own comment already asserted both halves ("The server refuses anyone but an org admin (and refuses self-grants). This only keeps the form from offering what the server would reject"), so anyone reading the call site saw a claim that the check was complete. **A comment describing the intended behaviour is the easiest possible place for a missing half to hide**, which is the same thing that happened in G19.3's certification invalidation, where the comment named the line that was never written |
| G21 | A parameter no caller passes is a capability with no way in | M | done | **The `EmergencyBlockCorrection` finding generalises, and the gate to find the rest was already built.** `check-rpc-call-signatures` parses what each function declares and what every call site sends, so "which declared parameters does no caller ever pass" is a join over data already in hand. **27 called functions have one.** Most are genuinely optional refinements -- an idempotency key, a filter, a link to a related row -- and are not debt. Two are a surface that cannot reach something the record depends on (G21.1, G21.2). The rest are surveyed rather than swept: an unpassed parameter is evidence of a gap, not proof of one, and turning all 27 into work would confuse the two. Deliberately not made a gate for that reason -- it would need an allowlist entry for every legitimately-optional parameter, which is most of them |
| G21.1 | A retracted vital sign left no trace of having existed | M | done | **The chart offers the retraction and then cannot show its result.** `ResidentClinicalChart` has a Retract button that toasts "Observation retracted (entered in error)", and `get_resident_clinical_observations` filters `entered_in_error` rows out unless `p_include_retracted` is passed -- which nothing passed. So the observation, and the `clinical_observation_amendments` row recording who withdrew it and why, became unreachable the moment the button was pressed. "Was this blood pressure ever recorded, and who took it back" is what a clinical record is for, and this is the same shape as G12.5's `employee_access_suspensions` gap -- a state change that leaves no answerable trace on any screen. Retracted rows stay excluded by default, because the chart is what care is delivered from and an entered-in-error vital does not belong in it; a Show retracted toggle renders them struck through, badged, and carrying the reason. Neither Retract nor Send to EHR is offered on one: the server refuses a second amendment with 'Observation is already retracted', and sending a withdrawn vital to the resident's EHR is the opposite of what retracting it meant |
| G21.2 | A care plan goal could never be marked achieved | S | done | `save_care_plan_goal` branches on `p_goal_id` -- insert when absent, update when present -- and nothing ever passed it. The plan rendered each goal's status (proposed, active, achieved, on hold, cancelled) and no goal could leave the value it was created with, so a care plan's goals only ever accumulated. The dialog now revises as well as adds. Worth separating from the unbuilt-feature class it resembles: the UI was honestly labelled "Add goal" throughout and promised no edit, so the defect is not a broken affordance -- it is a **status rendered as though it moves, by a product with no way to move it** |
| G21.3 | Status values nothing can set: swept, and clean | S | done | **The G21.2 shape generalised: 48 status-like columns with an enumerated CHECK, 457 distinct values, asking which the client can never choose.** 16 columns have a value the client never names, and on inspection every one is legitimately server- or externally-set and rendered generically through `human()` -- job pipeline states (`generating`, `in_flight`, `retry_scheduled`), notification delivery outcomes (`bounced`, `unsubscribed`), and the external eMAR's `administration_status` values (`late`, `not_available`), which arrive from the integration rather than from charting. **The first two versions of the probe were both wrong and both wrong in the direction of overclaiming**: counting a literal anywhere in the tree found zero, and counting only quoted client strings falsely accused `code_status` -- DNR, DNR-DNI, full code -- of being unsettable, when `CODE_STATUS_LABELS` holds them as unquoted object keys and `EditResidentCareProfileDialog` offers all of them. Recorded because a sweep that reports a clean result is only worth as much as the probe behind it, and this one needed two corrections before its answer meant anything. Noted in passing: `governed_content_review_comments` has `grant select` and no writer at all, so review comments on a governed revision cannot be created -- left alone rather than built, because nothing in the product offers or implies them, which makes it an unbuilt feature and not a broken one |
| G22 | A forced sign-out that left the impersonation tokens behind | S | done | **`MaintenanceGate`'s own comment already names this: "a raw `supabase.auth.signOut()` would strand the impersonation session in sessionStorage."** Three call sites did exactly that. The sharpest is `auth.tsx`'s definitive-profile-absence path, which signed the user out and cleared the query cache but called neither `clearImpersonationSession()` nor `clearSupabaseRuntimeCache()` -- so the ORIGIN platform_admin's access and refresh tokens stayed in `sessionStorage`, `useImpersonationStatus` kept reporting `isImpersonating`, and `MainLayout`'s banner kept offering an "Exit impersonation" button whose handler calls `setSession(originSession)` without checking that the impersonated session is still live. Whoever used that browser tab next had a working route back into a platform_admin session. The deactivated-profile path cleared the impersonation record but not the runtime cache; `MfaSettings`' assurance-refresh failure cleared neither. All four now route through one exported `clearLocalSessionState()`, which is the part that keeps the next forced-sign-out path from picking a subset again |
| G22.1 | The clinical chart's access log recorded the other surface's reason | S | done | `get_resident_clinical_chart`'s `p_minimum_necessary_reason` does not change the payload -- it is the HIPAA minimum-necessary annotation that `log_clinical_access` writes, one row per CALL. `useResidentClinicalChartSummary` keyed on `[key, residentId]` and dropped it, while the two callers pass different reasons: `ResidentClinicalChart` sends "Resident clinical chart view" and `MyResidentChart` sends "Caregiver clinical charting". They shared one cache entry, so opening the second surface within the 60s `staleTime` served the first one's payload, the RPC never ran, and that access produced no log row at all -- leaving an audit trail with one entry carrying the wrong reason. The reason is in the key now; the mutations invalidate on the 2-element prefix, so they still match |
| G22.2 | One undecryptable draft made every other draft unreadable and unsyncable | M | done | `readAllServiceDrafts` and `readAllObservationDrafts` decrypted under `Promise.all`, so a single throwing record rejected the whole call -- and all three callers propagate it: the review list went to an error state, syncing ONE draft failed because a DIFFERENT draft would not decrypt, and sync-all threw before attempting any draft. `decryptDraft` throws on a scope mismatch, on any AES-GCM failure, and on `assertFloorDraftAllowed` rejecting the payload, and that last one is reachable on an ordinary app upgrade: a release that tightens the field guards turns drafts already on the device into throwing records. Meanwhile the plaintext purge clock kept running, so care documentation that was only unreadable was deleted at `UNSYNCED_PURGE_AFTER_MS` without ever having been syncable. Now read per-record and skipped with a logged draft id; the plaintext lane the counts and purge already use still lists it |
| G22.3 | One `CLOSED` set, two different status vocabularies | S | done | `medicationSafetyAnalytics` used `{closed, resolved, completed}` for both `incidents.status` (`reported`/`investigating`/`closed`) and `corrective_actions.status` (`open`/`in_progress`/`completed`/`overdue`/`cancelled`). It covered the incident side and missed `cancelled` on the other, so a called-off corrective action past its due date counted toward `overdueFollowUps` and raised a retraining recommendation off an incident whose follow-up had been deliberately stopped. The SQL computing the same number for the operations snapshot has always excluded both terminal states. `resolved` was in the set and is not legal in either table. Split into two sets that match the CHECK constraints |
| G22.4 | `not_applicable` was in COMPLETED, and the evidence rule made that unreachable | S | done | `moveInReadiness` puts `not_applicable` in `COMPLETED` on purpose, then required a linked signed state form on top of it -- and an inapplicable item is precisely the one that will never have a form attached. It satisfied COMPLETED, failed the evidence test, fell past OPEN and landed on `needs_review`. All three rows using the default `requiresEvidence` are `blocker: true`, so the packet carried a blocker no action could clear and could never reach `inspection_ready` |
| G22.5 | A facility filter applied after the server's LIMIT | S | done | `usePromotableBinderExports` asked for the 25 most recent succeeded exports across the whole organization and only then narrowed to the facility in JavaScript. In a multi-facility org, 25 newer exports belonging to other facilities pushed this facility's out of the window and the page reported no promotable artifacts while they existed. `facility_ids` is `uuid[]`, so containment moves server-side ahead of the limit along with the `content_sha256` predicate; the one-element check stays client-side but now only discards multi-facility exports that already contain this facility |
| G22.6 | Two more invalidations that named nothing, in the shape G19.3 predicted | S | done | `credential-renewal-queue-summary` is a separate root, not a suffix of `credential-renewal-submissions`, so neither `useReviewCredentialRenewal` nor `useCreateCredentialRenewalSubmission` refreshed it. The RPC counts exactly what a decision changes and the query holds `staleTime: 30_000`, so a reviewer approved or rejected an item and watched the queue tiles keep the pre-decision counts. Invisible to `check:query-invalidations` by construction -- the gate asks whether an invalidation matches a query, never whether a mutation is missing one. Also: `/health` was the only server response with no cache directive at all |
| G22.7 | Three destructive-write bugs of one shape: a form that never tracked the record it edits | M | done | **All three saved defaults back over a real record, and two of them regenerate downstream work from it.** `QapiProjectDetail` had no hydration at all -- zero `useEffect` in the file -- so every field sat at its initial literal no matter which project was open, and `update_qapi_project_plan` is a straight `update ... set` with `nullif(btrim(...),'')`, not a coalesce-to-existing. Pressing "Save plan" wrote status back to `active` (regressing a `monitoring` or `pending_closure` project and losing the closure gate it had reached), reset the method to five_whys and the frequency to monthly, blanked `team_members` to `[]`, and NULLed the root-cause analysis, planned interventions, audit sample, barriers, adjustments, effectiveness determination and sustainment period -- the narrative body of the regulatory QI record. `ServiceDelivery`'s `RequirementDialog` is mounted unconditionally and gates itself on `open={!!requirement}`, so its `useState(requirement?.x ?? default)` initializers ran once, on the first render, while `selectedRequirement` was still null: "Configure" showed daily / 09:00-11:00 / DCS / two-staff off regardless of the requirement, and Save wrote that over the resident's service requirement and regenerated the future task instances from it, exactly as its success toast says. `DietaryOperations`' `ResidentNutrition` hydrates on `[profile]` but bails on `if (!profile) return`, and `resident_dietary_profiles` is read with `maybeSingle()` -- so `null` is a resident with no profile, not a pending load, and the form kept the PREVIOUS resident's diet order, food allergies, and texture and liquid consistency to be saved onto the newly selected one. Allergies and consistency are where the wrong resident's values stop being a data-entry annoyance. Fixed by hydrating the QAPI form (on `p?.id`, not `p`, so an action-item invalidation cannot wipe unsaved edits) and by keying the two dialogs on the id they edit |
| G22.8 | A hospital transfer recorded against a resident nobody chose | S | done | `ResidentCareDelivery` computed `residentId \|\| residents.data?.[0]?.id`, and that value feeds only three write actions -- of which the appointment and hospital-transfer cards render no resident picker at all. A user who had not chosen anyone could file a transfer episode or a provider appointment against whoever sorted first, with nothing on screen naming them. The fallback is gone and both cards now carry the same picker the DME card has. The same three mutations passed only `onSuccess`, and `queryClient` sets no global `onError`, so a server rejection produced no toast and no visible change -- indistinguishable from a click that did not register |
| G22.9 | The offline retry loop that could never reach its own deadline | M | done | `useRunAllOfflineSyncs` returned a fresh `run` on every render, and `OfflineSyncManager`'s retry loop lives in a `useEffect` whose deps transitively include it. The effect therefore tore down and re-armed its `setTimeout` from zero on every render, so while the caregiver kept navigating -- or any shell query settled -- the 30s backoff tick never reached its deadline and the backlog was never retried. That defeats exactly what the manager's own header comment says the timer is for: the failures that produce a draft (captive portal, dead route, Supabase down) leave `navigator.onLine` true, so the `online` event never fires and the timer is the only signal left. `run` is now a stable `useCallback` reading the latest body through a ref, rather than pinning correctness to the render-identity of two mutation objects and the toast handle |
| G22.10 | A cleared field that set a quiz's passing score to 0% | S | done | `Number("")` is `0`, and `0` is finite -- so `Number.isFinite(passingScore) ? passingScore : quiz.passing_score_percent` caught `"abc"` but waved an EMPTY field through as a 0% passing score, which `quizzes_passing_score_check` (`between 0 and 100`) accepts without complaint. Clearing the field to retype it and hitting Save made every subsequent attempt a pass on a quiz whose result backs a training certification. Blank now means "leave it alone"; a present value must be in range and is rejected loudly rather than coerced. `max_attempts` had the same `Number("abc")` -> NaN -> null hole and is guarded with it |
| G22.11 | Three more mounted-once dialogs, one of them deleting appended QAPI reasoning | S | done | The G22.7 shape again, found by sweeping for it. `InvestigationStepDialog` seeds its textareas at mount and is rendered unconditionally, while `record_incident_qapi_decision` APPENDS to the very column it edits -- `coalesce(investigation_findings \|\| E'\n\n', '') \|\| 'QAPI not indicated: ' ...` (20260726080100). A manager who recorded a QAPI decision and then opened "Investigation record" saw the pre-append narrative, and saving wrote it back over the appended reasoning. Re-keyed on open so it seeds from the incident as it is now. `CorrectiveActionForm` used `assigneeEmployeeId` as both the guard and a dependency of its default-to-self effect, so picking "Unassigned" -- the one choice its comment promises the reporter -- re-ran the effect and re-assigned the action to them; applied once via a ref now. `CreateWorkItemDialog` stamped a facility-local date with a literal `Z`, making "due today" 19:59 local in a Pennsylvania summer, so from ~8pm `manualWorkItemIssues` rejected it as "in the past" on a day with four hours left; it uses `facilityDayBounds().through` |
| G23 | Every destructive write ran before the only authorization check | S | done | **`accept-learning-package` authenticated the caller and then never asked whether they could touch THIS package.** Having a valid session is not permission. Everything from the package load to the hash update uses the service-role client, which bypasses RLS: it read the row, downloaded the zip, rewrote it, overwrote the stored object at its original path, and updated `content_sha256` -- and the only authorization in the function was `accept_learning_package`'s own check, in the RPC call that came AFTER all of it. So any authenticated user, of any tenant and any role, holding a `learning_packages.id` could have another organization's package rewritten and re-hashed; the 400 the RPC handed back did not undo it. RLS cannot stand in here -- the sole SELECT policy (`learner_packages_select`) requires `validation_status='accepted'` and this function acts on pending/validating/rejected rows, so a caller-scoped read returns nothing for every legitimate call. The rule is restated from the RPC it precedes and evaluated with the caller's JWT before anything destructive runs, answering 404 rather than 403 so a caller who may not act on a package does not learn the id is real |
| G23.1 | A signed download that would package another tenant's compliance binder | S | done | `package-survey-evidence-packet` takes `binder_export_job_id` straight from the request body and looked it up with the service-role client and no `organization_id` predicate. A user entitled to build a packet for their own organization could name another tenant's binder job and receive that organization's compliance binder PDF, appendix manifest and CSVs inside their signed download. `orgId` was already resolved a few lines above -- the binder now has to belong to it |
| G23.2 | The one browser-invoked function using the cron-only CORS style | S | done | `generate-certificate-pdf` built `withCronCorsHeader({"Access-Control-Allow-Origin": "*"})`, and stripping that header is precisely what `withCronCorsHeader` is for ("cron/webhook endpoints are not browser-invoked and must not advertise wildcard (or any) CORS origin"). The responses -- preflight included -- carried no `Access-Control-Allow-Origin` at all, so the browser blocked the "Prepare PDF" call `useCertificates.ts` makes and `config.toml` documents this function as serving. Every other `withCronCorsHeader` caller genuinely is cron-only; `generate-compliance-binder`, which `config.toml` names as having this same dual-purpose split, already used the origin-aware helper. Now matches it, with the cron secret named in Allow-Headers so the worker is unaffected |
| G23.3 | The shared kiosk was the one signed-in surface with no idle lock | S | done | `SessionSecurityGates` computes `isKiosk` from the path, reads `kiosk_idle_timeout_minutes` (default 5, against 30 for an ordinary session) and locks with its own `kiosk_timeout` reason -- but `IdleSessionLock` only ever mounted inside `MainLayout`, and the one route carrying `chrome="kiosk"` (`/trainer/classes/:id/kiosk`) renders `KioskLayout` instead. The entire kiosk branch, its setting and its lock reason could never execute, and the shared class-kiosk device -- the one most likely to be left unattended in a room full of people -- had no idle lock at all. Mounted in `KioskLayout` too; `lock()` already returns early without a user, so it degrades to a passive wrapper on any unauthenticated kiosk page |
| G23.4 | Exiting impersonation destroyed the only way back | S | done | On the restore step, `setSession` failing removed the sessionStorage record -- the sole copy of the origin admin tokens. The operator was left authenticated as the TARGET user with no route back and, because the amber banner renders off that same record, no remaining indication the session was an impersonation at all. The record is kept on failure now, so "Exit impersonation" stays retryable and the banner stays up. (Distinct from the `verifyOtp` path in `useStartImpersonation`, which correctly drops a PROVISIONAL record for a swap that never happened.) |
| G23.5 | The agreement you must accept could not be read | S | done | `/legal/facility-signup` -- the platform agreement and BAA -- is a real route linked `target="_blank"` from the consent checkbox on `/signup`, which is itself a signed-out page. It was absent from `isPublicPath`, so the new tab bounced straight to `/login` and the document being accepted was unreachable. Added to `MARKETING_EXTRA_PATHS` alongside `/privacy` and `/terms`. Also `sanitizePostLoginPath` rejected a leading `//` but not `/\`: the URL parser reads a backslash in the authority position as a forward slash, so `?next=/\evil.example` normalized to a protocol-relative cross-origin URL, which wouter's `history.pushState` refuses with a SecurityError -- breaking sign-in for anyone following such a link, and one consumer's `location.href` away from being an open redirect |
| G24 | A worker whose own defaults were infinite recursion | S | done | **`sync-billing-quantities` has never completed a run in production.** Its optional dependency defaults were `getEnv = (name) => getEnv(name)`, `randomUUID = () => randomUUID()` and `nowMs = () => nowMs()` -- a destructuring default is evaluated in the scope of the binding it initializes, so each called ITSELF. `index.ts` injects only `createClient`/`stripePost`/`stripeGet`, so every real invocation took all three and died with `RangeError: Maximum call stack size exceeded` at the first `getEnv("SUPABASE_URL")`, immediately after the cron check. It survived because no test reached the defaults: the unconfigured cases return 503 first, and the one test that goes further injects all three. Replaced with the same real implementations every sibling handler uses, plus a test that deliberately omits `randomUUID`/`nowMs` -- verified to fail with that exact RangeError against the old code. A repo-wide sweep for the same `x = () => x()` shape found no other instance. **2026-08-14: necessary but not sufficient.** The recursion fix was real and is unchanged, but the job still has zero rows in `app_private.system_job_runs` -- it now dies one line later, at the config check, because `STRIPE_SECRET_KEY` is not set on the project (A2). Two lessons this row could not have taught on its own, both closed in G269: a 503 that returns before `claim_system_job_execution` leaves no record at all, so "never completed a run" and "never scheduled" look identical on /admin/system-jobs; and the watchdog was reading pg_cron's exit status, which reports that the request was enqueued, so it called the job fresh the entire time |
| G24.1 | Three importers that stamped the UTC day onto a Pennsylvania record | S | done | `_shared/paDay.ts` exists precisely for this and names the four functions it was applied to; three more were missed. `bulk-import-credentials` compared `expiration_date` against the UTC day, so after 20:00 ET a credential expiring today imported as already `expired`. `bulk-import-residents` and the durable `process-data-import-jobs` worker both defaulted a missing `admission_date` to the UTC day, recording an evening admission as tomorrow's -- the date a resident's whole compliance timeline is computed from. All three use `paToday()` now |
| G24.2 | The one importer that reads the whole roster, unpaginated | S | done | `bulk-import-training-records` loads every employee with an `employee_number` into a lookup map, and an unpaginated select is silently cut off at PostgREST's max-rows (1000). Past that the missing employees came back as `Unknown employee_number: X` for people plainly on the roster -- the import refusing rows it should accept, with nothing indicating the roster read was what got truncated. Now paged until a short page. The other importers resolve one entity per row with `.limit(1).maybeSingle()` and were never exposed |
| G25 | A recurring manual campaign opened a cycle with nobody on it | S | done | `spawn_due_policy_campaign_cycles` carries the targeting columns across and copies the knowledge check forward -- explicitly, so "an annual re-attestation" does not "silently degrade to read-and-sign in its second year" -- then called `materialize_policy_campaign_targets`, which returns 0 untouched for any campaign whose `targeting_mode` is not `declarative`. For a MANUAL recurring campaign the worker therefore created the child cycle, copied its questions, enrolled ZERO employees, advanced `next_occurrence_on` and counted itself as spawned. Nothing raised and nothing was flagged; the annual re-attestation simply did not happen, and the only evidence was a campaign that looks open with no participants -- indistinguishable from one nobody has finished setting up. The roster is now carried forward the same way the questions are, re-checked against `employees.status` so a terminated person is not re-enrolled. `20260805120000`, covered by `policy_campaign_cycle_population.test.sql`, which was verified to fail on the old function |
| G25.1 | Two guard rails that raised a Postgres internal error instead of their own message | S | done | **`text[] \|\| 'literal'` does not append in PL/pgSQL.** With an untyped literal Postgres resolves the operator to `anyarray \|\| anyarray`, tries to read the string as an array literal and raises `malformed array literal`. `public.complete_appointment_follow_up` and `app_private.certification_attempt_blockers` both build their refusal message that way, so the moment either had something to report -- a follow-up closed before the outcome was recorded, an attempt blocked by an unpublished checklist version, self-assessment, or an unqualified assessor -- the specific, actionable refusal it was written to give was replaced by an internal error. Both fail closed, so no bad data got through; what was lost is the explanation. Verified against the local stack rather than inferred, and fixed by casting the literal. **`supabase db lint --level error` had been reporting both**, and `pnpm run check:database` runs it with `--fail-on error` -- so the database gate was red on main for this pair, and is green again. `20260805130000` |
| G25.2 | Refuted: declarative enrolment does NOT leave a NULL due date | S | done | Recorded because the code reads exactly like it does, and this was reported as a defect before being checked against a running database. `materialize_policy_campaign_targets` inserts into `policy_attestations` without naming `due_date`, and the column is nullable with no default -- which looks like every declaratively enrolled attestation gets a deadline that `p.due_date < public.pa_today()` and the pending index both lose against. It does not: the BEFORE INSERT trigger from `20260716221235_remediate_policy_attestation_security.sql` assigns `new.due_date := v_campaign_due_date` from the campaign unconditionally, along with the organization, facility, version and status. The trigger owns those columns. `policy_campaign_cycle_population.test.sql` pins that guarantee to the overdue predicate that depends on it, so removing the assignment fails a test rather than quietly disabling every overdue count |
| G26 | The gate certifying 34 unauthenticated edge functions was satisfied by an import | S | done | **The same vacuity this check has now had twice.** Its own comment records the first: the marker matched `export function requireCronRequest` inside `_shared/cronAuth.ts`, so any function importing that module for anything at all was certified as cron-gated, and `blankDeclarationHeads` was added to stop it. But an IMPORT is not a use either, and `import { requireCronRequest } from "../_shared/cronAuth.ts"` still matched -- so a function could have its gate call deleted and keep passing. That certification is what stands behind 34 functions running with `verify_jwt = false`. Measured against the old and new module side by side: import-only proved the gate `true` before and `false` after, while a real call still proves it. Import statements are now blanked whole (newlines preserved, and `importClosure` walks the raw text so the closure is unchanged), with three self-test cases covering both directions -- 15 now, and the real tree still certifies all 73 functions, so nothing was relying on an import to look gated |
| G26.1 | A fork PR could deploy unmerged code to the production database | S | done | `deploy-migrations.yml` triggers on `workflow_run` for the CI workflow with `branches: [main]`, and that filter matches the triggering run's HEAD BRANCH NAME. CI runs on `pull_request`, including from forks -- so a fork PR opened from a branch the contributor happens to have named `main` satisfies it. The job's only other condition was `conclusion == 'success'`, which the fork's CI supplies for the fork's own code, and the checkout takes `github.event.workflow_run.head_sha`: the fork's commit. That is a path from opening a pull request to running `supabase db push` and deploying edge functions against production on unmerged, unreviewed code, with the `production` environment's reviewers as the only thing in the way. The trigger now additionally requires `workflow_run.event == 'push'`, `head_branch == 'main'` and `head_repository.full_name == github.repository`; a fork PR is always `pull_request` from another repository |
| G26.2 | Two more gates that failed open | S | done | `deploy-migrations.yml`'s path short-circuit ran its `git diff` under `\|\| true`, making an ERRORED diff indistinguishable from one that found nothing -- and "nothing" means skip the deploy. An unfetchable previous SHA (force-push, pruned object, shallow fetch) therefore skipped a production deploy and reported success, which is the exact failure the workflow's own header says it exists to close ("migrations merged to main could sit undeployed indefinitely"). The status is captured now and an undetermined diff deploys rather than skips, which is the safe direction because `supabase db push` skips migrations already applied. Separately, `check-dormant-rpcs.mjs` excluded `*.test.ts` from the CLIENT walk but not the EDGE walk, so an RPC whose only caller was a Deno edge-function test counted as reached -- a function called only by its own test is what dormant means. Both walks exclude tests now; the check still passes, so nothing was relying on it |
| G27 | Two crosswalk rows that could never return to covered | S | done | `evaluateEvidence`'s `physical_site` branch carries an explicit guard -- "Completed/cancelled corrective actions keep their past due_date forever; they must not register as permanent physical-site gaps via the overdue check" -- and the `resident` and `policy` branches, which have exactly the same problem, did not. A resident compliance item completed even one day LATE is `compliant` with `due_date < today` from then on, and `policy_attestations.status` is only pending/attested with signing leaving `due_date` untouched. Both therefore counted as open gaps forever, so an obligation whose evidence was fully satisfied still reported uncovered and no action available to the facility could clear it. That is worse than a miscount: the number never moves. Both branches now exclude the satisfied statuses from the date-based check, covered by two tests verified to fail against the old code |
| G28 | Two CI jobs budgeted for less than their own clone | S | done | `planning-registers` and `changes` both spend essentially their whole runtime on a `fetch-depth: 0` clone -- the checks themselves finish in under a second -- and both were capped at `timeout-minutes: 5`. On `016401c` that checkout took 4m59s and the timeout cancelled the job mid-checkout, which failed the aggregate `ci-result` and so the PR, for a reason with no connection to the code; the retry cloned the same commit in 25s. That is the shape worth naming: not a flake, a budget one slow clone away from failing on any PR, and when it fires it looks like the register check found something. `changes` is the worse of the two because every other job is gated on its outputs, so a slow clone there takes the whole run rather than one check. Both raised to 10, matching `migration-immutability` -- the closest analogue, full history plus a fast script -- and still below `secret-scan` (15) and `application` (25) |
| G29 | Evening incidents, a literal backslash-n, three 404 links, and a search that answered to the forbidden term | S | done | Sweep of the remaining lib-layer findings. `incidentAnalytics.daysSince` ended its window at `T23:59:59Z`, which is 19:59:59 in Pennsylvania -- an incident reported at 20:30 was AHEAD of "the end of today", so `daysSince` returned -1 and both `days >= 0` recency filters dropped it: the last-7 and last-30-day counts understated exactly the freshest, most likely still unresolved incidents. `calendarExport` joined its description lines on the two-character sequence `\n` and then ran `escapeIcsText`, which escaped the backslash again -- calendars showed a literal `\n` instead of a line break. `carebaseGlossary` pointed three terms (Violation, Plan of correction, Attestation) at `/app/compliance`, which `App.tsx` does not register, so the glossary's own "related route" button rendered the 404 page; `check-server-route-links` enforces precisely this rule but only for links built in migrations and edge functions, so a client-side registry is outside its scope -- a sweep of every client `href:` literal against the route table found no others. And facility-type search answered to `ALR` but not `ALF`: `documentTemplates` matched the stored code only, while `dhsFormsLibrary` had bolted on `query.includes("alf")` -- an inverted substring test, so "half" returned every ALF form. Both now use one `facilityTypeMatchesQuery` helper that matches the code OR the displayed label, which also picks up "assisted living" and "personal care". Separately `pchAlrOperationalSnapshot.severityFor` took a `watchThreshold = 1` that all sixteen call sites left at the default, making `"watch"` unreachable and the dashboard's "Attention buckets" metric merely "buckets with anything open"; the dead dial is gone, and choosing a real threshold is left as the product decision it is |
| G30 | A 24-hour grace period nobody granted, a DME no card could see, and a day that depended on where you were sitting | S | done | `residentNeedsAttention` used one overdue predicate -- `daysBetween(...) > 0`, a whole-day floor -- for both a DATE column and a `timestamptz`. For the instant it was simply wrong: a change-of-condition follow-up due at 09:00 did not count as overdue until 09:00 the NEXT day, so every follow-up had a silent 24-hour grace period on the card whose entire job is to say a resident needs looking at now. Split into `isDateOverdue` (facility day) and `isInstantOverdue` (the moment it passes). **Both medical-evaluation cycles were absent from `ASSESSMENT_ITEM_TYPES`, and that set gates BOTH the assessment card and the missing-state-form card** -- so a missing or overdue DME (55 Pa. Code 2600.141 / 2800.141) produced no card from any path. An existing test asserted that exclusion by name; it now asserts the type that is legitimately excluded, `support_plan_30day`, which has its own dedicated cards and would otherwise raise two for one obligation. `residentCareConflicts.localCalendarDay` derived its day from `getFullYear/getMonth/getDate` -- the VIEWER's timezone -- so a return recorded at 21:00 in Pennsylvania resolved to the next day for anyone on a UTC machine and the same record produced different conflicts for different staff; it uses `facilityToday()` now, the same day `pa_today()` computes server-side |
| G30.1 | Refuted: work-item source routing and the two lookback windows | S | done | Both reported, both wrong, both worth recording because the first is easy to re-report from a partial read. `sourceRouteForWorkItem` was said to be missing cases for five registered source types; extracting the full switch shows 34 cases against 32 registered keys -- every one is routed, plus a `default: return null`. The earlier count came from reading a truncated line range, which is the failure mode to avoid on a long switch. And `residentNeedsAttention`'s 7-day service-exception window was reported as disagreeing with change detection's 14-day one: they are different signals. The assistance and refusal thresholds ARE imported from `residentChangeDetection` and shared, exactly as the file's header promises; the 7-day figure pairs with `serviceExceptionsLast7Days`, the RPC fallback aggregate it substitutes for, so the typed and untyped paths agree with each other |
| G30.2 | A bookmark that said "suspend", equipment changes nobody saw, and a reason that was dropped rather than deferred | S | done | `learningRuntime`'s suspend-data chain ended in `raw["cmi.core.exit"]`. That is SCORM's exit MODE -- "suspend", "logout", "time-out" -- not suspend data, so a package that set an exit mode without any suspend data had the learner's bookmark stored as the literal string `"suspend"`, and resuming handed the content that string as its saved state. `cmi.suspend_data` is the real key in both SCORM 1.2 and 2004 and was already in the chain. `hospitalReconciliation.recordedChanges` omitted `dme_changes`, which is in `RETURN_CHANGE_FIELDS`, is asked for by the return dialog, and is written by `complete_hospital_return` -- so a return whose only recorded change was new equipment (a walker the resident did not go in with, oxygen, a lift) displayed "no changes recorded" on the section a nurse reads before revising the plan, which is the change most likely to alter a transfer-assistance service. And `employeeReadiness`'s `conditionally_ready` verdict listed due-soon credentials but not due-soon training; because that verdict RETURNS rather than falling through to `expiring_soon`, the training reason was not deferred to a later branch, it was dropped |
| G30.3 | Refuted: the corrective-action verification blocker | S | done | Reported as counting non-completed actions into an unclearable closure blocker. It does not: the `unverified` branch is inside `if (open.length === 0)`, so it only runs once every live action is complete, and `live` at that point contains exactly those completed actions. The message it produces ("N completed actions have no verification recorded") is accurate, and recording verification notes clears it |
| G31 | Three more gates that reported on something they had not examined | S | done | `check-date-only-parsing` scanned FILE CONTENT line by line, and its balanced-paren walk needs the closing paren to be on the same line. A Prettier-wrapped call -- `new Date(\n  row.effective_date,\n);` -- left the walk with `arg` equal to just `"new Date("`, which contains no column name, so the call site was skipped and the file reported clean. Proven rather than argued: planting exactly that call in `complianceDates.ts` makes the old checker exit 0 and the new one report `src/lib/complianceDates.ts:35`. The walk never cared about newlines; only the caller did. Now scans whole files and derives the line number from the match offset, with the wrapped forms added to the self-test (15 cases). `check-migration-policies` resolved its migrations directory from `process.cwd()` while already computing `SCRIPT_DIR` for its allowlist, and its ENOENT branch printed "No supabase/migrations directory found; nothing to lint" and exited 0 -- so run from anywhere but the repo root (a hook, an editor task, a CI step with `working-directory`) the lint passed without linting. It resolves from the script now and a missing directory throws. And `ci.yml`'s path filter omitted `railway.json`, `railpack.json`, `tsconfig.base.json` and `.nvmrc`: a PR touching only those set `application=false` and `database=false`, every gated job was skipped, and `ci-result` aggregates a skipped job as success -- so it merged with no validation at all |
| G19.1 | The incident one is the sharpest, because the code said what it wanted | S | done | `useIncidentFollowThrough`'s own comment reads: "a single call routinely changes all three -- determining reportability, for instance, also creates or stands down notification rows." It then invalidated `["incident-notifications"]` while the query is `["incident_notifications"]`. The intent was written down, the hyphen defeated it, and the notification list never heard about rows the same call had just created |
| G32 | A forgeable IP on a signed legal record, an unauthenticated buffer, and a preview that approved rows the apply refuses | S | done | Sweep of the remaining edge-function findings, all five on functions running `verify_jwt=false`. `submit-confidential-intake` carried its OWN `clientIp`, taking `cf-connecting-ip` unconditionally and otherwise the FIRST hop of `x-forwarded-for` -- the half of that list the caller writes. `_shared/clientIp.ts` exists because that is backwards (the last hop is the one the gateway observed and appended, and `cf-connecting-ip` is honored only under `CF_FRONTED`), and this is the endpoint where it matters most: the value keys the per-IP rate limit, so a fresh fake per request defeated it, and it is also stored as the ESIGN attribution on the intake -- an attacker-chosen address written onto a signed legal record. `integration-api` checked `content-length` and then called `req.text()`; a chunked request has no such header, `Number(null ?? "0")` is 0, and the whole body was buffered into memory BEFORE the API credential on the next lines is authenticated. `readTextBody` aborts the stream at the cap instead. `survey-packet-guest-download` had the same unbounded `req.json()` and additionally returned the raw Postgres error text from `resolve_survey_packet_guest_token` to a caller who holds nothing but a token; it is logged now and answered with a fixed message. `bulk-import-incidents`' dry run accepted a summary of 8 characters while BOTH apply paths -- `record_incident_from_import` and the durable worker's `apply_incident_import_row` -- reject `length(btrim(narrative)) < 10`, so the preview called rows valid that the import then refused, which is the one thing a preview exists to rule out. And `fhir-writeback` treated a failed `complete_fhir_writeback` as an ordinary error, but the resource is already POSTed by then: the row stayed claimable and the next run re-sent it, duplicating a clinical record in the customer's EHR. Retried three times with backoff, and if it still fails the external resource id is logged at error level -- it is the only handle for reconciling the duplicate and exists nowhere else once the function returns |
| G33 | A SECURITY DEFINER helper that restated one of the three rules its own RLS policy states | S | done | `materialize_policy_campaign_targets` writes `policy_attestations` with the service-role-equivalent authority of SECURITY DEFINER, so `policy_attestations_insert` is never evaluated on the rows it creates and the function has to restate that policy itself. It restated the role/tenant rule and neither of the other two. **`is_assigned_to_facility(facility_id)`**: that predicate is true unconditionally for org_admin and auditor, so it constrains exactly one role -- facility_manager -- and `policy_attestation_campaigns` has no facility column and an org-scoped insert policy, so a facility_manager may author a campaign with `target_facility_ids => null`, which means every facility in the organization. Materializing it enrolled every active employee in the org. The same person inserting the same row through PostgREST is refused. Reproduced against a real Postgres with the helper bodies copied from their migrations: acting as a manager assigned to Facility A only, the old function returned `enrolled=2` and left an attestation row at Facility B; the new one refuses and writes nothing, while the manager's own facility-scoped campaign still enrols. **`identity_assurance_is_current('policy_document_admin')`**: every call arriving through `create_policy_campaign_with_questions` has already passed this, because that RPC is SECURITY INVOKER and its campaign insert is governed by `policy_attestation_campaigns_write` -- but the materializer is granted to `authenticated` and reachable directly over PostgREST with no campaign insert ahead of it, and `policy_document_admin` is in the program-wide baseline `identity_operation_requires_aal2` applies even with no tenant policy row. Both checks sit inside the existing `auth.uid() is not null` branch, so the daily sweep and the recurrence spawner -- which carry no JWT and are gated by the EXECUTE grant -- still reach every matched facility and stay idempotent, verified. Refusing rather than silently narrowing the insert is deliberate: a partial enrolment is indistinguishable from a complete one on the campaign screen, which is the failure G25 and 20260803060000's own note both describe. The target set moved into `app_private.policy_campaign_matched_employees` so the scope check and the insert cannot come to disagree about which employees a campaign matches. `20260805140000`, covered by `policy_campaign_enrolment_scope.test.sql` |
| G34 | Two appliers on one import ledger, and only one of them ever took the claim | S | done | **The durable worker and the browser applied the same import rows to the same customer.** `claim_data_import_jobs` claims any job in `ready` or `applying` whose claim is absent or expired, and `process-data-import-jobs` then applies every `data_import_rows` row still marked `valid`. A browser apply is EXACTLY that shape: the eight `bulk-import-*` functions walk the CSV in chunks and hold the job at `applying` between them, and nothing on that side had ever written `claim_expires_at` -- so an import a manager was halfway through was indistinguishable from one a dead tab had stranded, which is the case the worker was built to rescue. Both then applied the same rows, because the browser's loop reads the CSV rather than the ledger. Not an error on either side: duplicate employees, residents, credentials, contacts, assessments and incidents in a customer's data, from an import that reported success twice. All eight durable domains were exposed. The mechanism to prevent it already existed and only one participant was using it. `record_data_import_chunk` -- the single RPC every browser chunk already goes through -- now holds the job's claim for its caller, renews it per chunk, refuses when someone else holds a live one, and clears it the moment a phase is reported finished. Two things follow with no change to the worker: `claim_data_import_jobs` skips a job a session is working (its `claim_expires_at` predicate finally has something to read, and its `for update skip locked` passes over the row this function locks), and calling `record_data_import_chunk` with an EMPTY chunk and no status is an atomic lease acquisition -- which is what the importers now do before applying their first row, because discovering the conflict when the receipt is written is too late, the duplicates already exist (`_shared/importJobLease.ts`). Measured against the old and new function side by side on a real Postgres: with the old one a worker sweep claimed the job mid-apply with four ledger rows still `valid`; with the new one it claims none, a second session is refused, the holder is not locked out by its own claim, a finished phase hands the job straight back, and an EXPIRED claim is still takeable -- the stranded-run recovery the worker exists for, kept. `20260805150000`, covered by `data_import_apply_lease.test.sql` |
| G35 | Which annual rule-pack row a resident got was the planner's choice | S | done | `resident_compliance_rule_packs` is keyed by (organization_id, state, facility_type, item_type, admission_track) -- its own lookup index says so, and `instantiate_resident_compliance_items` reads it that way, substituting `'standard'` for PCH, which has no expedited track. The successor lookup 20260804170000 added to `complete_resident_compliance_item` dropped two of those predicates: it matched on item_type, facility_type, is_active and tenant, then `order by rp.organization_id nulls last limit 1`. For ALR that matches BOTH seeded `annual_medical_evaluation` rows -- standard and expedited, derived from the two `medical_evaluation` rows 20260706155617 seeded for 2800.141 -- and both are global, so they tie on the only ORDER BY key and LIMIT 1 returns whichever row the plan reached first. The two rows disagree: `warning_days` is carried from the source row, 30 on the standard track and 14 on the expedited one. So an ALF resident's annual medical evaluation began warning 30 days out or 14 days out with nothing in the data to say which, on the cycle whose entire purpose is to raise the item before it lapses. Reproduced on a real Postgres with both seeded rows present: the old function handed an EXPEDITED resident `warning_days=30`, the standard track's window; the new one gives 14 while a standard resident still gets 30. The predicates are restated as the rest of the schema writes them, with the same PCH substitution, and the ORDER BY made total (`created_at desc, id`) so a duplicate key cannot reintroduce a coin flip. `20260805160000`; `medical_evaluation_cycles.test.sql` gains an expedited resident, and its existing assertions are untouched because the resident it already had is on the standard track |
| G36 | The seeded catalog still called it an Assisted Living Residence | S | done | CLAUDE.md's rule is that every customer-facing string says Assisted Living Facility (ALF) while the STORED CODE stays `'ALR'`. The app source had been swept and is clean -- every remaining `ALR` in it is an identifier, a constant name or a comment, and `facilityTypeLabel` has always produced the ALF label. What no sweep had reached is the text seeded into TABLES, which the app renders verbatim: `dhs_citation_topics.title` is the violation topic picker, the governance list and the dashboard's topic badges, `.category` sits under it, `courses.title`/`.category` are the catalog a learner reads, and one onboarding template label is what a new ALF hire is measured against. Those rows said "ALR Medical Evaluations", "ALR Annual Topics", "ALR 18-hour initial training". Three things make it a data change rather than a copy change and each is handled: `dhs_citation_topics.category` is a UNIQUE lookup key that `auto_tag_resident_compliance_item_citation_topic` selects on by literal, so the trigger is redeclared with the literals moved in the same migration -- renaming the rows alone would have left every new ALF resident-compliance item with a NULL citation topic, silently untagged, which is worse than a wrong word; only the PLATFORM catalog is rewritten (`organization_id is null`), because a course a customer named is their copy; and the prefix is matched as `'ALR '` with the trailing space so `catalog_code`, `training_type_code` and `topic_code` (ALR-18HR-INITIAL, ALR-DIRECT-ANNUAL, ALR-2800.65-I1) are untouched -- they are joined on and never shown as prose. The migration ends with a guard that raises if any category the trigger asks for does not exist, verified to pass on the renamed data and to fail on a deliberately stale rename. The courses half additionally needs `app.privileged_write`: `validate_course_catalog_publication` fires BEFORE UPDATE on every published course and re-runs the whole publish gate, whose readiness check refuses a caller who is not a platform admin -- so a pure copy edit failed authorization as `postgres`, which the database CI job caught before this reached main. `20260805170000`; `medical_evaluation_cycles.test.sql` moves with it |
| G37 | A required build-time variable set to a single space passed the check that exists to stop it | S | done | `vite.config.ts` throws when `VITE_SUPABASE_URL`, `VITE_SUPABASE_ANON_KEY` or `VITE_TURNSTILE_SITE_KEY` is missing, because Vite inlines them and a bundle built without them is a blank page while `/health` still returns 200 and Railway calls the deploy healthy. The test was `!env[key]`, which catches unset and `""` and not `" "` -- and the file's own optional-variable warning, eight lines below, exists precisely to say that a whitespace-only value is TRUTHY and ships as itself. So the one input the gate did not test produced exactly the deploy it was written to prevent: `createClient(" ", " ")` throwing at module init. A Railway `${{...}}` reference that resolves to a padded value gets there without anyone typing a space. Now `!env[key] || env[key].trim() === ""`, with the message saying which case it is |
| G38 | A pre-departure sign-off that outlived the thing it signed for | S | done | `set_appointment_preparation_item_ready` already states the rule and states it well -- "Reopening an item after the pre-departure sign-off would leave `preparation_completed_at` asserting something that is no longer true" -- and clears the sign-off in the same statement. `add_appointment_preparation_item` is the other way to make it untrue and did not. Its item defaults to `required => true` and inserts `ready = false`, so adding one to an appointment already signed off produces exactly the state the reopen guard prevents; the ON CONFLICT branch reaches it too, by promoting a not-ready optional item to required. Not a display quibble: `complete_appointment_preparation` refuses while anything required is outstanding and cannot be called again on an appointment that is already signed off, so the new item never gets its gate. The nurse who adds "oxygen cylinder" twenty minutes before transport sees a green sign-off on an appointment with an outstanding required item, and the sign-off is the thing meant to say otherwise. Cleared now, conditional on the item actually being required and not ready -- verified on a real Postgres across all four cases: required-and-outstanding clears it, an optional item does not, re-adding an already-ticked item does not, and promoting an outstanding optional item to required does. `20260805180000` |

| G39 | Four submit buttons that stayed enabled after their date field was cleared | S | done | `new Date("").toISOString()` raises `RangeError: Invalid time value`, and raising it out of an onClick is a blank page, not a validation message. Five components already get this right by refusing to submit while the datetime-local field is empty (`!startsAt`, `!returnTime`, `!identifiedAt`, `Number.isNaN(new Date(...).getTime())`); four did not. `ResidentServicesCalendar`'s reschedule and create dialogs gated only on ORDERING -- `ends <= starts` -- and every non-empty string is greater than `""`, so clearing "Starts" left the comparison false and the button live; its follow-up drafts were added on title/description length alone, so a cleared due field reached the same call at submit. `BreakGlassCard` is the sharpest of the four because its guards read as thorough: `expiryPast` and `expiryTooFar` are both `!!expiry && ...`, so an EMPTY field violates neither and `canGrant` stayed true -- the emergency-access grant, of all things. A usable expiry is now its own condition rather than the absence of a violated one, which also covers an unparseable value, where every comparison against NaN is false |
| G40 | Three more places where the day was the browser's, not the facility's | S | done | `IncidentDetail` stored the final report submission date as `new Date("2026-08-05").toISOString()`, and a bare date string parses as UTC MIDNIGHT -- 2026-08-04 20:00 in Pennsylvania. So the date a manager typed was filed as the PREVIOUS day on the one field 55 Pa. Code measures a reporting deadline against, and the two read-backs beside it disagreed with each other about which day it was. `facilityDayBounds().from` resolves the same calendar day the way `pa_today()` does. `AuditLog`'s export manifest built its window from `new Date("YYYY-MM-DDT00:00:00")`, which is the VIEWER's midnight -- an auditor on Pacific time got a manifest starting three hours after the facility day the operator asked for, and the manual `setDate(+1)` on the end was reaching for what `facilityDateRangeBounds().through` already gives. And `ComplaintTrendsCard` defaulted its two pickers from `new Date().toISOString().slice(0, 10)`, the UTC day: from 20:00 ET the card opened on TOMORROW and looked back ninety days from there, against an RPC that reads `date_received::date` in the facility's calendar. `check-date-only-parsing` does not catch any of these -- it matches `new Date(<column name>)`, and all three go through local state variables |
| G41 | A page clamp that fired before the count it clamps against was known | S | done | `Residents`, `Incidents` and `Alerts` each run `if (page > totalPages) setPage(totalPages)` in an effect, where `totalPages` derives from `query.data?.count ?? 0`. On the first render `data` is undefined, so `totalPages` is 1 -- and the effect fires immediately, rewriting a deep-linked, bookmarked or Back-navigated `?page=7` to `?page=1` and refetching the wrong page before the real count ever arrives. Every one of those three pages stores its page in the URL specifically so that Back returns to the same view, which this defeated. The clamp is for a page that no longer exists after a filter narrows the set; "we have not asked yet" is not that, so it now waits for the count to be known |
| G42 | Five links that pointed at the domain root of a deploy that is not served from it | S | done | `vite.config.ts` reads `BASE_PATH` into Vite's `base`, so every route can be prefixed -- and ten link builders reached for `window.location.origin`, five of which prepended that prefix and five of which did not. The five that did not are the ones that leave the app: the invitation reset-password redirect, the class check-in QR code, the training-passport QR, the anonymous safety-report link a facility posts publicly, and Stripe's return/success/cancel URLs. Under a base-path deploy every one is a 404, and a QR code and a payment redirect are exactly the links nobody notices are wrong until someone outside the building follows one. The asymmetry is why it survived: at the domain root, which is how the app is run in development, the broken form and the correct one produce the same string. `lib/appUrl.ts` is the rule now -- `appPath` for in-app routes, `absoluteAppUrl` for links that get pasted or scanned -- and the five sites that were already correct use it too, since ten hand-rolled copies is what let half of them drift |
| G43 | An unconfigured quiz that reported as an error, a maintenance gate that never refetched, and a discharge that failed in silence | S | done | `useGetQuizByBlockId` used `.single()`, so a course block with no quiz yet -- the ordinary state of a block being authored -- came back as a PGRST116 ERROR, retried three times, and was then reinterpreted by the caller as "No quiz configured yet". The failure was being used as the empty signal, which also means a genuine fault (a row RLS filtered out, two rows on one block) read as "not configured yet". `.maybeSingle()` makes null mean absent and an error mean an error. `usePlatformStatus`'s own comment says "refetchOnWindowFocus (default) covers tab re-focus sooner" -- but `lib/queryClient.ts` sets `refetchOnWindowFocus: false` app-wide, so returning to a backgrounded tab did not re-check the maintenance gate at all and waited out the poll interval instead; it opts back in explicitly now, which is the exception that gate earns. And `ResidentDetail`'s status Select was the only mutation on the page with no error path: a refused discharge (RLS, or a trigger holding it until something is documented) left the control showing the new value until the next refetch quietly snapped it back, saying nothing -- and silence reads as success on a status change |
| G44 | A single-facility convenience that deleted the resident it was navigated to | S | done | Three pages (`DietaryOperations`, `ResidentServicesCalendar`, `ResidentFinancialOperations`) auto-pick the facility when an org has exactly one: `if (!facilityId && facilities.length === 1) setFacilityId(...)`. But `useResidentNavigationContext` resolves `facilityId` as `state.facility || linkedResident.data?.facility_id`, and on a deep link of the form `?resident=X` with no facility that second term is undefined until the resident query returns -- so on the first render `facilityId` was empty, the effect fired, and `setFacilityId` cleared `resident`, exactly as a USER changing facility should. The deep-linked resident was gone before the page finished loading, on precisely the single-facility orgs the convenience exists for. Fixed in the hook rather than three times over: `adoptDefaultFacility` seeds the facility without clearing anything and refuses outright once the URL carries either value, because a seeded default has no business overwriting the state the caller arrived with. `setFacilityId` keeps its clearing semantics for a real facility change |
| G45 | An evidentiary date that moved every time the form was saved | S | done | `BackgroundChecks` wrote `non_disqualification_statement_signed_at`, `supervision_attestation_confirmed_at` and `suitability_determined_at` as `flag ? new Date().toISOString() : null` on every save. So re-opening a profile to fix a typo in the notes moved all three to today -- the date the OAPSA non-disqualification statement was signed, the date supervision was confirmed, and the date suitability was determined, which are the dates a surveyor asks for. The record kept saying "the last time anyone touched this form". A flag that was already set now keeps the timestamp it was set with; only a newly-set flag is stamped, and clearing one still clears it |
| G46 | A CSV column that was blank in the view it is usually exported from | S | done | `ComplianceCommandCenter` resolves each row's building name through `useComplianceFacilityBuildings`, which was `enabled: !!facilityId` -- and the page passes `undefined` whenever the facility filter is on "All facilities", its default. So in the most common view of the page the query never ran, `buildingName()` returned `""` for every row, and the CSV a facility hands to a surveyor had an empty Building column: not marked unknown, just blank, which reads as "no building" rather than "we did not look". The hook fetches every building the caller can see when no facility is named (RLS scopes the unfiltered read the same way), the building FILTER still only offers the selected facility's, and an id that genuinely cannot be resolved now says so |
| G47 | Six generic DOM ids on a page that already knew better | S | done | `EvidenceCollectionDetail` computes a `useId()` prefix and used it for exactly one field; the other six carried hardcoded ids -- `guest-label`, `revoke-reason`, `withdraw-reason`, `evidence-export`, `evidence-display-name`, `guest-expiry`. Those are generic enough to collide with anything else mounted at the same time, and a duplicate id sends both the label's click and the screen reader's announcement to whichever element the document reached first. All seven derive from the prefix now |
| G48 | A termination case that showed a UUID where the person's name goes | S | done | `EmployeeLifecycleCases` fetches employees with `status: "active"` and uses that one list for two things with opposite needs: the create dialog's picker, which should only offer people who can still be transitioned, and `employeeName()`, which labels EXISTING cases. A termination case is by definition a case whose employee is no longer active, so the screen that reviews terminations fell through to `id.slice(0, 8)` -- a truncated UUID in the column where a person's name belongs. The fetch is unfiltered now and the picker narrows to active in a memo |
| G49 | The filing gate refused the incidents that need filing most | S | done | `Incidents`' create dialog blocks a major/critical incident with no manually-added notification, and its own message says "(or the auto state-hotline entry)" -- a notification the check did not account for. `auto_create_incident_notifications` files a state-hotline row (and a law-enforcement one for abuse and assault) on insert for EVERY incident type the form offers except `other`, so a critical death or abuse allegation could not be filed until the reporter hand-entered a duplicate of the row the database was about to create. On the pathway where minutes matter, the guard rail was in the doorway. The auto-notified types are now named once in `incidentStages.ts`, mirrored from the trigger's preset table with a test that pins the pair together, and the manual requirement applies only to `other`, which is the one type no obligation can be inferred from |
| G50 | A comparison card that opened on nothing, and a QAPI meeting recorded in the future | S | done | `SupportPlanVersionComparison` seeded its two version selects with `useState(() => ordered[0]?.id)`. A lazy initializer runs once, at mount, and `plans` is `query.data ?? []` -- so it ran against an EMPTY array, stored `""`, and when the versions arrived the card rendered its full comparison UI with both selects blank and no diff. Its entire purpose is to open on latest-versus-previous. Both ids are derived with a fallback now, which also self-heals if the selected version stops existing. Separately `QapiProjectDetail` built two `datetime-local` defaults as `new Date(...).toISOString().slice(0, 16)` -- a UTC wall clock handed to an input that renders it as LOCAL time, so at 21:30 ET the "held at" field opened on 01:30 the NEXT day and submitting it recorded a QAPI meeting as held in the future, on the wrong date; reading back was already local, so the round trip disagreed with itself. `toDateTimeLocal` is what the rest of the app uses. Both submit paths also gained the G39 blank-input guard -- the meeting button did not check `held` at all |
| G51 | A compliance report that stopped at a thousand rows | S | done | `useListAllResidentComplianceItems` ran one unpaginated select, and PostgREST caps those at max-rows (1000 by default) with no error -- just a short list. It backs the resident compliance REPORT and the State Forms Center queue, every resident carries roughly half a dozen items, so a 170-resident organization is already past the cap. Because the rows are ordered by `due_date` ascending, what fell off was the far end of the calendar, and nothing on screen said the list was incomplete. Paged until a short page, the same way `useComplianceInstances` already does it and for the same stated reason -- this is the third instance of the shape after G24.2 |
| G52 | Enterprise SSO could not be completed at all | S | done | `signInWithSSO` does not sign anyone in: it returns the identity provider's authorization URL and the caller has to send the browser there -- the method's own documentation is literally `if (data?.url) window.location.href = data.url`. `Login.tsx`'s mutation returned that data and dropped it. So "Continue with enterprise SSO" resolved SUCCESSFULLY, cleared its pending state, raised no error, and left the user sitting on the login page. The whole enterprise sign-in path was unreachable, and the shape of the failure -- success with nothing happening -- is why it reads as a broken button rather than a bug anyone reports. Redirects now, with an explicit message for the case where a domain has no SSO connection configured |
| G53 | Two dialogs that reopened holding the last person's details, and a shortcut that worked once | S | done | `AdmissionOperations` renders `ProspectDialog` and `RoomDialog` unconditionally, so their field state was created once at mount and never reset: after adding a referral the dialog closed with every value still in it, and opening it for the NEXT person showed the previous one's name, date of birth, phone and contact details, pre-filled and ready to save. Re-keyed on open, the same fix as G22.7/G22.11. Its move-in workspace link also went to `window.location.href = \`/app/...\`` with no base path (G42's shape). And `Employees`' `?action=add` handler ran on a query-string CHANGE but never consumed the parameter, so following "Onboard employee" from the sidebar or the dashboard a second time -- having closed the dialog once -- set an identical search string and opened nothing: the shortcut worked once per visit and then looked broken. The parameter is cleared once acted on, which also stops the URL asserting a dialog that is no longer open. Reviewed on #458: the `?action=` effect was flagged for reading `canManage` and `basePath` without listing them, on the theory that a cold load sees `canManage === false` before auth resolves and never retries. That path is not reachable -- `useAuth` keeps `isLoading` true until the PROFILE query resolves, and `ProtectedRoute` renders `FullPageLoading` until then, so the page cannot mount before `user.role` is settled. Both values are listed anyway (`b4ab814`): the effect's last act removes the `action` param, so an extra run returns at the guard, and the code no longer leans on that `ProtectedRoute` guarantee holding |
| G54 | Refuted: APP_PAGES role metadata does not disagree with the route guards | S | done | Reported as a registry drifting from `ProtectedRoute`, and recorded because the first check I wrote APPEARED to confirm it -- one hit on `/app/invitations`, which turned out to be my own regex pairing a path with a later route's `allowedRoles` because the route between them spelled its roles as an inline array rather than a named constant. Resolving both forms and splitting on route boundaries first: all 134 registered pages compared against 175 routes, zero pages offered to a role the route refuses. The check is worth keeping even though it found nothing -- nothing else stops someone widening `APP_PAGES.roles` without widening the route, and that produces a page the app offers and then refuses, which a user cannot tell apart from a broken permission. Added to `routeRegistration.test.ts` with a vacuity guard so it cannot pass by resolving no routes. The REVERSE direction is deliberately not asserted: a route admitting a role the page does not advertise is how two intentional postures are expressed (trainers are navigated to `/trainer/*` equivalents, platform_admin can open several `/app/*` operator surfaces that no admin menu lists) |
| G55 | Refuted: EmployeeCredentials does not save silently | S | done | Reported alongside the ResidentDetail discharge (G43), and worth recording separately because the two look alike and only one was real. Every mutation on this page carries an `onError` with a destructive toast -- create, update, delete, and document delete -- and the upload and download paths are `mutateAsync` inside try/catch with the same treatment. The `verified_at`/`verified_by_profile_id` re-stamp on save is also not the G45 defect it resembles: the code says what it is doing and why ("who last recorded a verification, and when", with prior values in `audit_logs`), which is a different column contract from BackgroundChecks' one-time attestation dates. Nothing changed |
| G56 | Six review findings on this PR, five of them defects I introduced | S | done | Codex review of #458. **P1, and the worst of them:** `BackgroundChecks` (G45) preserved the attestation `_at` while still writing the current user into `_by`, so the pair now DISAGREED -- an attestation made in March by one administrator was attributed to whoever edited the notes in August. That is worse than the bug it replaced, where actor and timestamp at least agreed. An attestation is one fact, who and when, so it is preserved, stamped, or cleared as a unit; a CHANGED suitability verdict now re-stamps rather than inheriting the old determination's actor. **`CorrectiveActionForm`** (G22.11): the ref that stops "Unassigned" being undone was never cleared, and all three call sites keep the form mounted -- so the first action of a visit defaulted to the filer and every one after it silently did not. `resetForCreate` re-applies the default directly, because the effect's dependencies do not change on a reset and it would never re-run. **`regulatoryCrosswalk`** (G27): excluding satisfied records from the gap count left `summarize`'s `?? sortedDates[0]` fallback reporting the oldest of those settled dates, so a row could read "inspection ready" beside a next-due date that had already gone by; a past date is only meaningful when something is outstanding. **`incidentAnalytics`** (G29): `daysSince` divided elapsed milliseconds by 86_400_000, which is not a calendar-day count -- across Pennsylvania's November fall-back an incident seven facility dates old measures 8, and the windows are inclusive `<= 7`, so it dropped out of the recency counts for about a week after every transition. Reproduced (8 vs 7 on the exact input) and now differences facility DATES read at UTC midnight, which has no zone in it. **`EmployeeLifecycleCases`** (G48): dropping the server-side `status` filter traded a correctness bug for a scale bug -- one unpaginated read capped at 1000 rows, with inactive records consuming the window. `useListEmployeesByIds` already exists for precisely this ("payload proportional to the work on screen"); the picker is server-filtered to active again and case labels are a targeted lookup of the ids on the page. **`offlineServiceDraftCache`** (G22.2): an unreadable draft was logged and omitted, while the header's count comes from the plaintext lane that still lists it -- so the panel read "N pending" above a row that never appeared, "Sync now" attempted nothing and reported success, and nothing cleared it before the purge deadline. My own comment called that "nothing is concealed", which was the wrong reading of the same fact. The ids come back with the drafts now and render as an unreadable row with the one action that still works on them -- dismissal needs no decryption |
| G57 | Second full-app review after #458: auth holes, fail-open gates, stranded publishes, and empty-success errors | L | done | Picked up where #458 left off. **`evaluate_learning_path`** accepted any caller whose `current_org_id` matched the assignment, so an ordinary employee could submit outcomes for a coworker; it also reevaluated completed assignments into contradictory state. Tightened to assignee / privileged roles / active-only (`20260805190000`). **`MfaPolicyGate` and `IdleSessionLock` failed open** while loading and after query errors — both now fail closed with retry/sign-out (enrollment route still reachable). **Policy publish** was two Data API writes; a failure between them stranded an immutable published version that was not current — `publish_policy_document_version` is one transaction. Assessment autosaves are serialized and finalization awaits the latest attempt (including a failed one). Customer PDFs no longer print raw `ALR`. Empty datetime fields no longer throw `RangeError`. End-of-day stamps use Pennsylvania via `facilityDateTimeToUtc`. Several surfaces that treated query errors as empty success now show `QueryError`. Auditors can no longer overwrite POC PDFs. Facility managers can delete compliance evidence they can upload (`20260805191000`). Portal revoke/share/reply dialogs clear state on close so one grant's draft cannot act on another. Follow-on: document delete no longer ignores Storage failures; support-ticket create rolls back the empty ticket when the first message fails; support ticket/message list views page past PostgREST max-rows; conversation query errors no longer look like an empty thread |
| G58 | Third-pass review: auditor packet writes, FM cross-facility grants, orphan regenerates, readiness fail-open | L | done | **Auth.** Auditors could `add`/`remove` survey packet items and `assemble` (audit side-effects) while storage INSERT and product copy treat them as read-only — dropped from those writers; `list` still admits them (`20260806010000`). Facility managers could issue/revoke survey packet guest grants and submit credential renewals for any facility in the org; both now require `is_assigned_to_facility`. Packet item add/remove likewise. **`save_learning_path_version`** took `max(version_number)+1` without a lock; advisory xact lock per definition serializes it. **Edge / storage.** POC regenerate updates the existing `poc` row instead of delete-then-insert. Survey packet packaging removes the ZIP when the export RPC fails and fails hard on signed-URL errors. Settings logo awaits the settings upsert and cleans up a new object key on failure. Support reply attachment cleanup surfaces remove failures like create. **Readiness / lists.** Training/credentials/incidents/corrective-actions/inspection-items page past PostgREST max-rows; InspectionReadiness treats loading and query errors as unknown, not Ready. **Calendar / classes.** Resident services datetime-local values go through `facilityDateTimeLocalToUtcIso`. Draft classes get Open for enrollment (`status=scheduled`) so SessionRoster is reachable. **Fourth pass.** `assert_can_manage_violation` (POC lifecycle) and `record_survey_evidence_packet_export` / packet list RPCs also lacked FM facility scope (`20260806020000`); packaging edge function checks assignment before service-role binder download; SCORM zip upload cleans up on register failure |
| G59 | Fifth-pass: more FM DEFINER writers, mock-inspection/copilot scope, facility datetime writes | L | done | **Auth (`20260806030000`).** `set_employee_checkin_pin`, `checkin_via_kiosk_pin`, `complete_training_class`, and `create_shift_report_entry` admitted any org FM (and trainers on class/shift paths) without `is_assigned_to_facility`, while sibling RLS / `generate_class_checkin_token` already required it. **Edge.** `run-mock-inspection` dropped auditor from the write allowlist and requires FM/trainer assignment before the service-role receipt; `compliance-copilot` mirrors voice-tools' FM assignment check so unassigned managers cannot mint facility-scoped runs from empty grounding. **Lists / TZ.** `useListPolicyAttestations` pages past max-rows and accepts `facilityId` (InspectionReadiness scoped). Complaints, incidents, resident appointments, and personal-fund transactions convert datetime-local via `facilityDateTimeLocalToUtcIso` |
| G60 | Sixth-pass: training/quiz FM scope, import facility map, FacilityDetail empty-on-error, facility TZ sweep | L | done | **Auth (`20260806040000`).** `ensure_training_requirement_record`, `grade_quiz_attempt`, and `get_quiz_review` required assignment like sibling RLS. **Imports.** Bulk employee/resident/incident/room validate maps facilities through `listImportFacilitiesForCaller` so FM cannot mark other-facility rows valid for the durable worker. **UX.** FacilityDetail surfaces `QueryError` for training/practicums/incidents/inspections instead of empty success. **TZ.** Remaining facility datetime-local writes (care delivery, clinical chart, change of condition, hospital return, emergency, med exceptions, agreements, admission tours, QAPI, work orders/items, maintenance, survey day, schedule overrides, time-off) use `facilityDateTimeLocalToUtcIso` |
| G61 | The drift checker read byte offsets as string indices and called the result deployment drift | S | done | `Deploy migrations` had been red on main since before #458 -- three files reported as CONTENT drift against the deployed ledger. Read the ledger directly (`supabase_migrations.schema_migrations` on the live project, the same Management API endpoint the checker uses) and the three are not one problem but two. **Two are real post-deploy comment edits**: strip `--` comments and collapse whitespace and `20260802080000` and `20260804200000` are byte-identical to their deployed statements (md5 `95701bcb...` / `5571b58d...`), so what runs is unchanged and they are recorded in `migration-content-allowlist.json` with those figures. **The third was never drift at all**, and neither were two of the entries already sitting in the allowlist. `supabaseStatementsJoined` reconstructs the form Supabase records at apply time by slicing the file at libpg-query's `stmt_location`/`stmt_len`. Those are UTF-8 BYTE offsets -- positions in the C string the parser saw -- and `String.prototype.slice` indexes UTF-16 code units. The two agree only while a file is pure ASCII, and diverge by one index for every extra byte before the statement, cumulatively: the three `·` in `20260804110000` shifted its closing `revoke ...;` / `grant ...` pair into `... anon; g` + `rant execute ...`, a difference that exists nowhere in the file. Slicing a `Buffer` by those offsets is the whole fix. It also cleared `20260704210000` and `20260801220000`, whose written allowlist exceptions turn out to have been describing this same artifact rather than any real divergence -- both now match the deployed ledger exactly, so the entries are removed. A `--self-test` fixture with four multi-byte characters ahead of three statements holds the line; under UTF-16 slicing it reassembles as `re`/`voke` and `gr`/`ant`, which is the failure this had been mistaking for drift. Worth keeping in view twice over: a gate that fabricates a failure spends the same credibility as one that misses a real one -- this one had been holding seven migrations out of production while pointing at an innocent file -- and an allowlist is where a fabricated failure goes to look permanent, since every entry is a written argument that the tool was right |
| G62 | Bulk alert resolve/dismiss threw after a successful write, and single resolve skipped audit/`resolved_at` | S | done | `bulk_update_alert_status` returns `{ idempotencyKey, total, …, results: [...] }`. `useBulkUpdateAlerts` cast that envelope to an array and called `.filter`, so every bulk action failed in the client after Postgres had already updated the rows -- optimism rolled back and the toast said failure while the audit trail said success. `parseBulkAlertStatusResult` reads the envelope (with a bare-array fallback). Separately `useUpdateAlert` still wrote `alerts` directly, so one-click resolve/dismiss skipped the RPC's `resolved_at` stamp and `alerts_bulk_status_updated` audit row that bulk already used; both hooks now share one RPC path |
| G63 | Inspection Readiness and Survey Day graded failed loads as Ready | M | done | Checklist chips collapsed `(data ?? [])` and treated zero outstanding as Ready while the query was still pending or had errored -- the worst answer a survey-prep page can give. Both pages now gate facility-scoped lists with `enabled`, return `unknown` when a source is unavailable, and surface a QueryError banner on Inspection Readiness. Entrance-conference copy also says Assisted Living Facilities, not residences |
| G64 | Always-mounted dialogs that could re-append or save stale state | S | done | `QapiConsiderationDialog` and `ReportabilityDialog` stayed mounted beside the already-keyed Investigation dialog; cancel left choice/note/rationale in place and a second open could re-append `not_indicated` notes or submit a stale project. Complaint interview/action/monitoring dialogs had the same shape. All re-key on open |
| G65 | Incident findings textarea could wipe a QAPI append on blur | S | done | IncidentDetail's uncontrolled Investigation findings field seeded once from `defaultValue`. Follow-through QAPI decisions APPEND into the same column; after that, blur compared against the live prop but wrote the stale textarea value back over the append. Re-keyed on the stored findings/root-cause strings so a server append remounts the field |
| G66 | Facility "today" defaults used UTC or browser-local day | S | done | `RegulatoryGovernanceCard` as-of and `CitationGovernanceSection` verified-on used `toISOString().slice(0,10)` / local-offset tricks; `DutyEligibilityCard` default expiry and DietaryOperations review-due mixed facility today with browser-local arithmetic. After ~20:00 ET those open on the wrong Pennsylvania day. All four now use `facilityToday()` (and calendar-day add for offsets) |
| G67 | High-stakes empty states that meant "fetch failed" | M | done | Occupancy board returned null on error; QAPI incident trends, support plans, hospital reconciliation, survey packet guest grants, copilot history, admission prospect activity, complaint activity/history, clinical FHIR meds/allergies/diagnoses, FacilityDetail training/practicum/incident/inspection cards, EmployeeDashboard competency/shifts, competency record checklist, credential documents, policy campaign roster, move-in history, and inspection work orders all rendered empty/zero copy without `isError`. Each now shows QueryError (or suppresses scoring) before empty |
| G68 | Compliance surfaces kept scoring after a documented load failure | S | done | OverviewTab still built the move-in readiness packet after QueryError; MedAdminRoster still answered "authorized today?" and safety metrics from empty data; AdministratorQualification still showed the rule-pack ready badge; RegulatoryCrosswalk still summarized and exported CSV on partial failure. Packet/metrics/badge/summary+export are now gated on source success |
| G69 | Learner progress and class delete hid the failure | S | done | TakeCourse's `upsertProgress` / `startAssignment` fired with no `onError`, so a lost checkpoint or stuck `assigned` status was invisible (and start could retry forever into `isError`). ClassDetail delete swallowed the catch body. Both surface the server message; start stops retrying once the mutation has errored |
| G70 | Compliance instance detail stuck on Loading, and notes rode between occurrences | S | done | `InstanceDetailDialog` treated a failed detail fetch the same as still loading (`!instance`), so the dialog never left "Loading…". The note textarea also survived across instance changes in the always-mounted dialog, so a note typed for one occurrence could be submitted against the next. Now shows QueryError and clears the note when `open`/`instanceId` change |
| G71 | Violation, work-order, and inspection-history panes still empty-masked errors | S | done | ViolationDetail corrective actions/documents, WorkOrderDetail repair docs/lifecycle history, and InspectionItemDetail event history only checked loading — a failed secondary fetch read as "none on file". Each now branches on `isError` before empty copy |
| G72 | More facility-day defaults and a QAPI recommendation that assumed projects loaded | S | done | Financial statement due (+15), QAPI project completion (+90), and licensing "due in 90 days" windows used browser-local/`toLocalIsoDate` arithmetic against server `pa_today()`. Incident-trends recommendations also treated a failed projects query as "no open projects". Defaults now use facility calendar-day math; recommendations wait on projects success and reset form fields on open |
| G73 | Shared `addFacilityCalendarDays` and remaining mixed-calendar defaults | S | done | QAPI metrics `ago()`, resident-services From/Through, and paid-training payroll start mixed browser-local arithmetic with facility-day ends. Centralized facility calendar-day add in `dateUtils` and switched those defaults over |
| G74 | Calendar agenda said empty while still loading; create dialog kept prior form | S | done | Resident Services Calendar treated undefined data as "no services match"; CreateEventDialog stayed mounted across opens. Loading gate + remount-on-open |
| G75 | Compliance templates empty-masked errors; archive/generate silent | S | done | Templates tab only checked loading; generate/archive mutations had no toasts. QueryError + success/error feedback |
| G76 | Work Queue / Documentation Room summary tiles showed 0 on fetch failure | S | done | `data ?? EMPTY_*` filled tiles with zeros while loading already used "—"; errors now also show "—" |
| G77 | Open-shifts and workforce queue cards still rendered empty on error | S | done | MySchedule open-shifts and QualifiedWorkforce decision cards showed "none pending" beside a page-level error. Skip empty copy when the queue/workspace query failed |
| G78 | Stale create-dialog defaults overnight and after cancel | S | done | Violations/CompetencyRecords baked `facilityToday()` into module constants; QAPI create and policy campaign dialogs kept fields after cancel. Fresh factories / reset-on-close |
| G79 | Compliance evidence delete could fail silently | S | done | InstanceDetailDialog `removeDoc.mutate` had no handlers while upload/transition toasted; now surfaces remove success/error |
| G80 | More facility-day defaults (QAPI escalate, Value Center staffing, complaint/QAPI action dues) | S | done | Incident QAPI escalate +90, Value Center staffing window, and complaint/QAPI owned-action due (+14) used browser-local arithmetic. All now use facility calendar-day helpers |
| G81 | QAPI project activity lists and mutates hid failures | S | done | Actions/measurements/meetings rendered from `activity.data?.…` with no `isError`; add/measure/meeting only toasted success. QueryError + onError toasts |
| G82 | Schedule publish/clear and setup unit/shift panels | S | done | Publish/unpublish/clear-auto-fill success-only; Units/Shifts panels empty-masked load errors and silent activate/delete. Errors toast; QueryError before empty |
| G83 | Summary tiles still zeroed on error across ops queues | S | done | Complaints, confidential incidents, change-of-condition, service delivery, guest access, and employee dashboard credential/attestation tiles used `isLoading ? "—" : value` only. Errors now show "—" too |
| G84 | Accounting exports and work-order doc delete | S | done | Resident financial exports tab passed `exports.data ?? []` on failure; work-order documentation delete had no toast. QueryError + delete feedback |
| G85 | Policy versions/campaigns empty-masked load errors | S | done | PolicyDocumentDetail Versions/Campaigns tabs only checked loading; failed lists read as "none yet". QueryError before empty |
| G86 | My Schedule upcoming shifts empty beside QueryError | S | done | Page-level shifts error still left the Upcoming card saying "No upcoming shifts published yet". Skip empty copy when `shiftsError` |
| G87 | Schedule patterns panel empty-masked errors / silent delete | S | done | PatternsPanel preferences used `(preferences ?? []).length === 0` with no `isError`; delete had no toast. QueryError + onError |
| G88 | Service Delivery alert acknowledge/resolve silent on failure | S | done | `resolveAlert.mutate` for acknowledge/resolve had no handlers; failures left alerts open with no feedback. Success/error toasts |
| G89 | Schedule Detail service-workload scored failed fetch as 0 gaps | S | done | `useScheduleServiceWorkload` only used `data`; pending/error showed "0 coverage gaps" and setup empty copy. Dash + QueryError / loading gate |
| G90 | Clinical disclosure consent now gates outbound paths | M | done | Counsel cleared Snapshot #4. `assert_clinical_disclosure_allowed` binds FHIR write-back enqueue/drain, organization export of clinical/FHIR resident rows, and designated-person portal document share/download (+ prep-instruction redaction). Charting stays ungated. Managers set posture via `set_resident_clinical_data_consent` on the clinical chart |
| G91 | Certification checklist load failure enabled Submit/Pass | S | done | `checklist.data ?? []` made outstanding empty on error so Submit/Approve stayed enabled. QueryError + disable until checklist loads |
| G92 | Resident care conflicts fetch failure read as all-clear | S | done | On care-header error, conflicts became `[]` and the panel showed green "No disagreements". Pass `isError` + QueryError before empty |
| G93 | Session roster failed load = nobody registered | S | done | After loading gate, `registrations.data ?? []` with no isError showed empty roster copy. QueryError card first |
| G94 | Campaign knowledge-check failed load = read-and-sign | S | done | `CampaignQuestions` treated error as zero questions. QueryError before empty |
| G95 | QAPI consideration project picker failed load = none exist | S | done | Projects error still claimed no facility project. QueryError + disable link-submit until load succeeds |
| G96 | Empty copy still beside QueryError (paths/revisions/DME) | S | done | AdaptivePaths, GovernedContentRevisions, DmeRegister showed "none yet" alongside QueryError. Require `!isError` for empty |
| G97 | HRIS staged rows failed load = validate first | S | done | Only isLoading checked; error showed "no staged rows yet". QueryError first |
| G98 | Acuity workload failed fetch vanished | S | done | `!data` returned null on error with no feedback. QueryError instead |
| G99 | Offline draft dismiss/sync silent on failure | S | done | UnsyncedDraftsPanel dismiss/sync mutateAsync had no onError toast. Surface failures |
| G100 | Shadow differences failed load = none recorded; portal expiry browser-local | S | done | RegulatoryGovernance empty-masked differences; ResidentPortalWorkspace defaultExpiry used browser local +30. QueryError + `addFacilityCalendarDays(facilityToday(), 30)` 
| G101 | Incident follow-through vanished on fetch failure | S | done | `if (!data?.incident) return null` with no isError hid the close-loop checklist. QueryError first |
| G102 | Certification versions empty-masked load failure | S | done | Versions error claimed "no published checklist". QueryError + disable Start until versions load |
| G103 | Retraining enroll dialog empty-masked class load | S | done | Failed classes query showed "No scheduled…". QueryError before empty |
| G104 | HRIS start-run source picker lied on load failure | S | done | Placeholder claimed no source configured when sources failed. QueryError + honest placeholder |
| G105 | My Schedule time-off empty on workspace error | S | done | Time-off card showed "No recent requests" while open-shifts already skipped empty on error. Mirror |
| G106 | Move-in guest links tile zeroed / list blank on error | S | done | Active guest links used `?? 0`; list only mapped data. Dash + QueryError |
| G107 | PCH/ALF ops, notification, security tiles zeroed on error | S | done | Portfolio/facility metrics, notification ops tiles, and governance tiles showed 0/v2 on failure. Show "—" |
| G108 | Silent mutates: shift ack, PM pause, announcements, admissions, finalize | S | done | MyShift acknowledge, Maintenance pause/resume, Announcements mark-seen, Admission activity/referral source, assessment finalize now toast onError |
| G109 | Guest portals accept/sign silent on failure | S | done | Move-in and resident-agreement guest portals only handled onSuccess. Toast accept/sign/respond errors |
| G110 | Employee search empty copy beside load error | S | done | `!isLoading && length===0` still said "No employees match" when query failed (error text already shown). Require `!isError` |
| G111 | State Forms Center empty⇒"all current" on load failure | S | done | QueryError banner existed but tiles still showed zeros and Needs Action rendered green CheckCircle "All state forms are current". Dash tiles + skip empty-success while `queueFailure` |
| G112 | Complaint closure readiness fail-open on activity error | S | done | `activity.data?.actions ?? []` made failed activity load score "Corrective actions complete". Unknown (unavailable) until activity loads |
| G113 | Admissions metrics/census empty-on-error; silent bed hold | S | done | Metric strip always `(data ?? [])` zeros; census tab mapped with no `isError`; `setBed.mutate` only onSuccess. Dash + QueryError + onError toast |
| G114 | Resident Needs Attention all-clear when care header fails | S | done | `careHeader.data` falsy ⇒ `cards=[]` ⇒ green "Nothing open…". Panel takes `isError`/`onRetry` |
| G115 | Credential summary tiles zeroed on list error | S | done | List had QueryError; Expired/Due soon/Missing/Gaps still showed 0. Show "—" while loading/error |
| G116 | Facility datetime-local defaults still browser-zone | M | done | Emergency ops/event detail, hospital return, WorkItemDetail due display used browser `datetime-local` then `facilityDateTimeLocalToUtcIso`. Added `toFacilityDateTimeLocal` / `facilityYear`; wire callers |
| G117 | Silent agreement revoke / copy delivery; stale share dialog | S | done | `revokeGrant` and `markCopy` lacked onError; External link reopen kept prior guest versions. Toast + reset on open |
| G118 | Designated-person schedule respond + offline remove silent | S | done | Portal Confirm/Change/Cannot-attend and MyCourses single offline remove had no failure UI. Error text / toast |
| G119 | Employee practicum empty-on-error; practicum year browser/UTC | S | done | Annual Practicum card said "No practicum record" on fetch failure; year used `getFullYear()`. QueryError + `facilityYear()` (RetrainingMonitor too) |
| G120 | Evidence Room create dialog kept prior name/purpose | S | done | Cancel left form state; reopen showed last draft. Reset on open + remount key |
| G121 | Journey-end filter Selects failed critical a11y (e2e) | S | done | Org-admin Guest Access and facility-manager Confidential Reports axe scans failed on unlabeled `SelectTrigger`s. `aria-label` on those filters; guest grants-in-view tile also dashes on error |
| G122 | Incident occurred-at default was browser-local | S | done | `toLocalDatetimeInputValue` used browser wall clock then `facilityDateTimeLocalToUtcIso`. Replaced with `toFacilityDateTimeLocal` |
| G123 | Incident summary tiles zeroed on load error | S | done | `data ?? EMPTY` showed Open/Major/Recent as 0 and "No open incidents" beside list QueryError. Dash while loading/error |
| G124 | Clinical observation defaults were browser-local | S | done | ResidentClinicalChart + MyResidentChart `observedAt` used `toDateTimeLocal`; now `toFacilityDateTimeLocal` |
| G125 | Work-order / services / schedule facility datetime round-trips | M | done | WorkOrderDetail target load, ResidentServicesCalendar create/reschedule/follow-up defaults, ScheduleDetail override expiry use facility helpers |
| G126 | Agreement / funds / med-exception / complaint / QAPI datetime defaults | M | done | Same browser→facility ISO mismatch on agreement effectiveAt, personal-funds txn time, med-exception due, complaint interview/monitoring/due, QAPI held/aDue |
| G127 | Silent shift delete on ScheduleDetail | S | done | `deleteAssignment.mutate` only onSuccess; now toasts onError |
| G128 | High-traffic filter Selects lacked accessible names | S | done | aria-label on Incidents, Residents, Employees, EvidenceRoom, Credentials, Alerts, Maintenance, MyCourses, StateForms, Emergency facility |
| G129 | Emergency ops dialogs kept prior draft after cancel | S | done | `openDialog` resets plan/event/resource/inventory/assignment fields before open |
| G130 | Portal payment-link dialog kept prior draft | S | done | Reset provider/url/amount/expiry on open |
| G131 | CoC / create-complaint / rights-ack datetime still browser-zone | M | done | `LogChangeOfConditionDialog`, `CreateComplaintDialog`, `ResidentAdministrativeMaster` now use `toFacilityDateTimeLocal` before `facilityDateTimeLocalToUtcIso` |
| G132 | Practicum year still browser `getFullYear()` | S | done | `useMedAdminAuthorization`, `MedAdminRoster`, `Practicums` use `facilityYear()` |
| G133 | Maintenance create dialogs stale; locations empty-on-error | S | done | Reset order/location/schedule forms on open; locations tab QueryError + loading gate |
| G134 | Work-order transition/verify dialogs kept downtime & notes | S | done | Clear downtime + verification fields when opening transition/verify |
| G135 | CCC / Dashboard / Emergency / InspectionReadiness metric fail-open | S | done | CCC StatCards dash while loading; Dashboard score null until data (not 100%); Emergency tiles dash while loading; special-care card QueryError on source failure |
| G136 | Remaining unlabeled filter Selects (a11y) | S | done | aria-label on TrainingMatrix, CCC, Documents, CourseAssignments, Violations, MyTrainings, MedAdminRoster, PendingApprovals, Practicums, BackgroundChecks, CompetencyRecords, TrainerClasses, Users, ComplianceBinder, ValueCenter, SurveyDay |
| G137 | Shift wall-clock still browser-zone for override/handoff | M | done | `ScheduleDetail` override expiry and `MyShift` handoff period use facility date+time → `facilityDateTimeLocalToUtcIso` (overnight via `addFacilityCalendarDays`) |
| G138 | Floor / My Residents task queue used browser midnight | S | done | Both use `facilityDayBounds(facilityToday())` for the queue window |
| G139 | Break-glass + release-cohort expiry browser-local | S | done | `BreakGlassCard` defaults/submit via facility datetime helpers; cohort date-only expiry uses facility `T23:59` |
| G140 | Financial / export / policy create dialogs stale on reopen | S | done | Receivables Rate/Entry/Monthly/Statement, FundOpen/Reconcile, ExportDialog reset on open; PolicyDocuments clears on close |
| G141 | Med-exception due sticky; QAPI measure/meeting/action forms stick | S | done | Reset due on Review/assign success; clear QAPI measurement, meeting, and action drafts on success |
| G142 | More unlabeled operational filter Selects | S | done | AdmissionOperations, ServiceDelivery, ChangeOfConditionQueue, Dietary, QAPI, Inspection*, PCH/ALF ops, lifecycle/import/crosswalk/exclusion filters + QAPI form Selects |
| G143 | Services calendar agenda grouped by browser day | M | done | Group + day headers use `facilityToday` / `formatDateForDisplay`; filter Choices get aria-labels |
| G144 | Adaptive path + guest-link expiry used UTC/ms arithmetic | M | done | Path due via `facilityDayBounds().through`; move-in + agreement guest links use facility calendar EOD |
| G145 | Support-plan activation / oneYearOut browser calendar | S | done | `isActivationOverdue` compares facility `YYYY-MM-DD`; `oneYearOut` uses `addFacilityCalendarDays` |
| G146 | Resident overview empty-on-error under failure banner | S | done | Informal supports error copy; admin master tiles/lists take `dataUnavailable` instead of zero/none |
| G147 | Stale create dialogs: FHIR, move-in guest, care plan, incident follow-through, admin master add | S | done | Reset on open; incident reportability/investigation/QAPI dialogs rehydrate when reopened |
| G148 | Complaint trends + remaining unlabeled admin/ops Selects | S | done | Trends window via `addFacilityCalendarDays`; aria-label on Organizations/SupportTickets/AiLog/NotificationDeliveries, ResidentCompliance, Reports, EmergencyEventDetail |
| G149 | Evidence / surveyor guest grant expiry used browser `Date.now()+Nd` | M | done | Collection + Survey Day packet grants expire at facility EOD via `addFacilityCalendarDays` / `facilityDayBounds` |
| G150 | Announcements / workforce / enterprise datetime-local → browser ISO | M | done | Announcement expiry, schedule-eligibility starts/ends, billing override expiry use `facilityDateTimeLocalToUtcIso` |
| G151 | Today / InspectionReadiness / incident trends / audit / lifecycle calendar bounds | M | done | Work-item `dueBefore`, oneYearAgo, trends window, audit date filters, archive range use facility day/range bounds |
| G152 | Support-ticket age used UTC `T23:59:59Z` | M | done | `supportTicketAnalytics.ageDays` matches incident calendar-day math via `facilityToday` |
| G153 | IncidentDetail silent status / findings / notifications / final report | S | done | `updateIncident` / `addNotification` / `completeNotification` toast onError; clear drafts only on success |
| G154 | SupportTickets tiles / open count fail-open to 0 | S | done | StatCards and header open-count show "—" / unavailable while loading or on error |
| G155 | InspectionItems bulk-log dialog + unlabeled complaint/credential filters | S | done | Reset bulk notes/result on open; aria-label on Complaints facility/status/category and CredentialRenewalInbox status |
| G156 | WorkQueue / audit export / entitlement term / API credential calendar windows | M | done | Due filter, audit default 30d window, package term from/to, integration credential 90d EOD use facility bounds |
| G157 | PendingApprovals age cutoff / war-room due / duty override max length | M | done | 90-day hide cutoff, ValueCenter request due (+2d EOD), duty max expiry vs facility calendar year |
| G158 | Lifecycle / dietary / service-delivery metric fail-open | S | done | Lifecycle tiles dash while loading/holds error; dietary tabs wait for load; service tab counts dash on load/error |
| G159 | EmployeeDetail onboarding mark-done silent mutate | S | done | `updateOnboardingItem` toasts onError |
| G160 | Remaining unlabeled Selects (Reports/compare/census/DataTable/dietary/CAPA) | S | done | Mobile report category, support-plan from/to, census resident/status, rows-per-page, corrective status; dietary Field→Choice aria-label |
| G161 | Home metrics due-today / due-week used browser EOD + ms | M | done | `buildHomeMetrics` uses facility day/range `through` bounds |
| G162 | Reports default date window + ScheduleDetail override fallback | M | done | Default −12/+6 months via facility calendar; override fallback tomorrow facility `T23:59` |
| G163 | IncidentDetail add-staff silent mutate | S | done | Toast onError; clear employee picker only on success |
| G164 | Stale dialogs: inspection log, work-order edit, bulk invite, attendees, PoC verify | S | done | Reset on open/close for each surface |
| G165 | Unlabeled Selects: EmployeeSearchSelect / Documents upload / ticket detail | S | done | aria-label when filter label empty; upload facility/employee/type; ticket status/priority |
| G166 | Administrator CE window / meal lookback / citation staleness browser calendar | M | done | Rolling CE cutoff, meal `served_at` lookback, citation `isStale`/future-check use facility day math |
| G167 | AdminDashboard / ReleaseFlags / MyCourses metric fail-open | S | done | Operating Priorities + flag/kill tiles dash on load/error; offline library QueryError + dash count |
| G168 | HelpCenter ticket + investigation approve dialog drafts sticky | S | done | Reset ticket form on open/cancel; clear approve note on open/close |
| G169 | Unlabeled Selects: emergency activate / incident staff / Users role / assign | S | done | Event mode/type, staff/involvement/notification/retrain, role-for-name, assign-staff; HelpCenter category/priority |
| G170 | Residents / Maintenance summary tiles fail-open to 0 | S | done | Dash while summary/orders/schedules loading or on error |
| G171 | Care-level review + compliance CCC used browser `daysUntil` | M | done | Rate-in-force / overdue / due-soon via `facilityDaysUntil` (+ facility-stable test `now`) |
| G172 | Unlabeled Selects: incident create / violation retrain / emergency ops / CoC | S | done | Create-form staff/notification/resident; violation retrain; resource/inventory/assignment; CoC party/status |
| G173 | Unlabeled Selects: work-item / schedule pattern / admissions / pending approvals | S | done | Dependency + status; pattern shift/unit; activity type + bed; manual employee/training type |
| G174 | License due-tone + acuity settling used browser ms days | M | done | `facilityDaysUntil` / facility day `daysSince` for license badges and acuity window |
| G175 | AdminDashboard org tiles + urgent badge fail-open to 0 / Ready | S | done | Dash org/credential/training counts while health/dashboard loading or error; badge says Metrics unavailable |
| G176 | Field/Choice unlabeled in licensing, financial ops, services calendar | S | done | Clone aria-label onto Choice/Input; Choice SelectTrigger accepts aria-label |
| G177 | Remaining unlabeled Selects (comms, templates, survey, assessment…) | S | done | Emergency audience/delivery/channel; notification channel/org; package; task owner; disposition; checklist; assessment degree/freq/party; class picker; portal request type; etc. |
| G178 | Administrator rule pack CE/due used UTC `addDays`/`daysBetween` | M | done | `addFacilityCalendarDays` + `facilityDaysUntil` for CE window and due-soon |
| G179 | Investigation overdue / citation age / care-profile age ms math | M | done | Facility calendar age for incident trends, citation library, care header staleness |
| G180 | Grant age / DME inspection / analytics `daysUntil` browser math | M | done | Enterprise grant label, DME inspection due, course/credential/compliance/state-form analytics |
| G181 | CompetencyRecords create dialog sticky drafts | S | done | Reset form/employee/item results on dialog close |
| G182 | Needs-attention / care-conflicts / change-detection ms windows | M | done | Facility day age + rolling windows for falls, assistance, change signals |
| G183 | Receivable aging + MyCertificates expiry browser math | M | done | `facilityDaysUntil` for AR days-past-due and certificate expired badge |
| G184 | Incident/support analytics age still used UTC midnight ms | S | done | Delegate to `facilityDaysUntil` (DST-safe facility calendar) |
| G185 | Incidents / Courses / Residents create dialogs sticky | S | done | Reset form to empty on dialog close |
| G186 | Time-off window error used browser `Date` for datetime-local | M | done | Validate start/end via `facilityDateTimeLocalToUtcIso` instants |
| G187 | Sticky create/edit dialogs across admin/app surfaces | S | done | Reset form (+ edit id) on close for TrainingTypes/credentials/violations/inspection items/facilities/templates/assignments/orgs/users/employee detail/facility detail/plans/packages/quiz |
| G188 | Remaining sticky dialogs: maintenance / trainer / PIN / WO | S | done | Reset order/location/schedule forms, trainer class form, PIN, quiz meta, WO transition/verify drafts on close |
| G189 | Sticky agreement share / copy-delivery / report save / guest expiry / exception / unpublish drafts | S | done | Reset signer role, delivery method, save-view name, expiresDays, Cancel reason, unpublish reason on close |
| G190 | Employee due-tone + formatDueDistance / readiness window still browser-local | M | done | `facilityDaysUntil` for employee pages; `formatDueDistance` + `isWithinWindow` on facility calendar |
| G191 | RetrainingMonitor / SystemJobs / DocumentAnalyzer / MedAdminRoster metric fail-open | S | done | Dash ("—") tiles while loading/error; med-safety body waits for load |
| G192 | Relationship QuickFill unlabeled + sidebar pin silent mutate | S | done | `aria-label` on participant relationship QuickFill; toast pin failures |
| G193 | InspectionReadiness special-care tiles fail-open while loading | S | done | Dash counts + Loading badge until units/residents/prefs/training resolve |
| G194 | FacilityDetail special-care card fail-open while loading | S | done | Dash gap count + Loading/Unavailable until units/residents/prefs/training resolve |
| G195 | Special-care summaries ignored training-types load/error | S | done | Include `useListTrainingTypes` in InspectionReadiness + FacilityDetail busy/error gates |
| G196 | CourseAssignments summary tiles fail-open while loading | S | done | Dash completion/overdue/due-soon tiles until paginated page resolves |
| G197 | RegulatoryCrosswalk summary tiles fail-open while loading | S | done | Dash ready/attention/missing/overdue until evidence sources resolve |
| G198 | ScheduleDetail coverage/PPD tiles fail-open on empty assignments | S | done | Dash snapshot + staffing calculator while assignments load/error |
| G199 | Admission pipeline funnel revenue fail-open to $0 | S | done | `PipelineFunnelSection` dashes funnel counts + revenue when prospects busy |
| G200 | MoveIn guest sign + RASP participation/signature sticky drafts | S | done | Reset signer/participation/signature fields on open and close |
| G201 | PolicyDocumentDetail lifecycle tiles fail-open while loading | S | done | Dash draft/campaign/attestation counts until versions/campaigns/attestations resolve |
| G202 | FacilityDetail admin rule pack ignored CE-entry load/error | S | done | Skeleton/Unavailable until profiles and CE entries both resolve |
| G203 | AdministratorQualification badge/rules fail-open while loading | S | done | Loading badge + hold requirement rows until profile/CE/facilities resolve |
| G204 | Dashboard state-forms banner hid open work while loading | S | done | Show checking banner; only surface counts after residents + items resolve |
| G205 | ResidentServicesCalendar follow-up Choices unlabeled | S | done | `aria-label` on follow-up owner/priority (and related draft inputs) |
| G206 | ClassDetail attendees count fail-open while roster loads | S | done | Dash header count + skeleton body until attendees resolve |
| G207 | EvidenceCollectionDetail empty copy while secondary loads | S | done | Loading copy for artifacts/grants/events before empty states |
| G208 | WorkOrderDetail documents empty while loading | S | done | Skeleton before "No repair documentation" empty state |
| G209 | CourseDetail ratings fail-open to "No ratings yet" | S | done | Loading ratings copy until feedback query resolves |
| G210 | ScheduleDetail PPD/census used empty residents while loading | S | done | Don't sync census to 0 while residents load; dash PPD tiles |
| G211 | StateFormsCenter tiles/empty states ignored residents load | S | done | `queueBusy` includes residents/facilities; skeleton renewals while busy |
| G212 | AdminDashboard open support tickets used dashboard `?? 0` | S | done | Dash ticket count in System Health while dashboard page busy |
| G213 | Secondary empty-state fail-open (MoveIn/Service/Complaint/QAPI/Clinical) | S | done | Loading copy/skeleton before "No X yet" for history, alerts, activity, projects, allergies/diagnoses |
| G214 | AdminDashboard Domain Review badges used health/dashboard while busy | S | done | Dash Tenant/People/Training/Compliance/Support card statuses while queries busy |
| G215 | EmergencyOperations + ResidentCareDocumentation empty while loading | S | done | Loading copy/skeleton before events/plan/assessments/care-plan empty states |
| G216 | WorkItemDetail activity fail-open (evidence/deps/approve) | S | done | `activityReady`; loading copy; hold approve until activity resolves |
| G217 | ChangeOfConditionDetail activity fail-open (monitor/close) | S | done | `activityReady`; loading copy; hold close until follow-ups resolve |
| G218 | Secondary empty-state fail-open (binder/ledger/WO/Settings/etc.) | S | done | Loading gates for binder exports, access ledger, linked WOs, deliveries, WO history, schedule patterns, CE roster, CopyTemplate facilities, promotable exports |
| G219 | MyAttestations/Credentials header counts fail-open to 0 | S | done | Dash title counts while employee/list queries load |
| G220 | Silent mutates: CE delete + inspection notes blur | S | done | Toast on CE delete failure/success; notes blur onError toast |
| G221 | AdmissionOperations list empties while queries load | S | done | Loading copy before prospects/beds/move-ins/census/history empty states |
| G222 | BreakGlass + FHIR/Med credential helpers fail-open | S | done | Loading copy before break-glass empty and credential helper empties |
| G223 | IncidentTrends recommended QAPI while projects load | S | done | Hold recommendations empty copy until existing projects resolve |
| G224 | Sticky drafts: clinical observation + portal create access | S | done | Reseed observedAt/expiry (and create fields) when open buttons fire |
| G225 | MedAdminRoster empty/count while employees load | S | done | `rosterBusy` loading copy + dash description until employees resolve |
| G226 | Employee-picker dialogs fail-open while roster loads | S | done | Loading copy in TrainingMatrix batch, TrainingPlans apply, Policy assign, Class add-attendees |
| G227 | ScheduleSetup workload profiles empty while loading | S | done | Loading copy before "No qualification…" empty state |
| G228 | ChangeOfCondition activity error showed empty monitoring | S | done | QueryError for monitoring/history/follow-ups on activity failure |
| G229 | IntegrationRegister credentials/webhooks empty while loading | S | done | Loading copy before "None issued/created" empties |
| G230 | ReleaseCohort membership + cohort picker fail-open | S | done | Loading membership copy; loading/disabled cohort Select items |
| G231 | PackageEntitlementTermCard open terms empty while loading | S | done | Loading copy before "No open terms" |
| G232 | AdaptivePaths assign Select + IncidentQapiEscalation link | S | done | Loading employee Select items; hold Escalate until QAPI link check resolves |
| G233 | PipelineFunnel referral sources empty while busy | S | done | Loading copy before "No inquiries in this view" |
| G234 | ScheduleSetup patterns + CourseAssignments employee pickers | S | done | Loading copy before "No active employees" empties |
| G235 | AdminDashboard Phase nested empties + task backlog badges | S | done | Hold readiness/timeline/hotspots empties on dashboardBusy; dash task badges on healthBusy |
| G236 | Dashboard alerts/tiles/facilities fail-open while summary loads | S | done | Loading copy for recent alerts/facilities; dash Active Staff/alerts/Med Admin tiles |
| G237 | ResidentServicesCalendar vehicles empty + OutcomeDialog draft | S | done | Vehicles loading/error gate; reseed follow-up draft when outcome dialog opens |
| G238 | Empty-on-error after load gates (admin/evidence/billing/learning) | S | done | QueryError/disabled Select on isError for BreakGlass, IntegrationRegister, ReleaseCohort, EvidenceCollection secondaries, PackageEntitlement, AdaptivePaths |
| G239 | OfflineServiceDeviceCard remove fail-open while drafts load | S | done | Treat draft queries busy as blocked before Remove this device |
| G240 | Settings deliveries + CourseAssignments progress empty-on-error | S | done | QueryError before empty success copy |
| G241 | QuizBuilder attempts badge + silent answer mutates | S | done | Loading badge while stats busy; toast on answer blur/clear-correct |
| G242 | QualifiedWorkforce HRIS run picker fail-open | S | done | Loading/error gate before "No runs yet" input |
| G243 | EmployeeDetail secondary panels empty-on-error | S | done | QueryError for assignments/onboarding/training/practicums/hours/docs/credentials/activity |
| G244 | OrgDetail facilities + Evidence promotable + ScheduleSetup errors | S | done | QueryError/disabled Select on isError for facilities, promotable exports, profiles, roster |
| G245 | MySchedule swap candidates empty-on-error | S | done | Suppress "No candidate shifts" while candidates.isError |
| G246 | GovernedContent register/author Selects fail-open | S | done | Loading/error Select items; hold author snapshot while blocks busy/error |
| G247 | SessionRosterCard employee picker fail-open | S | done | Pass employeesLoading/Error from ClassDetail; gate "Everyone registered" |
| G248 | TrainerClasses attendee counts fail-open to 0 | S | done | Dash ("—") while `useClassAttendeeCounts` loading or error |
| G249 | TrainingPlans progress + items empty-on-error | S | done | QueryError before "hasn't been applied" / "No items" empties |
| G250 | ReleaseFlags table empty-on-error | S | done | Suppress "No release flags" while flagsQ.isError (banner alone was not enough) |
| G251 | AdministratorQualification CE badge/list empty-on-error | S | done | Dash CE hours badge; QueryError before "No CE entries" |
| G252 | QuizBuilder questions + answers empty-on-error | S | done | QueryError for questions; error copy instead of "No answer choices" |
| G253 | Employee-picker dialogs empty-on-error | S | done | QueryError before empty roster in Policy/TrainingMatrix/Plans/Assignments/ClassDetail |
| G254 | CopyTemplate facilities + DME history empty-on-error | S | done | QueryError before "No facilities" / "No events recorded" |
| G255 | CourseDetail ratings/versions/blocks empty-on-error | S | done | Error copy/QueryError before "No ratings/versions/blocks yet" |
| G256 | CourseAssignments assign-facility Select mislabeled | S | done | Dedicated aria-label; stop reusing Employees Label htmlFor |
| G257 | Incidents/Complaint/hospital-return facility TZ compares | S | done | Future/monitoring/return-before-departure use facilityDateTimeLocalToUtcIso |
| G258 | WorkItemDetail activity panels empty-on-error | S | done | QueryError for evidence/deps/comments/watchers; deps also wait for load |
| G259 | ValueCenter referral Empty outside admissions gate | S | done | Loading/QueryError before "No referral-source activity yet" |
| G260 | Header notifications empty-on-error | S | done | Retry copy instead of "You're all caught up" on isError |
| G261 | Resident Needs Attention secondary fail-open | S | done | Gate panel on items/docs/appointments/exceptions/etc. errors, not only careHeader |
| G262 | Employee portal-access badge fail-open | S | done | Unavailable copy while accessActive/now isError (not "suspended") |
| G263 | Support plan proposals + acknowledgments empty-on-error | S | done | QueryError/hold ack CTA before empty success |
| G264 | Survey Day packet items/exports empty-on-error | S | done | QueryError for selected evidence + packages; hold assemble/package |
| G265 | AuditLog open-ended manifest ends at export instant | S | done | Default `to` is now(), not upcoming facility midnight (checksum interval) |
| G266 | ResidentServicesCalendar agenda times in facility TZ | S | done | `formatFacilityTimeForDisplay` under Eastern day headings |
| G267 | InvestigationStepDialog no longer resets on parent refetch | S | done | Mount-only init; rely on open remount key |
| G268 | Support-plan annual review leap-year safe | S | done | `addFacilityCalendarYears` instead of +365 days |
| G269 | A critical job failed hourly for weeks and the watchdog called it healthy | S | done | **`billing-quantity-sync` is registered `is_critical` with a 3-hour freshness SLA and had never once succeeded.** Two independent reasons, both here. **The critical definition had no cron name.** `run_system_job_watchdog()` iterates `where is_active and is_critical and cron_job_name is not null`, and two rows described the one job -- `billing-quantity-sync` (critical, `cron_job_name` null) and `billing-quantity-sync-cron` (carried the cron name, not critical). The split fell exactly across the watchdog's two filters, so neither half was iterated; every other edge-function job carries both on a single row. **And pg_cron "succeeded" is not the job succeeding.** The resolver took `greatest(own_success, cron_success)`, where `cron_success` is the newest `cron.job_run_details` success. For the 13 cron entries whose command is a `net.http_post` to an Edge Function that status records that the *request was enqueued*, not that the function did the work -- so `last_success_at` advanced every hour while the function returned 503 every hour, and the SLA could never be breached. Same failure mode `20260726250000` was written to close, one layer down: there the job was invisible for having no definition row, here because its health signal measured the wrong thing. The critical definition now owns the cron name (the duplicate is removed, guarded by its own run history since the FK is `ON DELETE RESTRICT`), and the watchdog credits the cron-side signal only for `sql_cron`, where the scheduled command *is* the work -- `edge_cron` and `worker` must prove success with a finished `system_job_runs` row. The handler also records the misconfiguration instead of returning 503 into the void. Blast radius is three rows: `notification-dispatch` unchanged (thousands of recent successes), `billing-quantity-sync` stale as intended, and `exclusion-screening` newly stale -- not a regression, a second critical job read off the same wrong sensor, and it needs its own look |
| G270 | Six more critical jobs still read their health off pg_cron's exit status | M | open | **The residual half of G269, and it is the larger half.** G269 stopped the watchdog trusting `cron.job_run_details` for `edge_cron` and `worker` definitions, because for a cron entry whose command is a `net.http_post` the exit status reports that the request was enqueued, not that the work happened. It keys that decision on `execution_kind` -- which is a label, and six critical definitions are labelled `sql_cron` while their cron commands are `net.http_post` calls to Edge Functions: `certificate-pdf-jobs`, `binder-export-jobs`, `document-analyzer-jobs`, `organization-export-jobs`, `data-lifecycle`, and `integration-webhook-dispatch-cron`. Verified against production: all six have zero rows in `app_private.system_job_runs` under their own job_key, so pg_cron is the only thing feeding their freshness and they would read healthy through exactly the outage G269 describes. Most are not silently broken today -- a sibling `worker` definition (`certificate-pdf-generation`, `binder-export-generation`, `document-analyzer-extraction`, `organization-data-export`, `integration-webhook-dispatch`) records the real runs -- but those siblings carry `cron_job_name = null`, so the row doing the observing and the row being observed are different rows again, which is the same split that hid the billing outage. **Deliberately not fixed in #469**: correcting it means re-pointing which definition owns each cron entry across certificates, binder exports, document analysis, data exports, retention and integration webhooks, several of which would start paging immediately, and that is a care- and compliance-critical alerting change that deserves its own verification rather than riding along with a billing fix. Close it by making each pair's watched row the one that records completion, the way `billing-quantity-sync` now is |

## Explicitly not now

| Item | Why |
| --- | --- |
| Capability bundles / config release envelope | Enterprise; post-portfolio |
| Vendor external portal | Until maintenance is the top customer pain |
| Full Spanish i18n retrofit | After SMS + mobile proven |
| Multi-state rule packs | Finish Pennsylvania install → activate first, then decide where expansion actually matters |
| Expanding Essentials/Pro SKUs | Need conversion data |
| Competing on pharmacy eMAR network | Multi-year moat elsewhere |
| New root "comprehensive review" markdown | Update **this** file instead; the check rejects it |

---

## Sequence

Single-threaded, because there is one person. This is an order, not a schedule — dates
would be fiction. Do not start the next block until the previous one is actually done.

**1. Live truth.** ~~SG-1~~ closed — A1, A2, A5 remain (ops-only).
A real signup now gets full functionality the moment it signs up — invite staff, complete
a course, export a binder, and *receive real email/SMS* — with no separate pilot-enrollment
step in the way. What is left in this block is ordinary launch operations (live Stripe
pricing, a signed BAA), not a product gate.

**2. Wire up what is already built.** ~~B1~~, B3, ~~B5~~, ~~D3~~, ~~D5~~.
Each is a half-built row: the code exists, no surface calls it. B1, B5, D3, and D5 are now
wired and done. B3 remains (real vendor packages — owner drop-path). This is the cheapest
block on the list and the one most likely to be skipped, because none of it looks like
progress. Doing it before new features is how the pile stops growing. (C2 was the fifth
and is now closed.)

**3. Finish SG-2 install → activate.**
Counsel cleared option 2 and the PA personnel templates are seeded, with the install UI wired
in Enterprise Foundation. Next step is not more debate; it is to install a PA draft, complete
guarded review and fixture verification, and activate a governed version -- activation no
longer waits on a live-shadow period (2026-08-04). Until that evidence exists, the copilot
remains a drafting aid for Pennsylvania.

**4. Product depth.** ~~B4~~ done, ~~E1~~ done, ~~C5~~ done.
C5 is complete: first-class `citation_ref` on packet items, RPC/UI wired, regulation order prefers
structured citation then label parse. Only once 1–3 are settled for live use.

**Deliberately not in this list:** A5 (BAAs), because it depends on someone outside the
repo. Start it early precisely because it is the only thing here that can wait on another
person's calendar.

---

## Verification contract for any row marked `done`

1. Code on `main` (or a merged PR linked in the row notes)
2. Relevant unit / edge / e2e tests pass in CI
3. If user-visible: real or demo org exercise recorded
4. This file updated in the same change set — enforced by `check:planning-registers`

A row is `in_progress`, not `done`, when the mechanism exists but nothing calls it. This is
the most common way this register goes wrong: #355 produced four such rows at the time
(B1, B3, C2, D3), and the very next commit added a fifth by shipping
`QuarantinePackageDialog.tsx` with no importer (B5). Of those, B1, C2, and D3 have since
been finished. Recording built-but-unreachable code as `done` is how a backlog stops
describing the product.

Ops-only rows close when runbook evidence exists outside the repo (do not commit customer
data). "Ops-only" means a different hat, not a different person — see "Operating reality"
above.

**On self-review.** Every gate in this list is one person checking their own work, so the
checks that do not depend on a second reader are the ones carrying real weight: CI, the
pgTAP suite, `check:all`, and this register's own freshness check. Treat a mechanical gate
that a second account merely unlocked (`approve_regulatory_rule_version`) as unverified,
and say so in the row rather than counting it as review.

<!-- Register verified: SG-2 counsel-cleared option 2; templates seeded; activation remains; C5 citation_ref done; B1 done; B3 owner drop-path documented; F6/F7/F8 done; SG-3 opened (video_watch_gate global-enable vs. non-prod Storage gap) -->
