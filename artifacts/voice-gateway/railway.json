{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "RAILPACK",
    "buildCommand": "pnpm install --frozen-lockfile --prod=false && pnpm --filter @workspace/voice-gateway run typecheck && pnpm --filter @workspace/voice-gateway run build",
    "watchPatterns": [
      "artifacts/voice-gateway/**",
      "package.json",
      "pnpm-lock.yaml",
      "pnpm-workspace.yaml",
      "tsconfig.base.json",
      ".node-version",
      ".nvmrc",
      ".npmrc"
    ]
  },
  "deploy": {
    "startCommand": "pnpm --filter @workspace/voice-gateway run start",
    "healthcheckPath": "/health",
    "healthcheckTimeout": 100,
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
