import{useState}from"react";import{Link}from"wouter";import{BarChart3,ChevronRight,Plus,Target}from"lucide-react";import{useAuth}from"@/lib/auth";import{useViewingOrg}from"@/lib/viewingOrg";import{useListFacilities}from"@/hooks/useFacilities";import{useListProfiles}from"@/hooks/useProfiles";import{useCreateQapiProject,useListQapiProjects,useQapiSourceMetrics}from"@/hooks/useQapi";import{Button}from"@/components/ui/button";import{Card,CardContent,CardDescription,CardHeader,CardTitle}from"@/components/ui/card";import{Dialog,DialogContent,DialogFooter,DialogHeader,DialogTitle}from"@/components/ui/dialog";import{Input}from"@/components/ui/input";import{Label}from"@/components/ui/label";import{Select,SelectContent,SelectItem,SelectTrigger,SelectValue}from"@/components/ui/select";import{Textarea}from"@/components/ui/textarea";import{Badge}from"@/components/ui/badge";import{useToast}from"@/hooks/use-toast";
const today=()=>new Date().toISOString().slice(0,10);const ago=()=>new Date(Date.now()-30*864e5).toISOString().slice(0,10);const human=(v:string)=>v.replace(/_/g," ").replace(/\b\w/g,l=>l.toUpperCase());
export default function QapiDashboard(){const{user}=useAuth(),{viewingOrgId}=useViewingOrg(),{toast}=useToast(),org=viewingOrgId??user?.organizationId??undefined;const[fac,setFac]=useState(""),[open,setOpen]=useState(false);const facilities=useListFacilities({organizationId:org}),profiles=useListProfiles({organizationId:org});const projects=useListQapiProjects({organizationId:org,facilityId:fac||undefined});const metrics=useQapiSourceMetrics(fac,ago(),today());const create=useCreateQapiProject();const[title,setTitle]=useState(""),[problem,setProblem]=useState(""),[source,setSource]=useState("Incident and operational trend"),[baseline,setBaseline]=useState(""),[objective,setObjective]=useState(""),[target,setTarget]=useState(""),[completion,setCompletion]=useState(new Date(Date.now()+90*864e5).toISOString().slice(0,10)),[lead,setLead]=useState(user?.id??"");
const submit=()=>create.mutate({facilityId:fac,title,problem,source,baseline,objective,target,completion,lead},{onSuccess:id=>{toast({title:"QAPI project created"});setOpen(false);location.href=`/app/qapi/projects/${id}`},onError:(e:Error)=>toast({title:"Could not create QAPI project",description:e.message,variant:"destructive"})});
const metricEntries=Object.entries(metrics.data??{}).filter(([,v])=>typeof v==="number").slice(0,10);
return <div className="space-y-6"><div className="flex flex-wrap justify-between gap-3"><div><h1 className="flex items-center gap-2 text-2xl font-bold"><BarChart3 className="h-6 w-6"/>QAPI & Quality Management</h1><p className="text-muted-foreground">Measurable improvement projects grounded in existing CareBase operational evidence.</p></div><Button disabled={!fac} onClick={()=>setOpen(true)}><Plus className="mr-2 h-4 w-4"/>New QAPI project</Button></div><Card><CardContent className="pt-6"><Select value={fac} onValueChange={setFac}><SelectTrigger className="max-w-sm"><SelectValue placeholder="Select facility"/></SelectTrigger><SelectContent>{facilities.data?.map(f=><SelectItem key={f.id} value={f.id}>{f.name}</SelectItem>)}</SelectContent></Select></CardContent></Card>{fac&&<><div className="grid gap-3 sm:grid-cols-2 xl:grid-cols-5">{metricEntries.map(([k,v])=><Card key={k}><CardContent className="pt-5"><p className="text-2xl font-bold">{String(v)}</p><p className="text-xs text-muted-foreground">{human(k)}</p></CardContent></Card>)}</div><Card><CardHeader><CardTitle>Improvement projects</CardTitle><CardDescription>Problem statement, RCA, interventions, action items, measurement, sustainment, and closure approval.</CardDescription></CardHeader><CardContent className="space-y-3">{!projects.data?.length?<p className="py-8 text-center text-sm text-muted-foreground">No QAPI projects yet.</p>:projects.data.map(p=><div key={p.id} className="grid gap-3 rounded-lg border p-4 md:grid-cols-[1fr_180px_160px_auto] md:items-center"><div><p className="font-semibold">{p.project_number} · {p.title}</p><p className="line-clamp-2 text-sm text-muted-foreground">{p.problem_statement}</p></div><div><p className="text-sm">{p.lead?`${p.lead.first_name} ${p.lead.last_name}`:"Unassigned"}</p><p className="text-xs text-muted-foreground">Target {new Date(`${p.target_completion_date}T00:00:00`).toLocaleDateString()}</p></div><Badge variant="outline">{human(p.status)}</Badge><Button asChild size="sm"><Link href={`/app/qapi/projects/${p.id}`}>Open<ChevronRight className="h-4 w-4"/></Link></Button></div>)}</CardContent></Card></>}<Dialog open={open} onOpenChange={setOpen}><DialogContent className="max-w-2xl"><DialogHeader><DialogTitle>Create QAPI improvement project</DialogTitle></DialogHeader><div className="grid gap-3 sm:grid-cols-2"><div className="space-y-1 sm:col-span-2"><Label>Project title</Label><Input value={title} onChange={e=>setTitle(e.target.value)}/></div><div className="space-y-1 sm:col-span-2"><Label>Problem statement</Label><Textarea value={problem} onChange={e=>setProblem(e.target.value)}/></div><div className="space-y-1"><Label>Source of concern</Label><Input value={source} onChange={e=>setSource(e.target.value)}/></div><div className="space-y-1"><Label>Project lead</Label><Select value={lead} onValueChange={setLead}><SelectTrigger><SelectValue/></SelectTrigger><SelectContent>{profiles.data?.filter(p=>p.is_active).map(p=><SelectItem key={p.id} value={p.id}>{p.first_name} {p.last_name}</SelectItem>)}</SelectContent></Select></div><div className="space-y-1 sm:col-span-2"><Label>Baseline data</Label><Textarea value={baseline} onChange={e=>setBaseline(e.target.value)}/></div><div className="space-y-1 sm:col-span-2"><Label>Measurable objective</Label><Textarea value={objective} onChange={e=>setObjective(e.target.value)}/></div><div className="space-y-1"><Label>Target</Label><Input value={target} onChange={e=>setTarget(e.target.value)}/></div><div className="space-y-1"><Label>Target completion</Label><Input type="date" value={completion} onChange={e=>setCompletion(e.target.value)}/></div></div><DialogFooter><Button variant="outline" onClick={()=>setOpen(false)}>Cancel</Button><Button disabled={!title||problem.length<10||!fac||create.isPending} onClick={submit}><Target className="mr-2 h-4 w-4"/>Create project</Button></DialogFooter></DialogContent></Dialog></div>}
