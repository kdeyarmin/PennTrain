[
  {
    "file": "20260723120000_regulatory_updates_and_newsletter.sql",
    "rule": "anon-public-grant",
    "match": "public.list_regulatory_updates",
    "rationale": "Intentional public read path for the signed-out marketing regulatory-updates page, mirroring public.verify_certificate. list_regulatory_updates is SECURITY DEFINER with a fixed status='published' filter and search_path pinned to 'public', so anon callers can only read already-published feed rows -- draft/archived content and the base table stay unreachable (no anon table grant)."
  },
  {
    "file": "20260731120000_workflow_ux_backlog_remediation.sql",
    "rule": "anon-public-grant",
    "match": "public.resolve_safety_report_facility",
    "rationale": "Intentional public walk-up path for /report-safety posters and QR codes. SECURITY DEFINER with search_path pinned empty; accepts only an opaque facility token (or legacy UUID) and returns facilityId/name/token \u2014 never a facility directory. Mirrors public.verify_certificate: anon can execute the resolver, base table has no anon grant."
  },
  {
    "file": "20260830120000_annual_diabetes_education_foundations.sql",
    "rule": "anon-public-grant",
    "match": "public.verify_certificate",
    "rationale": "Re-grant of the pre-existing public certificate-verification path, unchanged in intent. verify_certificate had to be dropped and recreated to widen its RETURNS TABLE (course code, version label, credential number, examination score, training provider and credential), and a dropped function takes its grants with it. It stays SECURITY DEFINER with search_path pinned to 'public' and is still keyed on the certificate's opaque random slug, returning only what the printed certificate already states; the certificates table itself has no anon grant."
  },
  {
    "file": "20260830210000_snapshot_certificate_provider_at_issuance.sql",
    "rule": "anon-public-grant",
    "match": "public.verify_certificate",
    "rationale": "Re-grant of the same pre-existing public certificate-verification path allowlisted for 20260830120000, unchanged in intent and in exposure. verify_certificate is replaced here only to read the provider name and credential snapshotted onto the certificate at issuance instead of joining the mutable course_provider_profiles row, so an edit to that profile can no longer restate what an already-issued certificate claims. CREATE OR REPLACE preserves grants, but the statement is re-issued alongside the REVOKE so the function's access is stated in full at the point it is defined rather than inherited silently. Same signature, same columns, same SECURITY DEFINER with search_path pinned to 'public', and lookup still requires the unguessable per-certificate slug; no table grant to anon is added."
  }
]
