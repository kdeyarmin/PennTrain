{
  "_readme": [
    "In-app path literals in CareBase client source that check-frontend-route-links.mjs must not",
    "fail on.",
    "",
    "WHY THIS GATE EXISTS. check-server-route-links.mjs enforces that every in-app path built in a",
    "migration or an edge function matches a route declared in App.tsx. The client had the same",
    "defect and no check. carebaseGlossary.ts still carries the note: `/app/compliance` is not a",
    "route App.tsx registers -- these three sent the reader to the 404 page. check-server-route-links",
    "verifies exactly this rule, but only for links built in migrations and edge functions; a",
    "client-side registry like this one is outside its scope. Three dead links reached users, were",
    "repaired by hand, and the gap was written down instead of closed. Nothing stopped the fourth:",
    "Dashboard.tsx reported `route: \"/app/dashboard\"` to product telemetry for a page mounted at",
    "`/app`, so every benchmark_viewed row named a route that does not exist.",
    "",
    "THE EXPECTED USE OF THIS FILE is a literal that is a PREFIX TO MATCH rather than a place to go.",
    "The check reads every string literal in client source, which is what lets it see a path routed",
    "through a constant or a ternary; the cost is that a list of prefixes used with startsWith() looks",
    "the same as a list of destinations. Both entries below are that case, and each names the function",
    "that consumes it so the claim is checkable.",
    "",
    "It is NOT a place to excuse a link that is genuinely broken. Declare the route or correct the",
    "link instead."
  ],

  "/account": {
    "reason": "A prefix to match, not a destination. `productModules.ts` uses it in INTERNAL_APP_PREFIXES, which productModuleForPath() feeds to matchesPath() to decide whether a path is inside the app shell at all before any module gate is consulted. The routes underneath it are real and declared (/account/security, /account/notifications, /account/announcements, /account/whats-new, /account/manager-digest/:id); bare /account is deliberately not one of them.",
    "consumed_by": "productModuleForPath() in artifacts/caremetric-carebase/src/lib/productModules.ts"
  },
  "/app/regulatory": {
    "reason": "A prefix to match, not a destination. `helpCopilot.ts` lists it in the compliance_risk intent's routePrefixes, which routeMatches() tests against the user's current location to pick which help answer applies. It is intentionally the shared stem of /app/regulatory-crosswalk and /app/regulatory-copilot -- both declared routes -- so that either page resolves to the same intent. Nothing navigates to /app/regulatory.",
    "consumed_by": "routePrefixes matching in artifacts/caremetric-carebase/src/lib/helpCopilot.ts"
  }
}
